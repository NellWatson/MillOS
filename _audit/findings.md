# MillOS Launch Audit — Findings

_275 findings • 2 critical, 44 high, 96 medium, 133 low • autoFixable 94 • flagForOwner 166_


## CRITICAL & HIGH (all)

- **[CRITICAL]** `public/v0.20/` · L9 Performance · local · conf 0.95 · FLAG
  - **418MB duplicate v0.20/ asset tree ships to GitHub Pages in the build** — `du -sh public/v0.20` = 418M, nearly half the 840MB public/ dir. Full duplicate of all 26 MP3s (incl. 12MB 'Fuzzball Parade.mp3'), the 35MB forklift-original.glb/forklift.glb.backup.glb, warehouse.hdr, and every texture. Vite copies the entire publicDir to dist verbatim (no publicDir/copyPublicDir override in vite.config.ts, not in .gitignore). Only one src reference exists (a UIOverlay/ContextSidebar version label string), not an asset load. Deployed dead weight.
  - _FIX:_ Remove public/v0.20/ from the deployed tree (move out of public/ or exclude in a build step). It is a stale snapshot of a prior published build; runtime paths use import.meta.env.BASE_URL ('/').
- **[CRITICAL]** `src/utils/geminiClient.ts:135` · L4 API/Errors · local · conf 0.78 · FLAG
  - **Gemini model 'gemini-3-flash-preview' likely invalid for the legacy @google/generative-ai SDK — live AI silently dead at launch** — Line 135: `model: 'gemini-3-flash-preview'`. package.json pins `@google/generative-ai@^0.24.1` — the LEGACY Google Generative AI SDK (superseded by `@google/genai`). That SDK targets the generativelanguage v1beta endpoint whose served catalog is the Gemini 1.5/2.0 family; Gemini-3 models route through the newer @google/genai unified SDK. If the ID is not served, every `model.generateContent()` (geminiClient.ts:296) throws, `recordFailure()` (:317) increments, and after CIRCUIT_BREAKER_THRESHOLD=3 the breaker opens — the headline client-side Gemini feature falls back to heuristic-only with NO user-visible error (connectionError is never set at runtime). The GREEN baseline does not catch this: all 1199 tests mock the SDK; no test asserts/contract-checks the model string (grep confirmed). Corroborating same-root evidence the running model isn't what the code assumes: comments/logs are inconsistent — geminiClient.ts:146 logs 'Initialized with Gemini 2.0 Flash', aiEngine.ts:3866 says 'Gemini Flash 3'; and pricing constants aiConfigStore.ts:26-27 ($0.075 in / $0.30 out, 'as of Dec 2024') are 1.5-Flash numbers, so the cost overlay is wrong for any other model. Only a live smoke call against the user's key resolves validity definitively.
  - _FIX:_ Owner decision: confirm the exact model ID the user's API key + @google/generative-ai 0.24.1 actually serves (live smoke test). If Gemini 3 is intended, migrate to @google/genai and set a verified ID; otherwise set a known-served ID (e.g. a current 2.0/2.5-Flash). In the SAME change, fix the stale '2.0 Flash'/'Flash 3' log+comment strings and re-check the GEMINI_FLASH_*_COST constants against the chosen model's published pricing.
- **[HIGH]** `.github/workflows/ci.yml:17,32,41,57` · L12 Config · local · conf 0.9 · FLAG
  - **CI cannot fail on lint or test — green CI does not gate regressions** — The lint job has `continue-on-error: true` (line 17) AND `run: npm run lint --if-present; exit 0` (line 32). The test job has `continue-on-error: true` (line 41) AND `run: npm test -- --coverage --reporter=verbose || true` (line 57). The `|| true` masks ALL test failures, not just the Vitest worker crashes the comment cites. For a launch-readiness baseline that claims '1199 tests pass', CI provides no actual gate: a future lint or test regression merges green. The `build` job depends on `[lint, test]` but those never fail.
  - _FIX:_ Remove `exit 0` (revert to `npm run lint`) and `|| true` from the test step, and drop `continue-on-error: true` once flakiness is addressed; if Vitest worker crashes are the real concern, scope the tolerance to that specific exit condition rather than swallowing all failures. Requires owner sign-off (changes what CI blocks on).
- **[HIGH]** `.github/workflows/deploy.yml:79` · L12 Config · local · conf 0.8 · FLAG
  - **CNAME file never copied into the deployed Pages artifact** — deploy.yml builds into staging/ and uploads it: `upload-pages-artifact@v3 with: path: staging` (line 79). The root CNAME file (contents: www.millos.net) lives at repo root, is NOT inside public/, and is never copied into staging/. dist/ also has no CNAME (confirmed: dist contains only index.html, og-image.png, sw.js). The published artifact therefore has no CNAME at its root. GitHub Pages relies on the domain in repo settings OR a CNAME in the artifact; shipping without it risks the custom domain reverting/unsetting on deploy.
  - _FIX:_ Add a step before upload: `cp CNAME staging/CNAME` (and ideally also into staging/v0.30/ if subpaths are served directly). Resolve the apex/www question first (see related finding).
- **[HIGH]** `.github/workflows/release.yml:58-95` · L12 Config · local · conf 0.85 · FLAG
  - **release.yml deploys a SECOND, conflicting GitHub Pages site on tag push** — release.yml has a `deploy-ghpages` job (lines 58-95) that builds plain `npm run build` (base `/`, no VERSION) and deploys `dist/` to the Pages root via configure-pages/upload-pages-artifact/deploy-pages. This conflicts with deploy.yml, which structures Pages as `/v0.30/`, `/v0.20/`, `/v0.10/` with a root redirect to `/v0.30/`. On any `v*` tag push (e.g. v0.3, which already exists), release.yml overwrites the entire Pages site with a flat root build, destroying the versioned layout and breaking the `/v0.30/` URLs. Both target the same `github-pages` environment.
  - _FIX:_ Remove the `deploy-ghpages` job from release.yml (let deploy.yml own all Pages deploys), OR make it mirror deploy.yml's versioned staging. Owner decision: which workflow owns Pages.
- **[HIGH]** `.gitignore:1-35` · L12 Config · local · conf 0.97 · -
  - **coverage/ not gitignored — 48 generated coverage files tracked in git** — `.gitignore` has no `coverage/` entry (verified: grep returns NOT IN GITIGNORE). `git ls-files coverage/` returns 48 tracked artifacts (coverage/base.css, coverage-final.json, index.html, etc.) — Istanbul/v8 HTML report output that should never be committed. These regenerate on every `npm run test:coverage` and pollute diffs.
  - _FIX:_ Add `coverage/` to `.gitignore`. NOTE: the gitignore line alone will NOT untrack existing files — also run `git rm -r --cached coverage/` and commit so the 48 files leave the tree.
- **[HIGH]** `GeminiSettingsModal.tsx:266-277` · L3 · local · conf 0.9 · FLAG
  - **No disclosure plant data sent to Google Gemini** — Only AI Studio link shown; factory state (aiEngine.ts:3437 via geminiClient.generateContent) sent to generativelanguage.googleapis.com undisclosed. GDPR transparency gap.
  - _FIX:_ Add notice MillOS sends simulation state to Google Gemini under Google Privacy Policy.
- **[HIGH]** `README.md:711` · L7 Copy · local · conf 0.92 · FLAG
  - **Quick Start env setup is stale: .env.local.example missing and GEMINI_API_KEY build-injection is disabled** — README Quick Start (lines 711-713) says: `cp .env.local.example .env.local` then `# Add your GEMINI_API_KEY to .env.local`. But .env.local.example does NOT exist in the repo, and vite.config.ts:75-78 has the env injection commented out (`// 'process.env.GEMINI_API_KEY': JSON.stringify(env.GEMINI_API_KEY)`) with note 'API keys should NOT be embedded in client bundles'. The key is actually entered at runtime via the settings modal and persisted in localStorage (aiConfigStore.ts:553 name 'millos-ai-config'). Following the README cp step fails (no source file) and the GEMINI_API_KEY env approach has no effect.
  - _FIX:_ Rewrite the env step to describe the real flow: run the app, open AI/Gemini settings, paste the key (stored in browser localStorage). Remove the `cp .env.local.example` line or add an actual .env.local.example. Owner should confirm intended onboarding UX.
- **[HIGH]** `SettingsPanel.tsx:458-468` · L3 · local · conf 0.92 · FLAG
  - **Reset clears 5 of 16 keys; plaintext Gemini key survives** — Reset removes 5 millos keys, leaves millos-ai-config plaintext key (aiConfigStore.ts:553-560) and 10 others. GraphicsSettingsPanel.tsx:623-624 same. GDPR erasure gap.
  - _FIX:_ Add Clear all data removing every millos key or a Forget API key button.
- **[HIGH]** `a.tsx:1` · L5 A11y · local · conf 0.9 · AUTO
  - **t1** — d1
  - _FIX:_ f1
- **[HIGH]** `b.tsx:2` · L5 A11y · local · conf 0.9 · AUTO
  - **t2** — d2
  - _FIX:_ f2
- **[HIGH]** `index.html:11` · L12 Config · local · conf 0.85 · AUTO
  - **CSP connect-src omits PeerJS cloud API host (https://0.peerjs.com) -> multiplayer signaling blocked** — connect-src is: "connect-src 'self' ws: wss: https://generativelanguage.googleapis.com https://cdn.jsdelivr.net https://fonts.gstatic.com https://fonts.googleapis.com;". PeerJS (src/multiplayer/SignalingService.ts uses the default cloud config) defaults to CLOUD_HOST="0.peerjs.com", CLOUD_PORT=443 (verified in node_modules/peerjs/dist/peerjs.min.js). The initial peer-discovery handshake is an HTTPS XHR/fetch to https://0.peerjs.com:443 which is NOT matched by 'self', ws:, or wss:. CSP will block it, so new Peer() never reaches 'open' and WebRTC multiplayer fails on the live GH-Pages site (where this meta CSP is enforced). The bare wss: token does cover the subsequent signaling WebSocket, so only the HTTPS API call is missing.
  - _FIX:_ Add https://0.peerjs.com to connect-src (and https://*.peerjs.com if self-hosting is ever swapped in). STUN/TURN (stun.l.google.com, *.turn.peerjs.com) are governed by ICE, not connect-src, so no CSP change needed for those.
- **[HIGH]** `index.html:11` · L2 Security · local · conf 0.85 · AUTO
  - **CSP connect-src omits www.gstatic.com → DRACO decoder fetch blocked, breaks 3D models in production** — connect-src lists 'https://fonts.gstatic.com' but NOT 'https://www.gstatic.com'. src/utils/dracoLoader.ts:33 sets DRACO_DECODER_PATH = 'https://www.gstatic.com/draco/versioned/decoders/1.5.7/' and useDracoGLTF() is called at runtime by src/components/models/WorkerModel.tsx:46 and ForkliftModel.tsx:48. Three's DRACOLoader._loadLibrary (node_modules/three/.../DRACOLoader.js:331-368) fetches draco_decoder.js/wasm via THREE.FileLoader (fetch/XHR — governed by connect-src). CSP host-source matching is per-host: 'fonts.gstatic.com' does NOT match 'www.gstatic.com'. forklift.glb is 1.6MB (vs forklift-original.glb at 36MB) = DRACO-compressed, so the decoder fetch fires and is blocked under the enforced CSP, leaving DRACO GLBs unrenderable. Tests are green because they never run under an enforced CSP in a browser. The SW (public/sw.js:142) already allowlists 'gstatic.com/draco' for caching, confirming intent — only the CSP was missed.
  - _FIX:_ Add 'https://www.gstatic.com' to the connect-src directive in index.html (line 11), e.g. '...generativelanguage.googleapis.com https://www.gstatic.com https://cdn.jsdelivr.net...'. Alternatively switch dracoLoader.ts to the local '/draco/' decoder path (commented at line 36) and vendor the decoder into public/draco/ to avoid any third-party connect entirely.
- **[HIGH]** `index.html:27` · L7 Copy · local · conf 0.9 · FLAG
  - **OG/Twitter image is a reused 3456x1993 screenshot but meta declares 1200x630** — index.html:27 `<meta property="og:image" content="https://millos.net/og-image.png" />` with declared dimensions og:image:width=1200 (line 28) / og:image:height=630 (line 29). The actual file public/og-image.png is 3456x1993 px (PNG header) and is BYTE-IDENTICAL (2,189,012 bytes) to src/0.10 Archive/assets/Screenshot.png — it is the interior screenshot copied in, not a purpose-built 1.91:1 OG card. Social platforms read the declared 1200x630 (matching the actual aspect ratio 1.74:1, ~16:9) and will crop/letterbox; some validators flag the mismatch.
  - _FIX:_ Replace public/og-image.png with a real 1200x630 social card, OR correct the og:image:width/height meta to the true 3456x1993 (and accept the larger upload). Owner decision on whether to author a proper card vs adjust metadata.
- **[HIGH]** `index.html:28` · L12 Config · local · conf 0.97 · FLAG
  - **og:image declared 1200x630 but public/og-image.png is actually 3456x1993** — Lines 28-29 declare <meta property="og:image:width" content="1200" /> and <meta property="og:image:height" content="630" />, but `sips` reports the real file is pixelWidth 3456 x pixelHeight 1993 (aspect 1.73:1, not the 1.91:1 of 1200x630), and the PNG is 2.09 MB. Mismatched dimension hints cause some scrapers to skip/crop the card; the oversized file also slows/blocks unfurling on platforms with a ~5MB-but-strict-timeout policy or 8MB hard cap.
  - _FIX:_ Re-export og-image.png at exactly 1200x630 (or update the width/height meta to the true 3456x1993 and compress under ~1MB). Preferred: regenerate a 1200x630 <300KB PNG/JPG to match the declared dims.
- **[HIGH]** `public/Fuzzball Parade.mp3 (+ 25 more)` · L9 Performance · local · conf 0.9 · FLAG
  - **26 high-bitrate MP3s total ~140MB; several 9-12MB single tracks** — find totals: 279712 KB across 52 MP3s (26 unique + 26 v0.20 dupes) = ~140MB unique music. Largest: Fuzzball Parade 12M, Interloper 10M, Killers 9.3M, Dark Fog 9.1M, All This 8.7M. audioManager.ts streams on demand via `this.musicAudio.src = this.currentTrack.file` (lines 389/402/716/734), so not all load at boot, but per-track size is excessive and the SW AUDIO_CACHE (sw.js:42,59) cache-firsts every played track, ballooning Cache Storage toward ~140MB over a session.
  - _FIX:_ Re-encode music to ~96-128kbps (target <2MB/track) via ffmpeg; background loops don't need high bitrate. Owner decision on quality/size. Cuts ~100MB+ from worst-case cached/transferred audio.
- **[HIGH]** `public/models/forklift/forklift-original.glb + forklift.glb.backup.glb` · L9 Performance · local · conf 0.9 · FLAG
  - **144MB of unreferenced GLB backups/originals shipped (35MB each x2 + worker backups)** — find: forklift-original.glb = 35M and forklift.glb.backup.glb = 35M (plus v0.20 dupes = 140MB across four, and worker.glb.backup.glb 480K). Runtime only loads forklift.glb (1.6M) per MODEL_PATHS in modelLoader.ts:44. grep for 'forklift-original'/'forklift.glb.backup' in src returned ZERO references. Pure dead weight in the deployed dir.
  - _FIX:_ Delete *-original.glb and *.backup.glb under public/models/ (and v0.20). Only forklift.glb (1.6M) and worker.glb (136K) are referenced.
- **[HIGH]** `public/textures/machines/downloads/` · L9 Performance · local · conf 0.92 · FLAG
  - **173MB of texture SOURCE cruft (.blend/.zip/.usdc/.tres/.mtlx) ships in public/** — `du -sh public/textures/machines/downloads` = 173M. Contains 26 each of .zip/.usdc/.blend/.tres/.mtlx source/archive files plus redundant NormalGL+NormalDX JPG pairs (~2.3MB each). These are raw AmbientCG download bundles, not runtime-loaded textures, yet all are copied to dist and served.
  - _FIX:_ Delete public/textures/machines/downloads/ (or move to a non-public source folder). Keep only the runtime-referenced JPG/KTX2 maps. Verify against texture load paths in src before deleting.
- **[HIGH]** `src/components/GeminiSettingsModal.tsx:185` · L5 A11y · local · conf 0.95 · -
  - **GeminiSettingsModal lacks dialog semantics + focus trap (sibling AboutModal has them)** — The modal container is a plain `<motion.div ... className="relative w-full max-w-md ...">` (line 185) with no `role="dialog"`, no `aria-modal="true"`, no `aria-labelledby`, and no focus trap / ESC handler / return-focus. There is no `useFocusTrap` call and no `onKeyDown` Escape handler anywhere in the file. The h2 'Gemini AI Settings' (line 196) is not wired as the accessible name. Keyboard users cannot ESC-close, focus is not trapped inside, and focus is not returned on close. The repo already has a working `useFocusTrap(ref, isOpen, onClose)` hook (src/hooks/useFocusTrap.ts) that AboutModal.tsx:19 uses for exactly this.
  - _FIX:_ Mirror AboutModal: add `const modalRef = useRef<HTMLDivElement|null>(null); useFocusTrap(modalRef as React.RefObject<HTMLElement>, isOpen, onClose);`. On the inner motion.div (line 185) add `ref={modalRef} role="dialog" aria-modal="true" aria-labelledby="gemini-settings-title"` and give the h2 (line 196) `id="gemini-settings-title"`. This changes runtime behavior (ESC closes, focus traps), so it is high-value but not silent.
- **[HIGH]** `src/components/GeminiSettingsModal.tsx:266` · L7 Copy · local · conf 0.85 · FLAG
  - **API-key field has no privacy note** — Only the AI Studio link (266-276). Key stored plaintext in localStorage millos-ai-config (aiConfigStore.ts:553); no note it stays in-browser and MillOS never receives it.
  - _FIX:_ Add note: stored only in this browser, sent direct to Google, MillOS never receives it.
- **[HIGH]** `src/components/TruckBay.tsx:2877-2946,3274-3551` · L1 Correctness/Bugs · local · conf 0.6 · FLAG
  - **Dock-equipment animations driven by ref reads in render never react to docking cycle** — WheelChock, DockLeveler, DockPlate, DockSpotter, StretchWrapMachine and WarehouseWorker receive their reactive booleans from refs that are mutated inside useFrame but read during render, e.g. line 2877 `<DockLeveler position={[0, 2, -1.5]} isDeployed={shippingDockedRef.current} />`, line 2925 `isDeployed={shippingDockedRef.current}`, line 2941 `isGuiding={ shippingStateRef.current === 'backing' || ... }`. `shippingDockedRef`/`shippingDoorsOpenRef`/`shippingStateRef` are assigned inside the useFrame loop (e.g. line 2627 `shippingDockedRef.current = truckState.phase === 'docked' || ...`). Reading `.current` during render does NOT trigger a re-render when the ref mutates. The cleanest victims are WheelChock (effect deps `[isDeployed]`, line 137) and DockLeveler (effect deps `[isDeployed]`, line 2550) — their deploy/lerp animation only re-registers when the prop value actually changes, which it never does mid-cycle. TruckBay (a non-memoized React.FC) only re-renders when its parent MillScene re-renders, and MillScene's productionSpeed prop comes from App's `useState(0.8)` slider — so re-renders are user-driven and infrequent. Net effect: wheel chocks, dock levelers, dock plates and the spotter's wave do not deploy/retract in sync with trucks docking; they stay frozen at whatever value was captured at the last unrelated re-render. The internal `isGuidingRef.current = isGuiding` in DockSpotter (line 1430) proves the author intended live reactivity.
  - _FIX:_ Lift the truck dock/door/phase state that drives equipment into React state (or subscribe the equipment to the production store dock-status values that useFrame already writes via setTruckDocked/updateDockStatus), so prop changes re-render the equipment. Minimal version: replace the docked/doorsOpen/phase refs that feed JSX props with useState updated in useFrame (throttled to state transitions only, which the code already detects). All edits stay inside TruckBay.tsx.
- **[HIGH]** `src/components/UIOverlay.tsx:2071` · L5 A11y · local · conf 0.95 · AUTO
  - **MachineInfoPanel close button has no accessible name (announces as "multiplication sign")** — The close button's only content is the "×" character and it has no aria-label or title:
```
<button
  onClick={onClose}
  className="text-slate-500 hover:text-white w-6 h-6 flex items-center justify-center rounded hover:bg-slate-800 transition-colors"
>
  ×
</button>
```
Per the accessible-name computation, element content wins, so screen readers announce this button as "× , button" (literally "multiplication sign"), giving no indication it closes the panel. The in-repo correct pattern is WorkerDetailPanel.tsx:440 (`aria-label={`Close ${worker.name} details`}`).
  - _FIX:_ Add `aria-label="Close machine details"` (and ideally `aria-hidden="true"` on the × glyph or replace with the Lucide `X` icon to match the no-emoji/icon convention).
- **[HIGH]** `src/components/multiplayer/MultiplayerLobby.tsx:34` · L6 UX · local · conf 0.9 · FLAG
  - **Lobby has no host-disconnect feedback** — Lobby lacks host-disconnected listener Panel(57-82) has; guest silently dumped to join form.
  - _FIX:_ Add host-disconnected listener to Lobby.
- **[HIGH]** `src/components/ui-new/dock/Dock.tsx:106` · L8 Visual · local · conf 0.65 · FLAG
  - **Mobile bottom Dock overflows screen width - no wrap/scroll/max-w guard** — The primary nav <nav> at line 106-110 is a single flex row 'fixed bottom-6 left-1/2 -translate-x-1/2 ... flex items-center' with NO flex-wrap, overflow-x-auto, or max-w. On mobile it renders 8 DockItems + a divider + Datalinks + FPS toggle + Fullscreen button, each with 'min-w-[44px] min-h-[44px]' (lines 199, 213, 235, 264) plus 'gap-1' and 'px-2'. That is ~11 touch targets x 44px = ~484px + gaps/padding ~= 540px. Centered with left-1/2 -translate-x-1/2, this exceeds every common phone width (360-414px), so the leftmost and rightmost dock items clip off both screen edges and become unreachable. This is the main navigation on the deployed mobile path (millos.net).
  - _FIX:_ On mobile add a width cap + horizontal scroll or wrap: e.g. add 'max-w-[100vw] overflow-x-auto no-scrollbar' (with safe-area-aware padding) to the nav when isMobile, OR reduce the mobile dock to a core subset and move secondary actions (Datalinks/FPS/Fullscreen) into an overflow menu. Which items to drop vs scroll is a product decision.
- **[HIGH]** `src/components/ui-new/panels/SettingsPanel.tsx:458` · L6 UX · local · conf 0.95 · FLAG
  - **Second 'Reset Simulation' button (active new-UI panel) also wipes localStorage + reloads without confirmation** — Lines 458-473: red 'Reset Simulation' button's onClick removes millos-settings/millos-game-simulation/millos-has-played/millos-ai-narration/millos-knowledge then `setGraphicsQuality('medium'); window.location.reload();`. No confirmation. This is the panel surfaced by the current Dock/Sidebar UI, so it is the one most users will actually hit.
  - _FIX:_ Add `if (!window.confirm('Reset the simulation and clear all saved data? This cannot be undone.')) return;` at the top of the onClick handler.
- **[HIGH]** `src/components/ui-new/sidebar/ContextSidebar.tsx:33` · L9 Performance · local · conf 0.8 · FLAG
  - **MultiplayerPanel imported EAGERLY, pulling peerjs into the boot path** — Line 33: `import { MultiplayerPanel } from '../panels/MultiplayerPanel';` is a static (non-lazy) import, unlike the ~18 sibling panels (lines 20-76) which all use lazy(() => import(...)). MultiplayerPanel.tsx:27 imports getMultiplayerManager from ../../../multiplayer, whose MultiplayerManager.ts:13 imports SignalingService which imports peerjs. ContextSidebar renders eagerly inside GameInterface. Result: the 'multiplayer' chunk (peerjs WebRTC) is requested on initial load for users who never touch multiplayer.
  - _FIX:_ Make MultiplayerPanel lazy to match siblings: `const MultiplayerPanel = lazy(() => import('../panels/MultiplayerPanel').then(m => ({ default: m.MultiplayerPanel })));` and wrap its render at line 168 in the existing Suspense. Defers peerjs to first panel open.
- **[HIGH]** `src/components/ui/AlertAcknowledgmentFlow.tsx:64` · L5 A11y · local · conf 0.9 · AUTO
  - **Toast alert container has no aria-live region (status messages not announced)** — The alert/toast container is a plain div: `<div className="fixed bottom-4 right-4 z-40 space-y-2 max-w-sm">` (line 64). It renders dynamic warning/critical alerts (alert.title/alert.message, lines 91-92) that appear and disappear, but has no role="region"/aria-live="polite" (and critical alerts have no role="alert"). Screen-reader users receive no notification when an alert is added. Violates WCAG 4.1.3 Status Messages.
  - _FIX:_ Add role="region" aria-live="polite" aria-label="Notifications" to the container div (and consider role="alert" / aria-live="assertive" for type==='critical' cards). No behavior change.
- **[HIGH]** `src/components/ui/DecisionReplay.tsx:71` · L5 A11y · local · conf 0.85 · FLAG
  - **Decision-replay modal lacks dialog role/aria-modal/ESC/focus management** — Modal root `<motion.div ... className="fixed inset-0 bg-black/60 ... z-[1000] flex items-center justify-center p-4" onClick={onClose}>` (line 71-77) and panel (line 78) have no role="dialog", aria-modal, aria-label, focus trap, or Escape-key handler. The close X (lines 96-101) is icon-only with no aria-label (`<X className="w-5 h-5 text-slate-400" />`). Violates WCAG 4.1.2, 2.1.2, 2.4.3.
  - _FIX:_ Add role="dialog" aria-modal="true" aria-label="Decision replay" to the panel; add Escape keydown -> onClose; add aria-label="Close" to the X button + aria-hidden on the icon; manage initial/return focus. ESC behavior is a UX addition.
- **[HIGH]** `src/components/ui/GraphicsSettingsPanel.tsx:621` · L6 UX · local · conf 0.95 · FLAG
  - **Destructive 'Reset Simulation' wipes localStorage and reloads with NO confirmation** — Lines 621-635: `onClick={() => { localStorage.removeItem('millos-settings'); localStorage.removeItem('millos-game-simulation'); setGraphicsQuality('medium'); window.location.reload(); }}`. A single click on a red button immediately erases persisted progress and force-reloads the page — no `window.confirm()` or in-UI confirmation step. A misclick destroys the user's session permanently.
  - _FIX:_ Wrap the handler in a confirmation: `if (!window.confirm('Reset the entire simulation? This clears saved progress and reloads.')) return;` before the removeItem calls, or gate behind a two-step confirm UI like the existing showGeminiConfirmation pattern in GeminiSettingsModal.
- **[HIGH]** `src/components/ui/KeyboardShortcutsModal.tsx:72` · L5 A11y · local · conf 0.85 · FLAG
  - **Modal lacks dialog role, aria-modal, focus trap, and ESC-to-close** — The modal root `<motion.div ... className="fixed inset-0 z-[1000] ... " onClick={onClose}>` (line 72-79) and its content panel (line 83) have no role="dialog", no aria-modal="true", no aria-labelledby pointing at the title h2 (line 102), no focus trap, and no Escape key handler — even though the footer (lines 166-176) and its own shortcut list (line 56 'ESC: Close panels') tell users ESC closes panels. Keyboard/AT users cannot escape or are not told they are in a dialog. Violates WCAG 4.1.2, 2.1.2 (no keyboard trap requires proper handling), 2.4.3 focus order.
  - _FIX:_ On the inner panel add role="dialog" aria-modal="true" aria-labelledby="kbd-shortcuts-title" (give the h2 that id); add a useEffect keydown listener calling onClose() on 'Escape'; move initial focus into the dialog and restore on close (or trap focus). UX adds ESC behavior, so owner should confirm.
- **[HIGH]** `src/hooks/useAdaptiveQuality.ts:23` · L9 Performance · local · conf 0.95 · FLAG
  - **Adaptive-quality system is dead code** — useAdaptiveQuality and adaptiveQualityManager appear only in their two definition files; no component calls the hook, so auto-downgrade never runs.
  - _FIX:_ Wire the hook into an always-mounted Canvas component with a non-destructive merge, or delete as dead code.
- **[HIGH]** `src/main.tsx:3,50` · L9 Performance · local · conf 0.85 · FLAG
  - **4200-line AssetPrototypePage statically imported into main entry, parsed on every visit** — Line 3: `import AssetPrototypePage from './prototypes/AssetPrototypePage';` (4200 lines per wc -l). Line 50: `const RootComponent = isPrototypeRoute() ? AssetPrototypePage : App;`. Static import bundles the prototype tool's entire JS into the main entry chunk, downloaded+parsed by EVERY normal millos.net visitor though the prototype route is dev tooling. Confirms the brief's 'prototypes 2nd input shipped?' concern: it's pulled into main, not just prototypes/index.html.
  - _FIX:_ Lazy-load: `const AssetPrototypePage = React.lazy(() => import('./prototypes/AssetPrototypePage'))` rendered under Suspense only when isPrototypeRoute() is true. Behavior-changing (adds Suspense on the prototype route), so flag for owner.
- **[HIGH]** `src/multiplayer/MultiplayerManager.ts:280` · L4 API/Errors · ripple · conf 0.85 · FLAG
  - **Host trusts unvalidated, spoofable fields from untrusted guest messages (no payload validation, no default case)** — handleMessage (:280) switches on message.type with zero shape/identity validation; incoming data arrives as `data as MultiplayerMessage` (PeerConnection.ts:50) — an unchecked cast. Concrete authority/spoofing harms, not a crash (PeerConnection.ts:47-55 wraps handling in a silent try/catch, so malformed payloads are dropped, not thrown): (1) PLAYER_UPDATE → `store.updateRemotePlayer(message.payload.id, message.payload)` (:285) lets ANY guest overwrite ANOTHER player's position/state by spoofing payload.id. (2) CHAT (:355) takes guest-supplied `from`/`fromName` verbatim via addChatMessage — trivial impersonation. (3) PLAYER_JOIN/PLAYER_LEAVE (:288,:302) are accepted from guests, letting a guest inject or evict phantom players. (4) No `default` branch — unknown types silently ignored, and any non-string/NaN numeric field flows straight into the store and the 3D render path unguarded. Peers are explicitly untrusted (public PeerJS cloud server, predictable host ID `millos-{roomCode}`).
  - _FIX:_ Add a validateMessage(peerId, message) guard at the top of handleMessage: assert message.type is a known literal, assert payload exists and required fields are correct primitive types (finite numbers for positions/rotation, string ids), and reject any message whose payload.id/from does not match the authenticated sender peerId for PLAYER_UPDATE/CHAT (host should derive identity from the connection, not the payload). Add a `default:` that logs+drops. Keep it in MultiplayerManager; payload type literals already exist in multiplayer/types.ts.
- **[HIGH]** `src/multiplayer/MultiplayerManager.ts:284-285` · L1 Correctness/Bugs - Integration (SCADA+multiplayer+protocols) · local · conf 0.85 · FLAG
  - **Host never relays PLAYER_UPDATE → remote players frozen at spawn in 3+ player sessions** — Topology is a star: guests connect only to the host (SignalingService.connectToHost, line 117). On receiving a guest's position the host only updates its own store and stops: `case 'PLAYER_UPDATE': store.updateRemotePlayer(message.payload.id, message.payload); break;` — no rebroadcast to other peers. The periodic STATE_SYNC payload (lines 448-458) carries `machines/gameTime/weather/emergencyActive` but NO player positions. So guest A's movement reaches the host and dies there; guest B never receives it and renders A frozen at the spawn position `[0, 1.7, 0]` set in addRemotePlayer (line 178) / PLAYER_JOIN handler (line 292). Two-player sessions work (host-guest is direct), which masks the bug.
  - _FIX:_ In the host branch of the PLAYER_UPDATE case, after updating the store, rebroadcast the update to all other peers excluding the sender: `if (store.isHost) this.broadcast(message, peerId);`. Verify against the message contract before shipping.
- **[HIGH]** `src/multiplayer/MultiplayerManager.ts:323-340` · L2 Security · local · conf 0.88 · FLAG
  - **Host trusts client-supplied playerId in INTENT (player impersonation / authority bypass)** — case 'INTENT': const result = this.onMachineIntent(message.payload). The host executes message.payload (a MachineIntent) whose playerId field is fully attacker-controlled by the sending peer. Nothing binds the intent's playerId to the authenticated `peerId` of the connection it arrived on. A malicious guest can submit intents tagged with another player's id (or the host's id), defeating any per-player attribution, machine-lock ownership, or rate logic in the intent handler. Same trust gap exists in handlePeerDisconnected (line 252-255) which removes a player based on connection metadata.playerId, and in handleMessage PLAYER_LEAVE (line 302-303) where any peer can send PLAYER_LEAVE with an arbitrary id to evict another player from the host's store.
  - _FIX:_ On the host, override the trusted identity: build the effective intent from the connection's authenticated peerId rather than the payload, e.g. const trustedPlayerId = this.peerIdToPlayerId.get(peerId); this.onMachineIntent({...message.payload, playerId: trustedPlayerId}). For PLAYER_LEAVE, ignore the payload id when isHost and use the peerId mapping. Maintain a peerId->playerId map populated from the verified connection at join time.
- **[HIGH]** `src/multiplayer/MultiplayerManager.ts:98-105` · L1 Correctness/Bugs - Integration (SCADA+multiplayer+protocols) · local · conf 0.8 · FLAG
  - **Join-timeout destroys singleton without nulling it → subsequent multiplayer attempts use a poisoned manager** — joinRoom's connection-timeout callback calls `this.destroy()` (line 103) which sets `isDestroyed = true` but does NOT null the module-level `multiplayerManager` singleton — only the exported `destroyMultiplayerManager()` (line 680) does. After a join times out, the next `getMultiplayerManager()` (line 673) returns the same poisoned instance, and every broadcast/state-sync interval body short-circuits via `if (this.isDestroyed) return` (lines 419, 443, 464). Normal leave is fine because UI calls leave() then destroyMultiplayerManager() (MultiplayerLobby.tsx:99-100, MultiplayerPanel.tsx:130-131); the internal destroy() paths (joinRoom timeout, and any future internal teardown) are the gap.
  - _FIX:_ Have internal teardown null the singleton: call the module-level destroyMultiplayerManager() from the joinRoom timeout instead of this.destroy(), or add an isDestroyed reset/recreate guard in getMultiplayerManager() so a destroyed instance is replaced. Confirm no double-destroy regression first.
- **[HIGH]** `src/multiplayer/PeerConnection.ts:47-55` · L2 Security · local · conf 0.9 · FLAG
  - **Untrusted peer DataChannel payload passed to handlers with zero validation** — conn.on('data', (data) => { ... const message = data as MultiplayerMessage; this.handleMessage(message); }). The incoming WebRTC data from an untrusted peer is type-asserted (`as MultiplayerMessage`) and dispatched directly. The empty catch only guards a synchronous throw that never happens here. By contrast WebSocketAdapter.handleMessage validates via isValidWSMessage() before use. A malicious peer can send `{type:'PLAYER_JOIN', payload:{...}}` with arbitrary id/name/color, or any message with a missing/malformed payload, and it flows straight into MultiplayerManager.handleMessage -> Zustand store mutations (store.addRemotePlayer, store.setMachineLock, store.resolveIntent, store.addChatMessage). message.payload.timestamp in the PING handler is dereferenced unchecked (line 79) - a PING with no payload throws and is silently swallowed, but the real risk is the unvalidated objects reaching store mutators.
  - _FIX:_ Add a validateMultiplayerMessage(data): data is MultiplayerMessage type-guard (mirroring messageValidation.ts/isValidWSMessage): check data is an object, type is one of the known string literals, and the payload shape matches per-type (e.g. PLAYER_JOIN requires id:string,name:string,color in PLAYER_COLORS). Reject (return without dispatch, increment an error counter) on failure. Wire it at the top of PeerConnection.handleMessage before any branch.
- **[HIGH]** `src/multiplayer/SignalingService.ts:26-30` · L3 Privacy/GDPR — Multiplayer data sharing · local · conf 0.85 · FLAG
  - **WebRTC exposes every peer's public IP address to untrusted peers with no consent or disclosure** — PEERJS_CONFIG passes no `config`/`iceServers`, so PeerJS falls back to its built-in default ICE servers (third-party): `const PEERJS_CONFIG = { debug: process.env.NODE_ENV === 'development' ? 2 : 0 };`. WebRTC connection establishment inherently exchanges ICE candidates that reveal each participant's public IP address (often local network IPs too) to every other peer in the room. The task states peers are untrusted, so every participant learns every other participant's IP. An IP address is personal data under GDPR (Recital 30 / Breyer C-582/14). It is disclosed with zero consent flow, zero disclosure, and no privacy policy anywhere in the app. This is the strongest GDPR exposure and does not depend on PeerJS broker internals.
  - _FIX:_ Owner decision required. Mitigations: (a) display a clear pre-join disclosure in MultiplayerLobby that joining a room reveals your IP address to other participants and routes signaling through a third-party broker; (b) optionally configure a TURN relay with `iceTransportPolicy: 'relay'` to hide peer IPs (cost/perf tradeoff); (c) publish a privacy policy covering this. Do not hardcode a specific STUN host as a 'fact'.
- **[HIGH]** `src/multiplayer/SignalingService.ts:85-90` · L6 UX · ripple · conf 0.85 · FLAG
  - **Room-not-found error never reaches join UI** — onError sets disconnected, never rejects joinRoom; lobby try/catch never fires, user silently bounced.
  - _FIX:_ Reject initialize() on peer-unavailable or set store error field.
- **[HIGH]** `src/scada/adapters/RESTAdapter.ts:173` · L4 API/Errors · local · conf 0.8 · AUTO
  - **REST batch read does not validate response shape before .map — malformed 200 response trips false connection-loss** — `const data: RESTBatchResponse = await response.json(); const tagValues = data.tags.map(...)`. There is no check that `data` is an object or that `data.tags` is an array. A 200 response with an unexpected/empty body (`{}`, an array, a server returning `{error:...}`) throws `TypeError: Cannot read properties of undefined (reading 'map')`. Because `readTags` is called from `readAllTags` → `poll()` (line 290) whose catch routes to `handleConnectionError()` (line 293), a single malformed-but-OK response is misclassified as a lost connection, incrementing reconnectAttempts and ultimately calling disconnect() which drops all subscribers (see separate finding).
  - _FIX:_ After json(), guard: `if (!data || !Array.isArray(data.tags)) throw new Error('Malformed batch response');` and validate each entry has tagId/value/quality/timestamp (mirror isValidBatchTag in messageValidation.ts) before parseTagResponse. Same guard belongs on readTag's `data` at line 145.
- **[HIGH]** `src/scada/adapters/RESTAdapter.ts:304` · L4 API/Errors · local · conf 0.78 · FLAG
  - **After 5 reconnect failures REST adapter silently disconnects and clears all subscribers with no consumer notification** — In handleConnectionError: `if (this.reconnectAttempts >= 5) { this.disconnect(); }`. disconnect() (lines 108-123) clears `this.subscribers` and `this.globalSubscribers`. Consumers that registered callbacks via subscribe() are never told the stream died — their callbacks just stop firing forever, and there is no error event/callback surfaced. `lastError` stays the generic 'Connection lost'. UI bound to live tags will freeze on stale data with no indication. Compare WebSocketAdapter which keeps retrying up to 10 times rather than tearing down subscribers.
  - _FIX:_ On terminal failure, set a distinct lastError (e.g. 'Reconnect limit reached') and emit a connection-status change rather than silently clearing subscribers; or do not clear subscribers in the auto-disconnect path so a later manual reconnect can resume. Needs a product call on desired reconnect ceiling/UX.
- **[HIGH]** `src/scada/adapters/WonderwareAdapter.ts:92` · L4 API/Errors · local · conf 0.85 · FLAG
  - **Wonderware historian base URL hardcoded to plaintext http:// — Basic-auth credentials sent in clear** — `this.baseUrl = \`http://${this.serverHost}:${this.serverPort}/Historian/v1\`;` is hardcoded to http. The constructor builds a Basic-auth header from username/password/domain (lines 95-102: `this.authHeader = \`Basic ${credentials}\``) and `fetchWithAuth` (line 318-320) sends it on every request. With no TLS option, historian credentials (potentially Windows domain creds) traverse the network base64-encoded but unencrypted. On a millos.net (https) page this is also a mixed-content request the browser will block, so the feature silently fails AND leaks creds where it does work. There is no `protocol`/`useSsl`/`https` config field in WonderwareConnectionConfig to override it.
  - _FIX:_ Add an optional `useSsl?: boolean` (or `baseUrlOverride`) to WonderwareConnectionConfig in HistorianInterface.ts and derive scheme (`https://` default) instead of hardcoding `http://`. Defaulting to https is the safe posture for SCADA credentials.
- **[HIGH]** `src/scada/adapters/WonderwareAdapter.ts:92` · L2 Security · local · conf 0.85 · FLAG
  - **Wonderware adapter forces plaintext HTTP, sending Basic-auth credentials in cleartext** — this.baseUrl = `http://${this.serverHost}:${this.serverPort}/Historian/v1`; is hardcoded to http:// (never https). The constructor then builds `Basic ${btoa(domain\\username:password)}` (lines 95-102) and fetchWithAuth attaches it as the Authorization header (lines 314-328). Base64 is encoding, not encryption: any on-path observer reading the cleartext HTTP request recovers the Windows/SQL domain credentials verbatim. There is no scheme option and no upgrade to TLS. This is a credential-disclosure / SCADA-network downgrade exposure.
  - _FIX:_ Default the scheme to https:// and make it configurable (e.g. add a `secure`/`scheme` field to WonderwareConnectionConfig, defaulting to https). Refuse to send Basic-auth credentials over a plain-http URL (throw, or warn-and-strip) unless the host is explicitly localhost. Mirror the same guard in any other adapter that builds a base URL.
- **[HIGH]** `src/stores/aiConfigStore.ts:558` · L2 Security · local · conf 0.95 · FLAG
  - **Gemini API key persisted in plaintext localStorage with no user warning** — partialize persists the raw key: `partialize: (state) => ({ aiMode: state.aiMode, geminiApiKey: state.geminiApiKey, managementGenerosity: state.managementGenerosity })` under store name 'millos-ai-config' (line 553). safeJSONStorage (storage.ts:43-58) writes to window.localStorage with no encryption/obfuscation. Any XSS, malicious browser extension, or shared-machine access reads the key verbatim. GeminiSettingsModal.tsx has no warning text about local plaintext storage (grep for 'plaintext|localStorage|not secure' returns nothing). This is a user-billing-cost exposure (the key is billable).
  - _FIX:_ Product decision needed: either (a) add a one-line caution under the API key input in GeminiSettingsModal.tsx ('Your key is stored unencrypted in this browser only and never sent to our servers'), or (b) move geminiApiKey to sessionStorage (cleared on tab close) by giving the persist config a separate storage, or (c) drop geminiApiKey from partialize so it lives only in memory for the session. Each changes UX/persistence behavior.
- **[HIGH]** `src/stores/gameSimulationStore.ts:830 (with src/components/MillScene.tsx:824 and src/components/WorkerSystemNew.tsx:383-385)` · L1 Correctness/Bugs · ripple · conf 0.85 · FLAG
  - **Fire-drill evacuation metrics never complete when camera is outside the factory** — startEmergencyDrill(workerCount) snapshots totalWorkers from useProductionStore workers.length (always 12). EmergencyControlPanel.tsx:75 and SafetyPanel.tsx:74 both call `startEmergencyDrill(workerCount)`. But markWorkerEvacuated only fires from a mounted Worker component, and the entire WorkerSystemNew is gated in MillScene.tsx:824 by `showInterior` (= isCameraInside || isCameraInDockZone, MillScene.tsx:739). gameSimulationStore.ts:862: `const evacuationComplete = newEvacuatedIds.length >= state.drillMetrics.totalWorkers;`. If the user orbits/zooms out to watch the evacuation (the natural action), the Worker components unmount (manager.register cleanup deregisters them), so evacuatedWorkerIds never reaches 12: evacuationComplete stays false, finalTimeSeconds stays null, the progress bar sticks and the 'ALL CLEAR' banner never appears. Manual END DRILL still works (endEmergencyDrill stops the alarm), so the alarm is not stuck, but the drill never self-completes.
  - _FIX:_ Product decision among: (a) cap totalWorkers to the count of currently-registered/mounted workers at drill start; (b) drive evacuation headlessly from the store so it completes regardless of camera; or (c) keep WorkerSystemNew (or a headless evacuation tick) mounted while a drill is active. Do not pick blindly; this changes drill UX/semantics.
- **[HIGH]** `src/utils/geminiClient.ts:146` · L7 Copy · local · conf 0.95 · AUTO
  - **Model name Gemini 3 vs 2.0 contradiction** — Header (line 2) and model id gemini-3-flash-preview (135) say Gemini 3, but log 146 says 'Initialized with Gemini 2.0 Flash'; aiEngine.ts:3866 says 'Gemini Flash 3'.
  - _FIX:_ Set 146 and 3866 to 'Gemini 3 Flash'.

## MEDIUM

- **[MEDIUM]** `.env.example (MISSING)` · L12 Config · local · conf 0.85 · AUTO
  - **.env.example missing — no documented env contract** — No `.env.example` exists (verified). The codebase reads `import.meta.env.VITE_ENABLE_SW === 'true'` (src/utils/serviceWorkerRegistration.ts:46) and src/main.tsx:59 instructs to 'Set VITE_ENABLE_SW=true in .env', but there is no example file documenting it. There is also a stale comment in vite.config.ts:73-78 about GEMINI_API_KEY define (correctly disabled) — a new contributor has no single place to learn that NO build-time API key is needed (key is entered at runtime) and that VITE_ENABLE_SW is the only optional dev var.
  - _FIX:_ Add `.env.example` documenting `# VITE_ENABLE_SW=true  # optional: enable service worker in dev` and a comment that no GEMINI/API key is needed at build time (Gemini key is supplied at runtime via the in-app settings modal). `.env*` is already gitignored so the example is safe to commit.
- **[MEDIUM]** `.github/workflows/deploy-k8s.yml + docker.yml` · L12 Config · local · conf 0.7 · FLAG
  - **Backend k8s/docker CI workflows present for a 'frontend-only, NO backend' launch** — The product is described as frontend-only with NO backend/server. deploy-k8s.yml deploys a kubernetes Deployment (`scada-proxy`, needs `secrets.KUBECONFIG`, namespace millos-scada) and docker.yml builds+pushes a `scada-proxy` Docker image to ghcr.io on every push touching scada-proxy/**. scada-proxy/ DOES exist (Dockerfile + package-lock.json confirmed), so these are not dead code — but they build/deploy backend infra that is not part of the frontend GitHub Pages launch. For a launch audit this is misleading scope: a reader assumes the deployed product includes a live SCADA proxy server.
  - _FIX:_ Owner decision: confirm whether scada-proxy is an intended shipped component. If yes, document it in README as separate infra; if it is a non-shipped reference/demo, gate these workflows behind workflow_dispatch only or move scada-proxy to its own repo to avoid implying a backend exists.
- **[MEDIUM]** `.github/workflows/deploy.yml:59` · L12 Config · local · conf 0.85 · FLAG
  - **Site root entry point (millos.net/) is a bare meta-refresh redirect with zero SEO** — The generated staging/index.html (lines 59-71) is `<meta http-equiv="refresh" content="0;url=/v0.30/">` with only a <title>; it has no canonical, no description, no OG/Twitter tags. Crawlers and social scrapers hitting the root https://millos.net/ (the URL declared as canonical and og:url) receive a contentless redirect page, not the rich metadata that lives in /v0.30/index.html. Meta-refresh redirects also pass weaker SEO signal than a 301.
  - _FIX:_ Either copy the full SEO <head> (canonical, description, OG/Twitter) into the root redirect page, or set canonical/og:url to https://millos.net/v0.30/ so the rich page is the indexed URL. Best: serve the real app at root and drop the redirect for the launch version.
- **[MEDIUM]** `CNAME:1` · L12 Config · local · conf 0.9 · FLAG
  - **CNAME is www.millos.net but every SEO/OG/canonical URL uses apex millos.net** — CNAME line 1 is `www.millos.net`. Yet index.html declares canonical https://millos.net/ (line 41), og:url https://millos.net/ (line 24), twitter:url (line 34), og:image/twitter:image https://millos.net/og-image.png (lines 27,37), and img-src 'self' ... https://millos.net (line 10). If the canonical host is www, the apex must 301 to www (or vice versa) and all meta should agree. As written, the declared canonical host differs from the Pages-configured custom domain, which splits SEO signals and can break the img-src allowlist for the OG image when served from the www host.
  - _FIX:_ Decide on one canonical host. If apex millos.net is canonical (as the meta implies), set CNAME to `millos.net` and configure GH Pages apex + www->apex redirect. Otherwise change all meta URLs and img-src to www.millos.net.
- **[MEDIUM]** `GEMINI.md:1-3` · L12 Config · local · conf 0.92 · FLAG
  - **GEMINI.md is a stale copy of CLAUDE.md, not Gemini-specific guidance** — GEMINI.md opens with `# CLAUDE.md` and `This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.` It is a verbatim (and out-of-date) duplicate of CLAUDE.md, not guidance for the Gemini agent/CLI it is named for. Misleading to any tool or contributor that loads GEMINI.md expecting Gemini-tailored instructions.
  - _FIX:_ Either replace GEMINI.md content with Gemini-specific guidance and correct the title, or delete GEMINI.md and rely on AGENTS.md/CLAUDE.md. Owner decision on which agent docs to keep.
- **[MEDIUM]** `README.md:7` · L7 Copy · local · conf 0.99 · AUTO
  - **Stale Three.js badge: claims R169, actual is 0.182 (R182)** — Badge line: `<img src="https://img.shields.io/badge/Three.js-R169-black?..." alt="Three.js" />`. package.json declares `"three": "^0.182.0"` (i.e. r182). The README advertises r169, 13 minor releases behind reality.
  - _FIX:_ Change badge text from `Three.js-R169` to `Three.js-r182` (or `R182`).
- **[MEDIUM]** `README.md:7-9` · L12 Config · local · conf 0.93 · AUTO
  - **README tech-stack badges are stale vs package.json** — README badges claim `Three.js-R169` (line 7), `TypeScript-5.6` (line 8), and `Vite-6.0` (line 9). package.json ships `three: ^0.182.0` (= r182, badge says R169), `typescript: ^5.9.3` (badge says 5.6), and `vite: ^7.3.0` (badge says 6.0). React-19 badge (line 6) is correct. Public-facing version claims on the landing README are wrong by 1-2 major/minor versions.
  - _FIX:_ Update badges: Three.js R182, TypeScript 5.9, Vite 7. Unambiguous doc-only text change, no runtime impact.
- **[MEDIUM]** `README.md:8` · L7 Copy · local · conf 0.99 · AUTO
  - **Stale TypeScript badge: claims 5.6, actual is 5.9.3** — Badge line: `<img src="https://img.shields.io/badge/TypeScript-5.6-3178C6?..." alt="TypeScript" />`. package.json declares `"typescript": "^5.9.3"`.
  - _FIX:_ Change badge text from `TypeScript-5.6` to `TypeScript-5.9`.
- **[MEDIUM]** `README.md:9` · L7 Copy · local · conf 0.99 · AUTO
  - **Stale Vite badge: claims 6.0, actual is 7.3.0** — Badge line: `<img src="https://img.shields.io/badge/Vite-6.0-646CFF?..." alt="Vite" />`. package.json declares `"vite": "^7.3.0"`. A full major version behind.
  - _FIX:_ Change badge text from `Vite-6.0` to `Vite-7.3` (or `Vite-7`).
- **[MEDIUM]** `README.md:97` · L7 Copy · local · conf 0.85 · FLAG
  - **Packer throughput copy '42 bags/minute' contradicts code (~60 bags/min)** — Features table line 97: `| **4** | 3 Packer Lines | High-speed packaging at 42 bags/minute |`. The codebase consistently models ~60 bags/min: materialFlowStore.ts:210 `processingRate: 25, // 25 kg/sec = ~60 bags/min at 25kg/bag` and SimulationAdapter.ts:78 `if (machineId.includes('packer')) return 60; // Packers ~60 bags/min`. No '42 bags' figure exists in packer logic (the only 42 is a mill temperature seed in MillScene.tsx:449).
  - _FIX:_ Change '42 bags/minute' to '60 bags/minute' (or whatever the canonical rate should be — confirm with owner since it's a stated spec number).
- **[MEDIUM]** `eslint.config.js:56-57` · L11 DeadCode · local · conf 0.95 · FLAG
  - **Lint disables no-unused-vars entirely — per-file dead imports/locals/params go undetected in the GREEN baseline** — Lines: `'no-unused-vars': 'off',` and `'@typescript-eslint/no-unused-vars': 'off',`. With both rules off, unused imports, unreferenced local variables, and unused function params accumulate silently and never fail CI/lint. This is the root reason structural dead code (whole modules, barrels, orphan panels) survives. It also means the 'unused imports/locals/params' checklist item cannot be relied on as caught-by-baseline anywhere in the repo.
  - _FIX:_ Owner decision: re-enable @typescript-eslint/no-unused-vars at 'warn' with `argsIgnorePattern: '^_'` / `varsIgnorePattern: '^_'` to surface dead imports/locals without breaking the build. Do NOT flip to 'error' in one shot — it would likely break the GREEN baseline given accumulated unused vars; introduce as 'warn' first, then burn down.
- **[MEDIUM]** `index.html:11` · L2 Security · local · conf 0.9 · FLAG
  - **connect-src allows wildcard ws:/wss: to any host** — connect-src is 'self' ws: wss: https://generativelanguage.googleapis.com https://cdn.jsdelivr.net https://fonts.gstatic.com https://fonts.googleapis.com. Bare 'ws:' and 'wss:' permit WebSocket connections to ANY origin. With PeerJS (src/multiplayer/SignalingService.ts:26-30) using the default public cloud server and untrusted WebRTC peers, this lets injected/compromised script (or a malicious peer-delivered payload) exfiltrate to arbitrary WS endpoints. The only WS endpoint actually needed is the PeerJS cloud signaler (0.peerjs.com over wss) plus any self-hosted SCADA WS.
  - _FIX:_ Replace bare 'ws: wss:' with the specific signaling host, e.g. 'wss://0.peerjs.com' (and the user's SCADA host if applicable). If a self-hosted PeerServer is later used, add that exact host. Keep 'ws:' only if local-dev SCADA over plain ws is required, scoped to localhost.
- **[MEDIUM]** `index.html:11` · L2 Security · local · conf 0.75 · FLAG
  - **cdn.jsdelivr.net in connect-src enables remote font-data fetch from third party (supply-chain surface)** — connect-src includes 'https://cdn.jsdelivr.net'. troika-three-text (bundled via @react-three/drei/three-fiber) fetches Unicode font-resolver data at runtime from 'https://cdn.jsdelivr.net/gh/lojjic/unicode-font-resolver@v1.0.1/packages/data' (seen verbatim in built bundle public/v0.20/assets/three-fiber-*.js and the dev module). This is connect-src data (not script-src remote code), so it cannot execute arbitrary JS, but it makes glyph rendering depend on a third-party CDN and adds an exfiltration-eligible host to the policy. If jsdelivr is compromised it can serve crafted JSON, which is parsed but not executed.
  - _FIX:_ Acceptable to keep if 3D text glyph coverage for non-Latin scripts is needed. To eliminate the dependency, self-host the unicode-font-resolver data and pass a local dataUrl to troika's font resolver (or restrict to the Latin subset shipped locally), then remove cdn.jsdelivr.net from connect-src. Product decision on whether non-Latin glyph coverage is required.
- **[MEDIUM]** `index.html:44-46` · L3 · local · conf 0.88 · FLAG
  - **Google Fonts leaks visitor IP to Google** — Lines 44-46 load fonts.googleapis.com/gstatic.com; visitor IP sent to Google pre-consent (GDPR LG Muenchen 2022). MedievalSharp.ttf self-hosted line 49.
  - _FIX:_ Self-host woff2, remove googleapis links and CSP font hosts.
- **[MEDIUM]** `index.html:5-13` · L2 Security · local · conf 0.85 · AUTO
  - **CSP missing object-src, base-uri, and frame-ancestors hardening directives** — The CSP meta tag declares default-src/script-src/style-src/font-src/img-src/connect-src/worker-src but omits 'object-src', 'base-uri', and 'frame-ancestors'. Without 'object-src none', plugin/embed vectors are not explicitly denied (default-src 'self' covers it but explicit is best-practice for XSS hardening). Without 'base-uri self', an injected <base> tag can hijack all relative URLs (the app relies heavily on relative paths — import.meta.env.BASE_URL, sw.js relative precache). frame-ancestors cannot be set via meta tag (it is ignored in meta and only works as an HTTP header), so clickjacking protection is absent on GitHub Pages which sends no X-Frame-Options/frame-ancestors header.
  - _FIX:_ Add 'object-src 'none';' and 'base-uri 'self';' to the CSP meta in index.html. For frame-ancestors/clickjacking, since GitHub Pages cannot set custom response headers, either accept the residual clickjacking risk (low for a single-page simulator with no auth/sensitive actions) or front the site with a CDN/proxy (e.g. Cloudflare) that injects 'Content-Security-Policy: frame-ancestors 'self'' and 'X-Frame-Options: SAMEORIGIN'. Document the limitation.
- **[MEDIUM]** `public/` · L12 Config · local · conf 0.9 · FLAG
  - **No robots.txt and no sitemap.xml for a public launch** — Neither public/robots.txt nor public/sitemap.xml exists (find + rg --files confirmed absent; the only file matching the SEO grep was index.html). For a public launch on millos.net this leaves crawlers without an explicit allow signal or a sitemap pointing at the canonical URL, and no place to advertise the sitemap. Low effort, real SEO/discoverability cost.
  - _FIX:_ Add public/robots.txt (`User-agent: *` / `Allow: /` / `Sitemap: https://millos.net/sitemap.xml`) and a minimal public/sitemap.xml listing https://millos.net/ with lastmod. Vite copies public/* to dist root automatically.
- **[MEDIUM]** `public/models/forklift/forklift.glb.backup.glb` · L11 DeadCode · local · conf 0.95 · AUTO
  - **71MB of unreferenced glb.backup.glb files are tracked AND shipped to production** — Four git-tracked backup binaries: public/models/forklift/forklift.glb.backup.glb (36.8MB), public/models/worker/worker.glb.backup.glb (491KB), public/v0.20/models/forklift/forklift.glb.backup.glb (36.8MB), public/v0.20/models/worker/worker.glb.backup.glb (491KB), 71MB total. The real assets exist and load; a grep for backup.glb in src returns zero hits. deploy.yml ships the v0.20 backups into Pages and Vite copies public models into the v0.30 dist. Users download dead 36.8MB binaries.
  - _FIX:_ git rm the four public glb.backup.glb files. Real GLBs untouched so 3D rendering unaffected.
- **[MEDIUM]** `public/og-image.png` · L9 Performance · local · conf 0.92 · FLAG
  - **Social og-image is 3456x1993 / 2.1MB but declared 1200x630 in meta tags** — `file public/og-image.png` = PNG 3456 x 1993, du = 2140KB (~2.1M). index.html:28-29 declares og:image:width 1200 / og:image:height 630. Asset is ~8x the displayed pixel area and 2.1MB — the single largest PNG shipped, fetched by social crawlers/previews, and dimensions mismatch the declared meta.
  - _FIX:_ Downscale og-image.png to 1200x630 (matching declared meta) and re-export; should drop to <200KB. Pure asset swap, no code change.
- **[MEDIUM]** `public/sw.js:204-206` · L2 Security · local · conf 0.8 · FLAG
  - **network-first caches ALL 200 responses including HTML/dynamic, risking stale-serve of stale SPA shell** — networkFirst() caches every cacheable (status===200) response into STATIC_CACHE (lines 204-206), including HTML and any non-asset GET. On a later network failure it serves the cached HTML (lines 213-221) which may reference hashed chunks already purged by a CACHE_VERSION bump, and there is no max-age / staleness check. isCacheable() (line 79-81) only checks status===200 with no Cache-Control or content-type gating, so error pages or redirected 200s also get cached. For a digital-twin sim this is mainly an availability/stale-shell issue rather than data integrity, but it can pin users to a broken old shell after a deploy.
  - _FIX:_ In networkFirst, only cache navigations/HTML into a dedicated short-lived cache and skip caching responses whose Cache-Control is no-store, or restrict network-first caching to known same-origin document/manifest requests. Add a Date-stamp check or rely on CACHE_VERSION + the SW update flow above. Behavior change → owner review.
- **[MEDIUM]** `public/sw.js:21` · L12 Config · local · conf 0.75 · FLAG
  - **PWA scaffolding present (service worker + caches) but no web app manifest -> not installable** — public/sw.js (CACHE_VERSION 'v4') is a full offline caching SW registered at app start (src/main.tsx imports registerServiceWorker; serviceWorkerRegistration.ts:56 registers it). However there is no web app manifest anywhere (no manifest.json/.webmanifest in public/, and index.html has no <link rel="manifest">). Without a manifest the app cannot meet PWA install criteria (no name/icons/start_url/display), so the SW investment yields no Add-to-Home-Screen/installability.
  - _FIX:_ Add public/manifest.webmanifest (name, short_name, start_url '.', display 'standalone', background_color #0a0f1a, theme_color, 192/512 icons) and link it: <link rel="manifest" href="manifest.webmanifest">. Use relative href to survive versioned base paths.
- **[MEDIUM]** `public/sw.js:220` · L12 Config · local · conf 0.7 · AUTO
  - **SW SPA navigation fallback hardcodes absolute /index.html, breaking under versioned base paths** — networkFirst's navigation fallback is `const indexResponse = await caches.match('/index.html');` (line 220). But everything else in the SW deliberately uses relative paths to support versioned deployments: PRECACHE_ASSETS uses './index.html' (lines 28-33, with the comment "work at any base URL (/, /v0.20/, etc.)"), and registration uses scope = BASE_URL (serviceWorkerRegistration.ts:54-57). On the live site the app is served from /v0.30/, so the precached entry is '/v0.30/index.html' (or './'), not '/index.html'. When offline, caches.match('/index.html') misses and the SPA offline fallback returns the 'Offline' 503 instead of the cached app shell.
  - _FIX:_ Match the precached relative entry instead, e.g. `await caches.match('./index.html')` or `await caches.match('./')` (the precached root), or compute from self.registration.scope. Mirror the relative-path convention used in PRECACHE_ASSETS.
- **[MEDIUM]** `public/sw.js:99-100,124-126` · L2 Security · local · conf 0.8 · FLAG
  - **Service worker uses skipWaiting()+clients.claim() unconditionally → mixed-version asset serving on deploy** — install handler calls self.skipWaiting() (line 100) and activate calls self.clients.claim() (line 125) unconditionally on every new SW. Combined with cache-first for hashed JS/CSS (shouldCacheFirst, lines 67-72) and main.tsx's onUpdate handler being a no-op (src/main.tsx:64-66, empty body — no reload prompt), a user mid-session can have the new SW take control and begin serving NEW index.html (network-first) while the page still references OLD hashed chunks that are gone from the server but may or may not be in cache — producing 'failed to fetch dynamically imported module' / blank-screen on deploy. The CACHE_VERSION bump (line 21) only purges caches on activate; it does not coordinate the in-flight page's chunk references.
  - _FIX:_ Gate skipWaiting behind an explicit SKIP_WAITING postMessage triggered only after the user accepts an update prompt, and implement the onUpdate handler in main.tsx to surface a 'new version available — reload' toast that posts SKIP_WAITING then reloads. This is a UX/behavior change so it needs owner sign-off; minimum safe step is to keep skipWaiting but make onUpdate force a one-time location.reload() after activation to avoid mixed-chunk states.
- **[MEDIUM]** `src/App.tsx:533-552` · L6 UX · local · conf 0.75 · FLAG
  - **No WebGL-unsupported detection; misleading error** — Absent-WebGL browser gets 'WebGL Context Lost' + Reload dead-end with wrong copy.
  - _FIX:_ getContext('webgl') check before Canvas; unsupported-browser screen.
- **[MEDIUM]** `src/App.tsx:581` · L6 UX · local · conf 0.85 · FLAG
  - **Screen-reader keyboard notice advertises 'B for management' but B has no handler** — Line 581 (sr-only role=note): 'Use keyboard shortcuts: I for AI panel, O for SCADA, B for management, Escape to close panels.' There is no 'B' (plain) shortcut in useKeyboardShortcuts.ts — the B asset-browser handler is commented out (lines 351-359) and only Ctrl+B exists (Blueprint Mode, line 341). A screen-reader user pressing B to open 'management' gets nothing. This is the WCAG-facing help text, so accuracy matters most here.
  - _FIX:_ Remove 'B for management' from the notice (management is reached via the Dock, not a key), or wire a real 'B' handler. Keep the I/O/V/1-5 entries which are accurate.
- **[MEDIUM]** `src/components/AICommandCenter.tsx:307` · L5 A11y · local · conf 0.7 · FLAG
  - **AI decision feed has no live region; new AI decisions are not announced to screen readers** — The decisions list at lines 307-350 renders `aria-hidden`-free but plain divs with no aria-live region. New AI decisions stream in (the panel 'now strictly visualizes the state', line 132) but a screen-reader user gets no announcement when a decision is added. The checklist calls for live regions for AI decisions. Note: putting aria-live on the whole scrolling list (307) would be noisy — the right fix is a small polite status summary that announces the latest decision action.
  - _FIX:_ Add an sr-only `role="status" aria-live="polite"` element that updates with a concise summary of the newest decision (e.g. `New AI decision: ${decision.action}`), rather than making the entire list a live region. UX/behavior change — recommend product sign-off on verbosity.
- **[MEDIUM]** `src/components/GeminiSettingsModal.tsx:198` · L5 A11y · local · conf 0.97 · AUTO
  - **GeminiSettingsModal close (X) button has no accessible name** — Icon-only close button: `<button onClick={onClose} className="p-1 rounded-lg hover:bg-slate-800 transition-colors"><X className="w-5 h-5 text-slate-400" /></button>` (lines 198-203). No `aria-label` and no visible text, so screen readers announce only 'button'. AboutModal.tsx:54-60 sets `aria-label="Close about dialog"` and `aria-hidden="true"` on the X icon for the same control.
  - _FIX:_ Add `aria-label="Close settings"` to the button (line 198) and `aria-hidden="true"` to the `<X />` icon (line 202). Pure SR fix, no visual/UX change.
- **[MEDIUM]** `src/components/GeminiSettingsModal.tsx:251` · L5 A11y · local · conf 0.9 · AUTO
  - **API key input label not programmatically associated with the input** — `<label className="block text-sm font-medium text-slate-300">Google API Key</label>` (line 251) is a bare label with no `htmlFor`, and the password `<input type="password" value={inputKey} ...>` (line 254) has no `id` and no `aria-label`. The label is not associated with the field, so screen readers do not announce 'Google API Key' when the input is focused. (The 'AI Visualization Overlays' label at line 73 and 'AI Operating Mode' at line 306 are also non-associated, but those wrap button groups; line 251 is the real form-control case.)
  - _FIX:_ Give the input `id="gemini-api-key"` and the label `htmlFor="gemini-api-key"` (or add `aria-label="Google API Key"` to the input). Pure SR fix, no visual/UX change.
- **[MEDIUM]** `src/components/GeminiSettingsModal.tsx:254` · L6 UX · local · conf 0.8 · FLAG
  - **No disclosure that the Gemini API key is stored in plaintext localStorage** — The API-key input (lines 254-264) and surrounding copy never warn that the entered key is persisted unencrypted in localStorage under 'millos-ai-config' (partialize at aiConfigStore.ts:555-560 persists geminiApiKey verbatim). On a shared/public machine this is a real privacy exposure; users entering a billable Google key deserve to know it is stored client-side in plaintext and persists across sessions. There is also no 'don't remember / session-only' option.
  - _FIX:_ Add a short note under the input, e.g. 'Your key is stored in this browser only (localStorage), in plain text. Use "Clear Config" to remove it.' Optionally offer a session-only (non-persisted) toggle.
- **[MEDIUM]** `src/components/GeminiSettingsModal.tsx:280` · L5 A11y · local · conf 0.88 · AUTO
  - **Connection test result / error not announced to screen readers (no aria-live)** — The test-result banner `{testResult && (<motion.div className={`p-3 rounded-lg flex items-center gap-2 ...`}>...{testResult.message}</motion.div>)}` (line 280-301) and the connection-error block `{connectionError && (<div className="mt-2 flex items-center gap-2 text-sm text-red-400"><AlertTriangle .../><span>{connectionError}</span></div>)}` (line 241-246) appear/update visually but carry no `role="alert"` or `aria-live`. A keyboard/SR user clicking 'Test Connection' or 'Save & Connect' receives no announcement of success or failure. This is the form error-association checklist item made concrete.
  - _FIX:_ Add `role="alert"` (or `aria-live="assertive"`) to the testResult motion.div (line 281) and `role="alert"` to the connectionError div (line 242). Pure SR fix, no visual/UX change.
- **[MEDIUM]** `src/components/GeminiSettingsModal.tsx:67` · L6 UX · local · conf 0.92 · FLAG
  - **Modal lists wrong keyboard shortcuts (H/V/C) that do something else entirely** — VisualizationToggles advertises `kbd` shortcuts that conflict with the real handlers in useKeyboardShortcuts.ts: 'Shift Handover' shows key 'H' (line 67) but H toggles the heatmap (useKeyboardShortcuts.ts:218) and shift-handover has NO key handler; 'VCL Context' shows key 'V' (line 62) but V toggles first-person mode (line 388); 'API Cost Tracker' shows key 'C' (line 59) but C toggles auto-rotation (line 331) — the cost overlay is actually on '$' (line 284). The modal's own caption (line 95) says 'Toggle overlays with keyboard shortcuts or buttons above', so users pressing H/V/C get surprising behavior.
  - _FIX:_ Remove the incorrect `key` chips for Shift Handover, VCL Context, and API Cost Tracker (or wire real handlers). For API Cost Tracker change the chip to '$' to match useKeyboardShortcuts.ts:284. Verify each remaining chip against the actual handler keys.
- **[MEDIUM]** `src/components/UIOverlay.tsx.original:1` · L11 DeadCode · local · conf 0.97 · AUTO
  - **UIOverlay.tsx.original (101KB) is a git-tracked backup file that can never be compiled/imported** — `git ls-files` shows src/components/UIOverlay.tsx.original is tracked (101349 bytes). The `.tsx.original` extension is not a module TypeScript/Vite resolves, so nothing imports it; it is a pure pre-migration snapshot of UIOverlay (contains its own duplicate `KeyboardShortcutsModal` at line 1321 and `UIOverlay` at 1621). Pure dead weight in the repo.
  - _FIX:_ Delete src/components/UIOverlay.tsx.original (`git rm`). Zero runtime/test impact — it is not part of any import graph.
- **[MEDIUM]** `src/components/UIOverlay.tsx:1296` · L11 DeadCode · ripple · conf 0.85 · FLAG
  - **UIOverlay (the entire old top-level UI, ~2000 lines / 89KB) is superseded by ui-new/GameInterface and no longer rendered** — App.tsx renders only `<GameInterface ...>` (App.tsx:586); UIOverlay is not referenced in App.tsx at all. `export const UIOverlay` (UIOverlay.tsx:1296) is reachable only via the dead components/index.ts barrel (line 38) and its test (__tests__/UIOverlay.test.tsx:14,180). It is the old-generation UI replaced by the src/components/ui-new/ migration. It transitively keeps several ui/ panels alive that are otherwise dead (EmergencyStopButton, SafetyConfigPanel, ZoneCustomizationPanel, TruckScheduleWidget, SafetyMetricsDisplay are imported ONLY by UIOverlay.tsx).
  - _FIX:_ Owner decision (migration completeness): if the ui-new migration is final, retire UIOverlay.tsx, delete its dedicated test, and clean up the 5 panels it solely consumes. Removal cascades into the test suite (breaks GREEN baseline) so it must be a coordinated change, not an isolated delete.
- **[MEDIUM]** `src/components/UIOverlay.tsx:1500-1511` · L8 Visual · local · conf 0.92 · AUTO
  - **Version-switcher <select> value 'v0.3' matches no <option>; renders blank + React warning** — The controlled select declares value="v0.3" but its options are value="v0.30", "v0.20", "v0.10": `<select ... value="v0.3"> ... <option value="v0.30">0.30</option>`. No option equals 'v0.3', so the browser resolves selectedIndex to -1 and shows a blank/indeterminate dropdown in the header, and React emits a dev console warning ('value provided ... no matching option'). The current version is visually missing from the version switcher.
  - _FIX:_ Change value to a real option that represents the running build, e.g. `value="v0.30"` (matching the first option), so the dropdown reflects the active version.
- **[MEDIUM]** `src/components/UIOverlay.tsx:2058` · L5 A11y · local · conf 0.85 · AUTO
  - **MachineInfoPanel status indicator conveys machine status by color alone (no text alternative)** — The status dot encodes running/warning/critical purely via background color with no accompanying status text or aria-label:
```
<div
  className={`w-2 h-2 sm:w-2.5 sm:h-2.5 rounded-full ${machine.status === 'running' ? 'bg-green-500 animate-pulse' : machine.status === 'warning' ? 'bg-yellow-500' : 'bg-red-500'}`}
/>
```
A screen-reader (or colorblind) user has no way to perceive the machine status here. Contrast with WorkerDetailPanel.tsx:475-479, which pairs an aria-hidden color dot with visible status text. WCAG 1.4.1 Use of Color.
  - _FIX:_ Add an accessible status label, e.g. `aria-label={`Status: ${machine.status}`}` on the dot div, or render the status text alongside the machine name (the panel currently shows name/type/id but not status as text).
- **[MEDIUM]** `src/components/WorkerDetailPanel.tsx:523` · L5 A11y · local · conf 0.9 · AUTO
  - **Worker detail tabpanels reference tab IDs that do not exist (dangling aria-labelledby)** — Each tabpanel back-references its tab via `aria-labelledby` (overview-panel: `aria-labelledby="overview-tab"` at line 523; skills 600; reviews 646; alignment 671), but the tab buttons rendered in the map at lines 496-511 carry `role="tab"`, `aria-selected`, and `aria-controls={`${tab.id}-panel`}` and have NO `id` attribute. So `aria-labelledby="overview-tab"` points at a nonexistent element — the panel→tab back-reference dangles for all 4 tabs. (The tab→panel direction via aria-controls is correct because the panels do have matching ids.)
  - _FIX:_ Add `id={`${tab.id}-tab`}` to the tab button in the map at line 496-511 so the existing `aria-labelledby` references resolve.
- **[MEDIUM]** `src/components/mobile/RotateDeviceOverlay.tsx:25` · L5 A11y · local · conf 0.8 · FLAG
  - **No prefers-reduced-motion guard anywhere in scope; indefinite looping animations run unconditionally** — A `prefers-reduced-motion` / `useReducedMotion` / `MotionConfig` grep returns ZERO hits across all in-scope files (ui-new, mobile, modals) despite framer-motion driving every modal/panel transition plus CSS infinite loops. Concrete indefinite-loop instances: RotateDeviceOverlay.tsx:25 `<div className="w-24 h-36 border-4 border-cyan-400 rounded-2xl relative animate-pulse">` and :32 `<div className="absolute -right-8 top-1/2 -translate-y-1/2 text-cyan-400 animate-bounce">`. Users who set 'reduce motion' at the OS level get no relief. Vestibular accessibility (WCAG 2.3.3 AAA / 2.2.2 for the looping pulse/bounce).
  - _FIX:_ Gate looping animations on reduced-motion: e.g. wrap framer-motion app region in `<MotionConfig reducedMotion="user">`, and for the CSS loops add a `motion-reduce:animate-none` Tailwind variant to the animate-pulse (line 25) / animate-bounce (line 32) / any animate-spin elements. Changes motion behavior under user preference, so owner should confirm scope.
- **[MEDIUM]** `src/components/physics/PhysicsForklift.tsx:131` · L1 Correctness/Bugs · local · conf 0.8 · AUTO
  - **Physics forklift registers isStopped=false while halted by standalone emergency stop** — Physics is default-ON (graphicsStore.ts:122 `enablePhysics: true`), so PhysicsForklift is the live path. Line 131: `const isStopped = emergencyDrillMode || operationRef.current !== 'traveling';` — it omits forkliftEmergencyStop. When the standalone E-stop is engaged (EmergencyStopButton.tsx toggles setForkliftEmergencyStop), the forklift is physically halted at line 152-154 (`if (emergencyDrillMode || forkliftEmergencyStop) { rb.setLinvel({x:0,y:0,z:0}); ... }`), but it then registers isStopped=false into positionRegistry. Workers querying the registry (isForkliftApproaching / nearest-forklift stopped state) treat the halted forklift as moving and may needlessly evade a stationary forklift. The code comment at line 129-130 even states isStopped 'should include emergency drill mode AND loading/unloading' but forgets the standalone E-stop.
  - _FIX:_ Read forkliftEmergencyStop in updatePosition and include it: `const isStopped = emergencyDrillMode || forkliftEmergencyStop || operationRef.current !== 'traveling';` (forkliftEmergencyStop is already subscribed at line 97). Add it to the updatePosition useCallback deps as well.
- **[MEDIUM]** `src/components/terrain/TerrainGround.tsx:127` · L9 Performance · local · conf 0.75 · FLAG
  - **Terrain geometry (+ splatMap/heightmap DataTextures) leak GPU memory on every graphics-quality change** — geometry is built in useMemo with deps [width, height, segments, heightmapData, riverConfig.depth]: `const geometry = useMemo(() => { if (heightmapData) { return createDisplacedGeometry(...) } return new THREE.PlaneGeometry(width, height, 1, 1); }, [width, height, segments, heightmapData, riverConfig.depth]);`. In MillScene.tsx terrainResolution/terrainSegments/terrainEnableRiverChannel all derive from graphicsQuality (lines ~716-728), and GraphicsSettingsPanel.tsx calls setGraphicsQuality at runtime. So toggling quality recreates the geometry (and the splatMap at :108 and heightmap at :114, both THREE.DataTexture GPU resources). The geometry is attached via `<primitive object={geometry} attach="geometry" />` (:152). R3F does NOT auto-dispose objects passed via <primitive> — it doesn't own them — and there is zero dispose() call anywhere in src/components/terrain/. Each quality change orphans the prior GPU vertex buffer + 2 DataTextures. Not tick-driven so it is not a continuous leak, but it is an unbounded leak across repeated user quality toggles.
  - _FIX:_ Add a useEffect cleanup that disposes the previous geometry/textures, e.g. capture geometry/splatMap/heightmap in refs and on change/unmount call geometry.dispose(), splatMap.dispose(), heightmapTexture.dispose(). Simplest: `useEffect(() => () => geometry.dispose(), [geometry]);` plus equivalents for the splatMap and heightmap DataTextures. Verify visually after a quality toggle (3D, not test-covered).
- **[MEDIUM]** `src/components/terrain/TerrainMaterial.tsx:264` · L9 Performance · local · conf 0.65 · FLAG
  - **TerrainMaterial MeshStandardMaterial created in useMemo is never disposed** — `const material = useMemo(() => { const mat = new THREE.MeshStandardMaterial({...}); ... return mat; }, [...])` (component form at :264, hook form useTerrainMaterial at :344 with deps []). The material is returned and rendered into the terrain mesh. There is no dispose() in the file. The component-form useMemo at :264 carries shader-injection deps, and the parent TerrainGround remounts/recomputes on graphics-quality change, so the prior MeshStandardMaterial (with its compiled custom shader program from onBeforeCompile at :285/:352) is orphaned on quality toggle. Compounds the TerrainGround geometry/texture leak.
  - _FIX:_ Add `useEffect(() => () => material.dispose(), [material]);` in both useTerrainMaterial and the component path so the GPU program/material is released when it is replaced or the terrain unmounts. Visual regression risk is low (dispose only fires on replacement/unmount) but it is a 3D path — confirm terrain still renders after a quality toggle.
- **[MEDIUM]** `src/components/ui-new/hud/StatusHUD.tsx:205-211` · L8 Visual · local · conf 0.82 · AUTO
  - **Safety alerts render the generic blue 'info' dot, indistinguishable from info notifications** — The notification-list status dot color chain handles only critical/warning/success then falls through to blue: `alert.type === 'critical' ? 'bg-red-500' : alert.type === 'warning' ? 'bg-amber-500' : alert.type === 'success' ? 'bg-green-500' : 'bg-blue-500'`. But AlertData.type includes 'safety' (src/types.ts:215) and AlertSystem.tsx:52-59 actively emits `type: 'safety'` near-miss alerts (the highest-attention category, paired with audioManager.playAlert()). These show a blue dot identical to ordinary 'info', so the most important alerts get no distinct visual treatment in the dropdown.
  - _FIX:_ Add an explicit branch for safety, e.g. `: alert.type === 'safety' ? 'bg-orange-500'` (or a dedicated shield-tinted color) before the blue default, so safety/near-miss notifications are visually distinct.
- **[MEDIUM]** `src/components/ui-new/panels/SettingsPanel.tsx:26` · L6 UX · local · conf 0.78 · FLAG
  - **Active Settings panel has no entry point to AI / Gemini configuration** — The new-UI SettingsPanel (the one mounted by the Dock) covers Simulation/Audio/Knowledge/Graphics/Reset but contains zero link to Gemini AI settings (grep for 'Gemini'/'API Key' in this file returns nothing). The only way to reach GeminiSettingsModal is a small button buried inside the AI Command Center sidebar panel (AICommandCenter.tsx:200-212). A user looking for 'where do I add my API key' in Settings finds nothing, and there is no first-run prompt — so the headline AI feature is effectively undiscoverable for new users.
  - _FIX:_ Add an 'AI Assistant' section to SettingsPanel with a button that opens GeminiSettingsModal (or deep-links to the AI Command Center), mirroring the existing button in AICommandCenter.tsx:200.
- **[MEDIUM]** `src/components/ui-new/panels/SettingsPanel.tsx:461` · L6 UX · local · conf 0.9 · FLAG
  - **'Reset Simulation' removes a nonexistent key ('millos-settings') and leaves graphics + plaintext Gemini API key persisted** — Line 461 removes 'millos-settings', but no zustand store uses that name (confirmed: actual keys are millos-graphics, millos-ai-config, etc.). The reset never removes 'millos-graphics' (real graphics key) nor 'millos-ai-config' (which holds the plaintext geminiApiKey via partialize at aiConfigStore.ts:558). So a 'reset' leaves the user's saved API key and graphics tweaks behind — a correctness and privacy gap (user reasonably expects reset to clear stored credentials).
  - _FIX:_ Replace 'millos-settings' with 'millos-graphics' and add 'millos-ai-config' (plus other millos-* persist keys: millos-bas, millos-safety, millos-ui, etc.) to the removeItem list, or iterate `Object.keys(localStorage).filter(k => k.startsWith('millos-')).forEach(k => localStorage.removeItem(k))`.
- **[MEDIUM]** `src/components/ui-new/sidebar/ContextSidebar.tsx:289` · L8 Visual · local · conf 0.9 · FLAG
  - **Version <select> controlled value 'v0.3' matches no <option> (React warning + blank/mismatched display)** — Line 289 sets value="v0.3" on a controlled select, but the options at lines 295-297 are value="v0.30", "v0.20", "v0.10". A controlled <select> whose value matches no option triggers a React console warning and renders with no option selected (browser falls back to first option's label), so the footer version indicator is visually wrong/inconsistent with the actual deploy. The onChange also navigates window.location.href = `/${e.target.value}/` (e.g. '/v0.20/'), so the option values must match real routes; the current state means the displayed 'current version' is indeterminate.
  - _FIX:_ Set value to an existing option (likely value="v0.30") so the control reflects the live build; verify the option values map to real millos.net subpaths before relying on the navigation onChange.
- **[MEDIUM]** `src/components/ui-new/widgets/FederationPanel.tsx:206` · L8 Visual · local · conf 0.85 · FLAG
  - **Sub-9px text (7px/8px) across 27 files is below legible/accessible minimums** — 27 files use `text-[7px]` or `text-[8px]` for body labels. Example FederationPanel.tsx:206-221 renders four stat labels at `text-[7px] text-slate-500` ('Members', 'Learnings', 'Adopted', 'Exchanges'). 7-8px text combined with low-contrast slate-500 on a dark panel is effectively illegible and fails WCAG legibility expectations. Counts: text-[8px] x159, text-[7px] x26. This is the practical fallout of the text-xs override forcing devs ever-smaller.
  - _FIX:_ Bump the smallest labels to at least 10px and raise contrast (slate-400 or lighter on dark). Best handled as part of the type-scale remediation above. Per-file change is low-blast but touches visual layout in dense widgets, so verify each panel still fits after enlarging.
- **[MEDIUM]** `src/components/ui/AlertAcknowledgmentFlow.tsx:94` · L5 A11y · local · conf 0.85 · AUTO
  - **Icon-only alert-dismiss button has no aria-label (title alone insufficient)** — The dismiss button contains only an X icon: `<button onClick={() => handleAcknowledge(alert.id)} className="..." title="Acknowledge"><X className="w-3.5 h-3.5 ..." /></button>` (lines 94-100). `title` is not a reliable accessible name for icon-only controls across assistive tech; the button has no aria-label and no text node. Violates WCAG 4.1.2 / 1.1.1.
  - _FIX:_ Add aria-label="Acknowledge alert" (or include the alert title, e.g. `aria-label={`Acknowledge: ${alert.title}`}`). Keep the existing X icon.
- **[MEDIUM]** `src/components/ui/ConfidenceBar.tsx:57` · L5 A11y · local · conf 0.85 · AUTO
  - **Confidence progress bar has no progressbar role/value and uses color alone** — The bar `<div className="w-full ${heights[size]} bg-slate-700/50 rounded-full overflow-hidden"><motion.div ... className="h-full ${colors.bg} rounded-full" /></div>` (lines 57-64) has no role="progressbar", aria-valuenow/min/max, or aria-label, so AT cannot read the confidence value when showPercentage is false. Confidence level (high/med/low) is signaled only by bar color green/amber/red at the 80/60 thresholds (getColor, lines 29-33) with no non-color indicator — WCAG 1.4.1 Use of Color. This component is reused (e.g. StrategicPriorityCards); fix stays inside ConfidenceBar so blast radius is local.
  - _FIX:_ Add role="progressbar" aria-valuemin={0} aria-valuemax={100} aria-valuenow={clampedConfidence} aria-label={label || 'AI confidence'} to the track div. Ensure the percentage/level is conveyed textually (not color-only).
- **[MEDIUM]** `src/components/ui/DecisionHistoryPanel.tsx:126` · L5 A11y · local · conf 0.9 · AUTO
  - **Pagination prev/next buttons are icon-only with no accessible name** — `<button onClick={goToPrevPage} disabled=... ><ChevronLeft className="w-4 h-4 text-slate-400" /></button>` (lines 126-132) and the next button (lines 136-142, ChevronRight) have no aria-label or text. Page indicator (line 133) is separate text. Violates WCAG 4.1.2.
  - _FIX:_ Add aria-label="Previous page" and aria-label="Next page" to the two buttons; aria-hidden on the chevron icons.
- **[MEDIUM]** `src/components/ui/DecisionReplay.tsx:201` · L5 A11y · local · conf 0.9 · AUTO
  - **Clickable div trigger is not keyboard operable** — DecisionReplayTrigger wraps content in a clickable div: `<div onClick={() => setShowReplay(true)} className="cursor-pointer">{children}</div>` (line 201). No role="button", tabIndex, or onKeyDown — keyboard users cannot open the replay. Violates WCAG 2.1.1 (Keyboard) and 4.1.2.
  - _FIX:_ Make it a <button> (or add role="button" tabIndex={0} onKeyDown for Enter/Space) with an aria-label like 'View decision details'. Preferred: replace the div with a <button type="button"> wrapper.
- **[MEDIUM]** `src/components/ui/EmergencyControlPanel.tsx:39` · L5 A11y · local · conf 0.85 · AUTO
  - **Collapsible section toggle missing aria-expanded (systemic across 10 panels)** — The expand/collapse toggle `<button onClick={() => setExpanded(!expanded)} ...>` (line 39-52) conveys state only via a swapped Chevron icon (line 51) with no text/label; it has no aria-expanded. grep confirms aria-expanded appears in 0 of 29 ui/*.tsx while the same toggle pattern exists in EmergencyControlPanel, IncidentHistoryPanel, SafetyAnalyticsPanel, CollapsibleLegend, DecisionHistoryPanel, VCLDebugPanel, ZoneCustomizationPanel, GraphicsSettingsPanel, SafetyConfigPanel, WeatherControlPanel. Screen-reader users aren't told the section is collapsed/expanded. Violates WCAG 4.1.2 (name/role/value).
  - _FIX:_ Add aria-expanded={expanded} to each section-toggle button (and aria-controls pointing at the collapsible content id). Pure state exposure, no behavior change. Apply across all 10 listed files.
- **[MEDIUM]** `src/components/ui/GraphicsSettingsPanel.tsx:519` · L6 UX · local · conf 0.9 · AUTO
  - **Keyboard shortcuts cheat-sheet shows stale 'A | AI Panel' — actual key is 'I'** — Lines 514-525 render a kbd 'A' labeled 'AI Panel'. The handler was deliberately moved off A to I to avoid the WASD movement conflict (useKeyboardShortcuts.ts:193 comment 'changed from A to avoid conflict with WASD movement'; line 194 `if (key === 'i')`). Pressing A does nothing for the AI panel; the in-app help is wrong. (App.tsx:581 screen-reader notice correctly says 'I for AI panel', so the two help surfaces disagree.)
  - _FIX:_ Change the kbd label from 'A' to 'I' in the Controls grid (and similarly verify 'AI Panel' label text). SCADA ('O') is missing from this list entirely and could be added.
- **[MEDIUM]** `src/components/ui/GraphicsSettingsPanel.tsx:623` · L6 UX · local · conf 0.9 · AUTO
  - **GraphicsSettingsPanel reset clears wrong/nonexistent keys, so graphics settings survive the reload** — Line 623 removes 'millos-settings' (nonexistent) and 'millos-game-simulation', then calls setGraphicsQuality('medium') and reloads. Because the real graphics persist key 'millos-graphics' is never cleared, the persist middleware re-hydrates the OLD graphics settings on reload, immediately overwriting the in-memory setGraphicsQuality('medium') call. The graphics 'reset' is therefore ineffective — only the game-sim store is actually reset.
  - _FIX:_ Change 'millos-settings' to 'millos-graphics' so the persisted graphics state is cleared before reload; setGraphicsQuality('medium') becomes the rehydrated default.
- **[MEDIUM]** `src/components/ui/KeyboardShortcutsModal.tsx:108` · L5 A11y · local · conf 0.9 · AUTO
  - **Icon-only modal close button (X) has no accessible name** — `<button onClick={onClose} className="w-8 h-8 rounded-lg ..."><X className="w-4 h-4" /></button>` (lines 108-117). The only child is the lucide X icon (decorative, no aria-hidden, no label), so the button has no accessible name. Compare EmergencyStopButton.tsx (aria-label + aria-hidden on icon) which the codebase already does correctly. Violates WCAG 4.1.2.
  - _FIX:_ Add aria-label="Close keyboard shortcuts" to the button and aria-hidden="true" to the X icon.
- **[MEDIUM]** `src/components/ui/KeyboardShortcutsModal.tsx:166` · L5 A11y · ripple · conf 0.85 · FLAG
  - **Secondary text color text-slate-500 fails WCAG AA contrast on dark panels** — Footer text `<p className="text-[10px] ... text-slate-500 ...">` (line 166) renders slate-500 (#64748b) on the slate-900/95 panel. Measured contrast: slate-500 on slate-900 = 3.75:1, on slate-800 = 3.07:1 (WCAG AA requires 4.5:1 for normal text; small <18px text gets no large-text exemption). slate-600 on slate-900 = 2.36:1. This slate-500/600 secondary-text pattern is pervasive across ui/*.tsx (timestamps, labels, footers, e.g. text-[9px]/[10px] text-slate-500). slate-400 (#94a3b8) measures ~7.0/5.7:1 and PASSES — only slate-500/600 fail. Marked ripple because the durable fix is a shared token/palette decision across many panels, and it changes visual appearance.
  - _FIX:_ Owner decision: raise dark-theme secondary text from slate-500/600 to slate-400 (or lighter) for body/label text on slate-800/900 surfaces, or reserve slate-500/600 only for decorative/non-essential text. Verify each change visually (3D/visual posture). Do not bulk auto-replace.
- **[MEDIUM]** `src/components/ui/KeyboardShortcutsModal.tsx:17` · L8 Visual · local · conf 0.85 · FLAG
  - **Internal `if (!isOpen) return null` defeats the AnimatePresence exit animation** — The component defines exit transitions (`exit={{ opacity: 0 }}` line 76, `exit={{ scale: 0.9, opacity: 0 }}` line 86) and is mounted at the call site inside <AnimatePresence> (UIOverlay.tsx:1389-1393: `<AnimatePresence>{showShortcuts && <KeyboardShortcutsModal .../>}`). But line 17 `if (!isOpen) return null;` makes the component unmount itself synchronously the instant isOpen flips false, before AnimatePresence can play the exit. Result: open animates in, close pops out with no fade/scale-out. AboutModal.tsx:22-23 demonstrates the correct pattern (AnimatePresence wraps `{isOpen && ...}` *inside* the component).
  - _FIX:_ Remove the internal `if (!isOpen) return null;` and instead wrap the returned JSX in `<AnimatePresence>{isOpen && (...)}</AnimatePresence>` inside the component (mirroring AboutModal), and drop the redundant outer AnimatePresence/`showShortcuts &&` gating in UIOverlay.tsx:1389-1393. Restructure, so verify the close path still mounts/unmounts.
- **[MEDIUM]** `src/components/ui/ShiftHandoverSummary.tsx:230` · L5 A11y · local · conf 0.85 · AUTO
  - **Auto-shown handover summary: unlabeled close X and not announced** — Icon-only close button `<button onClick={() => setShowSummary(false)} ...><X size={16} /></button>` (lines 230-235) has no aria-label. The summary appears automatically on shift change but its container (header at ~line 200) has no role="status"/aria-live, so AT users are not notified it appeared. Violates WCAG 4.1.2 and 4.1.3.
  - _FIX:_ Add aria-label="Close shift handover" to the X button (aria-hidden on icon); add role="status" aria-live="polite" (or role="dialog" aria-label) to the summary container.
- **[MEDIUM]** `src/components/ui/StrategicPriorityCards.tsx:100` · L5 A11y · local · conf 0.9 · AUTO
  - **Three icon-only dismiss buttons unlabeled; AI 'analyzing' status not announced** — Dismiss buttons at lines 100-105 (`<X className="w-3.5 h-3.5 ..." />`), 140-145, and 166-171 are icon-only with no aria-label. Additionally the 'Gemini analyzing...' thinking indicator (lines 41-52, `<span className="text-sm text-cyan-400">Gemini analyzing...</span>`) is dynamic status content with no aria-live region. Violates WCAG 4.1.2 and 4.1.3.
  - _FIX:_ Add aria-label="Dismiss priority" / "Dismiss insight" / "Dismiss trade-off" to the three X buttons; wrap the thinking indicator in a container with role="status" aria-live="polite".
- **[MEDIUM]** `src/components/ui/TimelinePlayback.tsx:124` · L5 A11y · local · conf 0.9 · AUTO
  - **Replay controls: icon-only buttons (close, play/pause) lack accessible names** — The exit-replay button `<button onClick={exitReplay} ...><X className="w-4 h-4 ..." /></button>` (lines 124-130) and the play/pause toggle `<button onClick={() => setIsPlaying(!isPlaying)} ...>{isPlaying ? <PauseCircle.../> : <PlayCircle.../>}</button>` (lines 178-187) are icon-only with no aria-label. The SkipBack button has title="Jump to start" (line 174) but the close and play/pause have nothing. The range slider (lines 147-161) also has no aria-label. Violates WCAG 4.1.2 / 1.1.1.
  - _FIX:_ Add aria-label="Exit replay mode" to the X button, aria-label={isPlaying ? 'Pause playback' : 'Play playback'} aria-pressed={isPlaying} to the toggle, and aria-label="Playback timeline" to the range input. Add aria-hidden to icons.
- **[MEDIUM]** `src/components/ui/ZoneCustomizationPanel.tsx:72` · L5 A11y · local · conf 0.8 · FLAG
  - **Remove-zone button only visible on hover (not focus-visible) and unlabeled** — `<button onClick={() => removeSpeedZone(zone.id)} className="opacity-0 group-hover:opacity-100 p-1 transition-all ..." title="Remove zone">` (lines 72-79). It is `opacity-0` until mouse hover with no `focus:opacity-100` / `focus-within` rule, so a keyboard user who tabs to it gets a focused-but-invisible control (fails WCAG 2.4.7 Focus Visible). The icon child relies on title only, no aria-label (4.1.2).
  - _FIX:_ Add focus-visible:opacity-100 (and group-focus-within:opacity-100 on the row) so the control becomes visible on keyboard focus; add aria-label="Remove zone".
- **[MEDIUM]** `src/hooks/useKnowledgeIntegration.ts:88` · L6 UX · local · conf 0.82 · FLAG
  - **First-run welcome flag is set even when narration is disabled, silently burning the only first-play moment** — The first-play effect (lines 88-109) writes `localStorage.setItem('millos-has-played', 'true')` (line 98) BEFORE/independently of whether the narration actually fires — triggerNarration (line 69-83) early-returns if `!FEATURE_FLAGS.AI_NARRATION_ENABLED || !narrationEnabled`. So if a user has AI Reflections turned off (toggleable in SettingsPanel:223-231) on first load, the welcome is permanently marked 'shown' and never appears even after they enable it. The flag should be set only when the welcome is genuinely delivered.
  - _FIX:_ Move the `localStorage.setItem('millos-has-played','true')` and `hasTriggeredFirstPlay=true` writes inside the success path (after a narration is actually returned/shown), or gate the whole first-play effect on narration being enabled so the flag isn't consumed while disabled.
- **[MEDIUM]** `src/hooks/useTextureWorker.ts + src/workers/textureWorker.ts` · L9 Performance · local · conf 0.85 · AUTO
  - **Entire texture Web Worker subsystem (~500 lines) is dead code — never imported** — grep across src (excluding the files' own definitions and 0.10 Archive) for useTextureWorker / TextureWorkerManager / getTextureWorkerManager / decodeTexture returned NO consumers. textureWorker.ts (287 lines) and useTextureWorker.ts (213 lines incl. a full TextureWorkerManager class) are unused. The worker is only constructed via `new Worker(new URL('../workers/textureWorker.ts', import.meta.url))` inside these unused hooks, producing an orphan worker chunk in the build.
  - _FIX:_ Delete src/hooks/useTextureWorker.ts and src/workers/textureWorker.ts (dead-code removal, no runtime change). Confirm no test imports first.
- **[MEDIUM]** `src/index.css:28` · L8 Visual · ripple · conf 0.85 · FLAG
  - **Type-scale fragmentation: text-xs override to 14px forces 1089 arbitrary sub-12px font sizes across 82 files** — src/index.css line 28 overrides `--text-xs: 0.875rem; /* 14px */` (Tailwind default is 12px) and line 29 `--text-sm: 1rem; /* 16px */`. This makes the smallest scale step (text-xs) 14px — too large for dense industrial UI. The codebase compensates by hand-writing 1089 arbitrary font-size utilities that bypass the scale entirely: `text-[10px]` x500, `text-[9px]` x375, `text-[8px]` x159, `text-[11px]` x27, `text-[7px]` x26, `text-[15px]` x2. Meanwhile 573 `text-xs` and 360 `text-sm` classes still exist rendering at the inflated 14px/16px. The result is two competing typographic systems and no enforceable hierarchy. Example FederationPanel.tsx:206 `<div className="text-[7px] text-slate-500">Members</div>`.
  - _FIX:_ Product/design decision: either revert --text-xs to 12px (0.75rem) and --text-sm to 0.875rem so the scale covers the small sizes devs actually need (reduces need for text-[10px]/[9px]), OR add named scale steps (e.g. `--text-2xs: 0.625rem` for 10px, `--text-3xs: 0.5625rem` for 9px) in @theme and migrate the arbitrary values to them. Do NOT auto-mass-edit: changing text-xs to 12px shifts 573 existing text-xs usages smaller and is a visible UX change requiring owner sign-off and visual review.
- **[MEDIUM]** `src/main.tsx:10-14` · L9 Performance · local · conf 0.75 · FLAG
  - **Rapier ~2.2MB WASM eagerly pre-warmed at module top-level on every page load** — Lines 10-14: `import('@dimforge/rapier3d-compat').then((RAPIER) => { RAPIER.init()... })` runs unconditionally at startup, downloading and instantiating the ~2.2MB Rapier WASM physics engine (the 'three-rapier' chunk, vite.config.ts:106) before any user action, competing for bandwidth with the React/three boot. The comment claims reduced perceived startup, but it front-loads 2.2MB onto the critical boot window even for users who never trigger physics.
  - _FIX:_ Gate the pre-warm behind requestIdleCallback or first-interaction so the visible app boot finishes before fetching/instantiating physics WASM. Behavior-sensitive (slight delay before first Physics mount), flag for owner.
- **[MEDIUM]** `src/main.tsx:33` · L11 DeadCode · local · conf 0.85 · FLAG
  - **prototype eagerly bundled in shared entry** — Static import of AssetPrototypePage stops Rollup splitting 162KB; loads for every visitor.
  - _FIX:_ React.lazy + Suspense.
- **[MEDIUM]** `src/multiplayer/MultiplayerManager.ts:141` · L2 Security · local · conf 0.85 · AUTO
  - **Player names from peer metadata stored unsanitized (sanitizePlayerName never wired in)** — handlePeerConnected reads `const playerName = metadata?.playerName || 'Player';` (line 141) from untrusted PeerJS connection metadata and stores it via store.addRemotePlayer/addPeer (lines 175-193) and re-broadcasts it (lines 228-238) with no sanitization. The PLAYER_JOIN receive handler does the same: `name: message.payload.name` (line 291). sanitizePlayerName() (sanitize.ts:162, caps to 20 chars, alnum/_/-/space only) exists and is exported but has ZERO callers. A malicious peer can inject arbitrary-length names with control chars that propagate to all clients and render in the chat/lobby UI.
  - _FIX:_ Wrap untrusted names: at line 141 `const playerName = sanitizePlayerName(metadata?.playerName) || 'Player';` and at line 291 `name: sanitizePlayerName(message.payload.name) || 'Player'`. Import sanitizePlayerName from '../utils'.
- **[MEDIUM]** `src/multiplayer/MultiplayerManager.ts:284-285` · L2 Security · local · conf 0.82 · FLAG
  - **PLAYER_UPDATE/PLAYER_JOIN let any peer mutate arbitrary remote-player entries by id** — case 'PLAYER_UPDATE': store.updateRemotePlayer(message.payload.id, message.payload). The id is taken from the untrusted payload, not from the authenticated peerId. A peer can send PLAYER_UPDATE with id set to another player's id and overwrite their position/name/color/selectedMachineId, or PLAYER_JOIN (line 288-300) to inject phantom players. There is no check that payload.id === the sending connection's player. In a host-authority model only the host should originate PLAYER_JOIN/UPDATE about other players; a guest should only be able to update its own entry.
  - _FIX:_ When isHost, derive the player id from the connection's peerId map and ignore payload.id for PLAYER_UPDATE from guests; only accept updates where payload.id maps to that peer. Treat PLAYER_JOIN/PLAYER_LEAVE as host-authoritative: guests must not be able to inject them (drop these message types when received from a non-host peer).
- **[MEDIUM]** `src/multiplayer/MultiplayerManager.ts:350-351` · L1 Correctness/Bugs - Integration (SCADA+multiplayer+protocols) · local · conf 0.7 · FLAG
  - **Host applies untrusted peer MACHINE_LOCK/PLAYER_JOIN/PLAYER_LEAVE without authority validation → desync vector** — handleMessage trusts message.payload for every type with no validation. A malicious/buggy guest can send `{type:'MACHINE_LOCK', payload:{machineId, playerId}}` and the host applies `store.setMachineLock(message.payload.machineId, message.payload.playerId)` (lines 350-351) directly, bypassing the intent-handler authority check (machine-lock is otherwise routed through ADJUST intents in handleMachineIntent, useMultiplayerSync.ts:88-94). Similarly a guest can inject arbitrary PLAYER_JOIN/PLAYER_LEAVE (lines 288-303) to add/remove phantom players in everyone's store. Peers are untrusted (PeerJS WebRTC). This is primarily a state-consistency/desync correctness issue (and an authority bug); not a server-auth concern since there is no backend.
  - _FIX:_ On the host, gate MACHINE_LOCK/PLAYER_JOIN/PLAYER_LEAVE to host-authored broadcasts only: ignore inbound MACHINE_LOCK from guests (require it to flow via INTENT/handleMachineIntent), and ignore guest-originated PLAYER_JOIN/LEAVE. Validate payload.playerId matches the sender's connection identity. Behavior/contract change — owner decision.
- **[MEDIUM]** `src/multiplayer/MultiplayerManager.ts:470-488` · L1 Correctness/Bugs - Integration (SCADA+multiplayer+protocols) · local · conf 0.75 · AUTO
  - **Stale-peer sweep drops a still-open DataConnection without closing it (leak on disconnect)** — The ping interval detects stale peers and routes them to handlePeerDisconnected (lines 486-487). handlePeerDisconnected only does `this.peerConnections.delete(peerId)` (line 274) and never calls `peerConn.close()`. On the normal 'close' event path the underlying DataConnection is already closing, but on the STALE path the connection is still open (stale = no messages for 5s, not closed). The PeerConnection wrapper and its underlying WebRTC DataConnection are dropped from the map while still open, leaking the connection and its handlers.
  - _FIX:_ In handlePeerDisconnected (or before deletion in the stale sweep), close the wrapper: `this.peerConnections.get(peerId)?.close();` prior to `this.peerConnections.delete(peerId);`. PeerConnection.close() already guards isOpen and calls connection.close().
- **[MEDIUM]** `src/multiplayer/MultiplayerManager.ts:596` · L2 Security · local · conf 0.9 · AUTO
  - **Chat messages broadcast & stored unsanitized (sanitizeChatMessage never wired in)** — sendChat() builds `const chatMessage = { ... message, ... }` from the raw string and both stores it locally (store.addChatMessage at line 608) and broadcasts it to peers (lines 611-614) with no sanitization. The matching receive path at line 354-355 `case 'CHAT': store.addChatMessage(message.payload);` stores raw peer-supplied content. sanitizeChatMessage() (sanitize.ts:178, strips \x00-\x1F\x7F control bytes, trims, slices to 500, HTML-encodes) exists and is exported but has ZERO callers. The UI maxLength=200 (MultiplayerChat.tsx:153) is a local-input-only attribute; untrusted remote peers can send arbitrarily long messages with control characters straight into the store. React text interpolation ({msg.message}) blunts direct-DOM XSS, so this is defense-in-depth + DoS/control-char hygiene rather than active script execution.
  - _FIX:_ In sendChat(), sanitize before building chatMessage: `const clean = sanitizeChatMessage(message); if (!clean) return; ... message: clean`. In handleMessage() CHAT case, sanitize on receive: `store.addChatMessage({ ...message.payload, message: sanitizeChatMessage(message.payload.message) });`. Import sanitizeChatMessage from '../utils'.
- **[MEDIUM]** `src/multiplayer/PeerConnection.ts:47` · L4 API/Errors · local · conf 0.9 · AUTO
  - **Silent swallow of incoming data with a misleading 'parse' comment; no message-shape guard at the wrapper boundary** — Lines 47-55: `this.connection.on('data', (data) => { try { const message = data as MultiplayerMessage; this.handleMessage(message); } catch { /* Failed to parse message - ignore */ } })`. The comment is wrong — PeerJS already deserialized the object, so nothing is being parsed; the try/catch silently swallows ANY error thrown downstream in handleMessage AND the entire MultiplayerManager.handleMessage chain (invoked via the onMessage callback at :94). That means real bugs/exceptions in message handling vanish with no log. The cast `data as MultiplayerMessage` also provides no runtime safety. This is the boundary where a `typeof message?.type === 'string'` guard belongs.
  - _FIX:_ Replace the bare cast with a minimal runtime guard: if (!message || typeof (message as { type?: unknown }).type !== 'string') return; then handleMessage. In the catch, log via logger.multiplayer.warn with the error instead of swallowing, and correct the comment to 'handler error' not 'parse'.
- **[MEDIUM]** `src/multiplayer/PeerConnection.ts:47-113` · L2 Security · local · conf 0.8 · FLAG
  - **No inbound message-size or rate bound on WebRTC DataChannel (untrusted-peer DoS)** — PeerConnection has no cap on the size or frequency of incoming messages. A malicious peer can flood the host with FULL_STATE_SYNC/CHAT/PLAYER_UPDATE messages or a single huge payload (e.g. CHAT.message of many MB, or a STATE_SYNC with a giant machines array), all of which are dispatched into Zustand store mutations and, for CHAT, accumulated in addChatMessage. There is no per-peer message-rate throttle or byte-length guard. Combined with the validation gap above, this is a cheap client-side DoS / memory-exhaustion vector.
  - _FIX:_ In PeerConnection.handleMessage, reject messages whose serialized JSON length exceeds a sane cap (e.g. 64KB for control msgs); enforce a max CHAT.message length (e.g. 500 chars) in validation; add a simple per-peer token-bucket rate limit (drop+count when exceeded). Cap chat history length in store.addChatMessage.
- **[MEDIUM]** `src/multiplayer/SignalingService.ts:134-138` · L3 Privacy/GDPR — Multiplayer data sharing · local · conf 0.8 · FLAG
  - **No consent gate: host auto-accepts any incoming peer and existing players never approve new participants** — handleIncomingConnection accepts every inbound connection with no gate: `private handleIncomingConnection(conn: DataConnection): void { const peerId = conn.peer; ...; this.setupConnectionHandlers(conn, peerId); }`. The host then broadcasts the newcomer's name to all other peers (MultiplayerManager.ts:228-238). Existing participants neither consent to, nor can refuse, a specific new peer learning their display name and receiving their avatar/position stream. There is no UI anywhere informing a user that entering a name and joining will share that name and live position with arbitrary other participants.
  - _FIX:_ Owner decision (UX/behavior): add an approval prompt for incoming peers and/or a one-line disclosure at name entry in MultiplayerLobby ('Your name, chat, and in-game position will be shared with other players in this room.').
- **[MEDIUM]** `src/multiplayer/SignalingService.ts:25-30` · L3 Privacy/GDPR — Multiplayer data sharing · local · conf 0.9 · FLAG
  - **Signaling (peer IDs embedding room code + player name/id metadata) routed through default PeerJS cloud broker, a third party, undisclosed** — The default PeerJS cloud broker (no custom `host`/`path`/`key` in PEERJS_CONFIG; comment: 'Uses default PeerJS cloud server (free tier...)') handles signaling. What touches this third party is: (1) the peer IDs, which embed the room code in cleartext — `const peerId = this.config.isHost ? \`millos-${this.config.roomCode}\` : \`millos-${this.config.roomCode}-${this.config.playerId}\`;` (lines 53-55); and (2) the connection metadata `{ playerName: this.config.playerName, playerId: this.config.playerId }` sent at connect time (lines 122-125). Note: chat messages and player positions travel peer-to-peer over the encrypted DataChannel and do NOT pass through the broker — only signaling/handshake data does. Still, player name + room code reaching an undisclosed third-party processor with no DPA, consent, or notice is a GDPR transparency/lawful-basis gap.
  - _FIX:_ Owner decision: disclose the third-party broker in a privacy notice, and/or self-host peerjs-server (link already in the file comment) to keep signaling under the operator's control. Do not transmit the player name as broker metadata if it can be exchanged post-connection over the DataChannel instead.
- **[MEDIUM]** `src/multiplayer/SignalingService.ts:53 / src/multiplayer/MultiplayerManager.ts:196-225` · L3 Privacy/GDPR — Multiplayer data sharing · local · conf 0.85 · FLAG
  - **Guessable 6-char room code + auto-FULL_STATE_SYNC discloses all players' names/colors to an uninvited peer** — The host peer ID is deterministic: `const hostPeerId = \`millos-${this.config.roomCode}\`;` where roomCode is a 6-char code from a 32-symbol alphabet (multiplayerStore.generateRoomCode). Anyone who guesses or enumerates a code connects to the host, and on connection the host immediately sends FULL_STATE_SYNC and then loops over every existing remote player broadcasting their name and color to the newcomer: `peerConn.send({ type: 'PLAYER_JOIN', payload: { id: existingPlayer.id, name: existingPlayer.name, color: existingPlayer.color } });` (MultiplayerManager.ts:214-225). So an uninvited peer receives the PII (display names) of everyone already in the room. No rate limiting, no room password, no host approval.
  - _FIX:_ Owner decision (UX/behavior change): add a host approval gate before sending PLAYER_JOIN/FULL_STATE_SYNC to a new connection, and/or a longer/secret room code or join PIN. Do not broadcast existing players' names to a peer until the host accepts them.
- **[MEDIUM]** `src/multiplayer/types.ts:20` · L6 UX · local · conf 0.8 · FLAG
  - **reconnecting styled but never set** — reconnecting styled in both UIs but never set; transient drop shows nothing.
  - _FIX:_ Set in reconnect path or remove dead branches.
- **[MEDIUM]** `src/protocols/vcp/decoder.ts:36` · L1 Correctness/Bugs - Integration (SCADA+multiplayer+protocols) · ripple · conf 0.9 · FLAG
  - **VCP encoder/decoder roundtrip collision: 'mastery' encodes to 'M' and decodes to 'meaning'** — FlourishingDimension includes both 'meaning' and 'mastery' (types.ts:130-136). The encoder writes the concern dimension as `concernDimension[0].toUpperCase()` (encoder.ts:114-115), so both 'meaning' and 'mastery' encode to the single letter 'M'. The wellbeing decode regex `/\[WELL:F(\d+)([↑→↓])\|?([MMCJWA])?\]/u` (decoder.ts:36 — note the duplicated 'M' in the char class) plus dimMap `{ M:'meaning', A:'agency', C:'connection', J:'joy', W:'wholeness' }` (decoder.ts:154-160) has NO entry for 'mastery'. Result: encoding concernDimension='mastery' decodes back to 'meaning' (silent data corruption). Same defect in the reasoning layer: encoder.ts:211 `flourishingFocus[0].toUpperCase()` vs decoder.ts:324-330 dimMap, also missing 'mastery'. Existing test uses concernDimension:'agency' (encoding.test.ts:74) so the collision is untested/latent. Decode is currently only re-exported (vcp/index.ts) with no found UI/state consumer, so impact is on cross-system interop/debugging, not live UI.
  - _FIX:_ Pick a distinct wire symbol for 'mastery' (e.g. 'Y' or lowercase 'm') and update all three places in lockstep: encoder mapping, the WELL/R decode regex char classes, and the decoder dimMaps. This touches the encoded wire format (ripple) and any persisted VCP strings; owner decision on the symbol.
- **[MEDIUM]** `src/scada/HistorianRouter.ts:126` · L4 API/Errors · local · conf 0.7 · AUTO
  - **No time-range validation before historian queries (start>end, NaN dates, intervals=0 reach adapters)** — getRecordedValues/getInterpolatedValues/getPlotValues (lines 126-166) and the underlying PI/WW adapters perform zero validation that `startTime <= endTime`, that dates are valid (an Invalid Date yields `.toISOString()` throw), or that `intervals`/`intervalMs` are positive. A reversed range silently produces an empty or server-error result; an Invalid Date throws inside the adapter URL build. There is no single choke point enforcing the contract documented in HistorianInterface.ts.
  - _FIX:_ Add a guard at the top of getBlendedData / each public method: if either date is invalid (`Number.isNaN(startTime.getTime())`) or `endTime < startTime`, return [] (or throw a typed RangeError) before delegating. Cheap, prevents adapter-level surprises.
- **[MEDIUM]** `src/scada/adapters/MQTTAdapter.ts:183` · L1 Correctness/Bugs - Integration (SCADA+multiplayer+protocols) · local · conf 0.85 · FLAG
  - **MQTT packets use single-byte remaining-length; payloads >127 bytes are malformed (encode + decode)** — MQTT 'Remaining Length' is a variable-length quantity (1-4 bytes). The encoder writes it as one byte: `packet[1] = remainingLength;` in sendConnectPacket (line 234), publish (line 183), sendSubscribePacket (line 252), sendUnsubscribePacket (line 275). For any packet whose remaining length exceeds 127 this silently truncates/corrupts the length field. The receiver mirrors the bug: handlePublish reads `const remainingLength = bytes[offset++];` (line 303, comment admits 'simplified - assumes single byte') and then `payload = ...slice(offset, 2 + remainingLength)` (line 314), so multi-byte length tag payloads are mis-parsed. Real protocol bug; low real-world severity because the shipped static deploy is simulation-first and a live MQTT broker is unlikely to be reachable (no broker/CORS in the GitHub-Pages config).
  - _FIX:_ Implement proper MQTT variable-length-integer encoding/decoding (continuation-bit scheme) for both the writer (packet[1]...) and handlePublish's remaining-length read. Behavior-changing protocol work — owner decision.
- **[MEDIUM]** `src/scada/adapters/PIAdapter.ts:151` · L4 API/Errors · local · conf 0.82 · AUTO
  - **PI adapter maps response.Items without verifying it is an array (response-shape gap → silent empty result)** — Every PI read does `const data: PIStreamValuesResponse = await response.json(); return this.mapPIValuesToHistoryPoints(data.Items);` (lines 151-152, 179-180, 205-206; getLatestValue line 219 maps `[data]` assuming the body is a single PIValue). If PI returns a body without `Items` (error envelope, partial response), `items.map` in mapPIValuesToHistoryPoints (line 363) throws. HistorianRouter.fetchFromRemote catches it (line 273) and returns `[]`, so a malformed response degrades to 'no data' with only a logger.warn — charts show empty trends and the operator gets no signal that the historian returned garbage.
  - _FIX:_ Guard each: `const items = Array.isArray(data?.Items) ? data.Items : []; return this.mapPIValuesToHistoryPoints(items);`. For getLatestValue, validate `data` is an object with a Timestamp before mapping.
- **[MEDIUM]** `src/scada/adapters/PIAdapter.ts:329` · L4 API/Errors · local · conf 0.66 · AUTO
  - **PI getWebId assumes /points?path= returns a single PIPointResponse object; PI returns Items[] for path queries** — `const data: PIPointResponse = await response.json(); this.webIdCache.set(tagId, data.WebId); return data.WebId;` (lines 329-331). PI Web API point lookups by path commonly return either a single point or, for some endpoints, an envelope. If the body is an error/empty object, `data.WebId` is undefined and gets cached (line 330) — the undefined WebId is then cached permanently, so every subsequent read for that tag short-circuits via the cache check `if (cachedWebId !== undefined)` at line 313... note: caching `undefined` actually does NOT poison the cache (it's not set when undefined-keyed), but caching a literal undefined value via set() stores `undefined` and the `!== undefined` guard then treats a cache-miss-stored-undefined as a hit returning undefined. Net: a one-time bad lookup is cached as a permanent failure for that tag until disconnect.
  - _FIX:_ Validate before caching: `if (!data || typeof data.WebId !== 'string') { return null; }` then `this.webIdCache.set(tagId, data.WebId); return data.WebId;` so transient failures are not cached as permanent.
- **[MEDIUM]** `src/scada/adapters/PIAdapter.ts:394` · L4 API/Errors · local · conf 0.7 · AUTO
  - **msToIsoDuration emits PT0S for sub-second intervals and malformed durations for negative/NaN input** — `const seconds = Math.floor(ms / 1000); if (seconds < 60) return \`PT${seconds}S\`;` — any intervalMs < 1000 (e.g. a 500ms interpolation request) floors to `PT0S`, which PI Web API rejects as an invalid interval. A negative intervalMs yields `PT-5S` and NaN yields `PTNaNS`. getInterpolatedValues passes this straight into the URL (line 166-171) with no lower bound.
  - _FIX:_ Clamp at the top: `const safeMs = Number.isFinite(ms) && ms > 0 ? ms : 1000;` and ensure the smallest unit is at least 1 (`const seconds = Math.max(1, Math.floor(safeMs/1000))`).
- **[MEDIUM]** `src/scada/adapters/RESTAdapter.ts:343-358` · L2 Security · local · conf 0.78 · FLAG
  - **Bearer token attached to any URL with no scheme/host allowlist (token leak to untrusted endpoints)** — fetchWithAuth validates only `new URL(url)` parses (line 346), then unconditionally sets `Authorization: Bearer ${this.config.apiKey}` (line 354) for every request, including to whatever baseUrl the user/config supplies. There is no check that the scheme is https or that the host matches the configured baseUrl origin. If baseUrl is http:// (allowed - no scheme restriction), the bearer token is sent in cleartext. The URL-validation comment implies a security control but it only guards against malformed URLs, not against sending credentials to an attacker-controlled or downgraded endpoint. PIAdapter.fetchWithAuth (PIAdapter.ts:338-360) has the same pattern with no URL/scheme validation at all.
  - _FIX:_ In fetchWithAuth, only attach the Authorization header when url.protocol === 'https:' (or host is localhost), and assert the request URL's origin matches the configured baseUrl origin before sending the token. Apply the same origin/scheme guard in PIAdapter.fetchWithAuth.
- **[MEDIUM]** `src/scada/adapters/WebSocketAdapter.ts:319` · L4 API/Errors · local · conf 0.65 · AUTO
  - **WS batch/snapshot handler trusts t.tagId/value/quality/timestamp without per-field guards despite validator gaps** — In handleMessage, batch/snapshot does `msg.tags.map((t) => ({ tagId: t.tagId, value: t.value, quality: this.parseQuality(t.quality), timestamp: t.timestamp }))` (lines 321-327). isValidWSMessage→isValidBatchTag does validate these fields, so today it is safe, but parseQuality (line 356) calls `quality.toUpperCase()` — if a future message path bypasses the validator or quality is the empty string the result is fine, but if quality were undefined (e.g. validator relaxed) this throws. The 'update' path is safer (parseTagValue uses `?? 'GOOD'`). Inconsistency: update defends with nullish coalescing, batch relies entirely on the external validator. Low-risk today, brittle to validator drift.
  - _FIX:_ Make parseQuality null-safe (`const q = String(quality ?? '').toUpperCase();`) and apply `?? Date.now()` / `?? 0` defaults in the batch map to match parseTagValue, so the two ingest paths fail identically.
- **[MEDIUM]** `src/scada/adapters/WebSocketAdapter.ts:319-331` · L2 Security · local · conf 0.7 · AUTO
  - **Validated WS message tags written to value store with no tagId allowlist (unvalidated tag writes / injection)** — In the 'batch'/'snapshot' branch, msg.tags is iterated and `this.values.set(tv.tagId, tv)` stores every tag id the server (or a MITM on an unauthenticated ws://) sends, even tag ids not in this.tags (the known TagDefinition set). isValidWSMessage only checks tagId is a non-empty string, not that it is a known tag. A malicious/compromised proxy can inject arbitrary tagIds and values that then propagate to global subscribers and any UI/alarm logic keyed on tag values. The 'update' branch (line 311-317) has the same gap. Writes outbound are gated by accessMode (line 211) but inbound values are not gated to known tags.
  - _FIX:_ In handleMessage update/batch/snapshot branches, drop tag values whose tagId is not in this.tags (or whose accessMode forbids the source). i.e. `if (!this.tags.has(t.tagId)) return;` before this.values.set. Optionally clamp numeric values to the tag's engLow/engHigh.
- **[MEDIUM]** `src/scada/adapters/WebSocketAdapter.ts:83-93` · L2 Security · local · conf 0.72 · FLAG
  - **WebSocket adapter silently downgrades https->ws (insecure) instead of wss** — const url = wsUrl.replace(/^http/, 'ws'); maps both http->ws AND https->ws? No: it maps https->wss correctly only by accident because 'https' starts with 'http' and becomes 'wsss'... actually `'https://x'.replace(/^http/,'ws')` yields 'wss://x' (correct). But a bare `ws://`/`http://` config is accepted with no enforcement of a secure scheme, so a plaintext, unauthenticated WebSocket to a SCADA proxy is allowed. The bigger gap: there is no auth on the WS connection at all (no token in the URL or first message) - any party who can reach the proxy URL can subscribe to and write tags. Writes are sent as `{type:'write', tagId, value}` (lines 216-220) over this unauthenticated channel.
  - _FIX:_ Warn or refuse when the resolved url is ws:// (non-wss) unless host is localhost. Add an auth token (from config) to the WS handshake (query param or first 'subscribe' message) so the proxy can reject unauthenticated clients. Confirm the https->wss replacement intent with an explicit scheme map rather than a regex on 'http'.
- **[MEDIUM]** `src/scada/adapters/WebSocketAdapter.ts:95` · L4 API/Errors · local · conf 0.68 · FLAG
  - **Timed-out WebSocket connect still schedules background reconnects after the connect promise rejects** — On connect timeout (line 95-98) the code `reject(new Error('Connection timeout')); this.ws?.close();`. Closing the socket fires `onclose` → `handleDisconnect()` (line 134-135), and because `isDisconnecting` is still false, handleDisconnect schedules an exponential-backoff reconnect (lines 383-393). So a caller whose `connect()` rejected (and who may construct a fresh adapter and retry) now has an orphaned background reconnect loop on the rejected socket, potentially doubling connection attempts and never surfacing as connected/disconnected to the caller.
  - _FIX:_ Set `this.isDisconnecting = true` (or a dedicated `connectAborted` flag) before calling `this.ws?.close()` in the timeout handler so onclose does not start a reconnect for a connect that the caller already saw fail.
- **[MEDIUM]** `src/scada/adapters/WonderwareAdapter.ts:166` · L4 API/Errors · local · conf 0.82 · AUTO
  - **Wonderware adapter maps response.Data without verifying it is an array (response-shape gap)** — `const data: WWHistoryResponse = await response.json(); return this.mapWWValuesToHistoryPoints(data.Data);` (lines 166-167, 191-192, 217-218; getLatestValue line 228 maps `[data]`). No check that `data.Data` exists/is an array. A 200 response missing `Data` throws in mapWWValuesToHistoryPoints (line 332 `items.map`). Caught upstream and converted to empty result, hiding the contract violation from the user.
  - _FIX:_ Guard: `const rows = Array.isArray(data?.Data) ? data.Data : []; return this.mapWWValuesToHistoryPoints(rows);` and validate the single-value body in getLatestValue before mapping.
- **[MEDIUM]** `src/scada/adapters/WonderwareAdapter.ts:213` · L4 API/Errors · local · conf 0.72 · AUTO
  - **getPlotValues fallback divides duration by intervals with no zero/negative guard (Infinity / negative interval)** — On TrendData failure the fallback computes `const intervalMs = Math.floor(durationMs / intervals); return this.getInterpolatedValues(tagId, startTime, endTime, intervalMs);` (lines 212-214). If a caller passes `intervals: 0` (reachable via HistorianQueryOptions.intervals), this is `durationMs / 0 = Infinity` → `resolutionMS=Infinity` in the InterpolatedData URL. If startTime>endTime, durationMs is negative → negative resolutionMS. Either produces a malformed historian request.
  - _FIX:_ `const safeIntervals = intervals > 0 ? intervals : 100; const durationMs = Math.max(0, endTime.getTime() - startTime.getTime()); const intervalMs = Math.max(1, Math.floor(durationMs / safeIntervals));`
- **[MEDIUM]** `src/stores/aiConfigStore.ts:497` · L4 API/Errors · local · conf 0.82 · FLAG
  - **Runtime Gemini failures (429/quota/network) never surface to the user — connectionError set only at setup time** — connectionError is written only during key validation (aiConfigStore.ts:487, :497) and cleared on success (:509). There is NO distinct handling for 429 / RESOURCE_EXHAUSTED / quota anywhere (grep: zero hits for 429/quota/RESOURCE_EXHAUSTED across src). Once connected, every runtime failure path — geminiClient.generateContent catch (geminiClient.ts:318) and generateStrategicDecision catch (aiEngine.ts:3951) — only calls logger.* and returns null; the circuit breaker may open after 3 failures (geminiClient.ts:201) with no UI signal. Net effect: when the user's free-tier key hits a rate/quota limit, the strategic AI silently stops producing decisions and the UI keeps showing 'connected' with no explanation. isConnected() at :158 also reports false once the breaker trips, but nothing maps that to a user-facing message.
  - _FIX:_ On runtime failure in generateStrategicDecision (and/or in geminiClient.generateContent), categorize the error (rate-limit/quota vs auth vs network) and surface it: call a new aiConfigStore action to set connectionError (e.g. 'Gemini rate limit reached — AI paused, retrying in 30s') so GeminiSettingsModal/AICommandCenter can show it. Behavior change — flag for owner.
- **[MEDIUM]** `src/stores/multiplayerStore.ts:14-21` · L2 Security · local · conf 0.75 · FLAG
  - **Room codes are low-entropy and brute-forceable via PeerJS public server** — generateRoomCode() draws 6 chars from a 32-char alphabet = ~30 bits, but the room peer-id is the predictable string `millos-${roomCode}` (SignalingService.ts:54/117). Because the host registers a guessable, namespaced peer-id on the shared PeerJS *public* cloud server, an attacker can enumerate `millos-XXXXXX` ids to discover/join active rooms (or probe which codes are live) without any invite, room password, or host approval. There is no admission control: handleIncomingConnection accepts every connection and immediately adds the peer (SignalingService.ts:134-137, MultiplayerManager.ts:136). Combined with the unvalidated-payload and intent-spoof issues, an unsolicited peer who guesses a code gets full guest authority.
  - _FIX:_ Add an admission step: host should require a join secret (the host-displayed code can stay short, but gate actual acceptance on a longer shared token passed in conn.metadata and verified before calling onPeerConnected/addPeer). Reject connections whose metadata token does not match. Longer term, self-host peerjs-server (already noted as a TODO in SignalingService PEERJS_CONFIG comment) and randomize the host peer-id instead of deriving it from the room code.
- **[MEDIUM]** `src/types.ts:841` · L7 Copy · ripple · conf 0.95 · FLAG
  - **Roster targetMachine 'mill-1.5' references a nonexistent machine** — Worker w2 (Sarah Mitchell): `targetMachine: 'mill-1.5'`. Mills are created in MillScene.tsx:440 as `rm-${millIds[millIndex]}` -> rm-101..rm-106. There is no machine whose id is 'mill-1.5', so `machines.find(m => m.id === 'mill-1.5')` returns undefined. (w1 correctly uses 'rm-103'.) Seed/initial value only — runtime reassigns via updateWorkerTask/assignedBreakdown — so visual impact is limited, but it is an invalid domain reference in the canonical roster.
  - _FIX:_ Change to a valid mill id, e.g. `targetMachine: 'rm-102'` (matches her 'Calibrating Roller Mill #2' task). Touches the shared WORKER_ROSTER in types.ts.
- **[MEDIUM]** `src/types.ts:931` · L7 Copy · ripple · conf 0.95 · FLAG
  - **Roster targetMachine 'sifter-0' references a nonexistent machine** — Worker w8 (Anna Kowalski): `targetMachine: 'sifter-0'`. Plansifters are created in MillScene.tsx:467 as `sifter-${['a','b','c'][idx]}` -> sifter-a, sifter-b, sifter-c. There is no 'sifter-0'; other roster entries correctly use 'sifter-a'/'sifter-b'. Lookup resolves to undefined.
  - _FIX:_ Change to a valid sifter id, e.g. `targetMachine: 'sifter-c'` (a/b already taken by other workers). Touches shared WORKER_ROSTER.
- **[MEDIUM]** `src/utils/apiSecurity.ts:1-642` · L11 DeadCode · local · conf 0.9 · FLAG
  - **Entire apiSecurity module (CSRF/JWT/secureFetch) is dead — intentional 'future auth' scaffolding in a no-backend app** — Every exported symbol has ZERO importers anywhere in src outside its own file and the (also-dead) utils barrel. Grep counts (src + test): secureFetch=0/0, generateCsrfToken=0/0, getOrCreateCsrfToken=0/0, clearCsrfToken=0/0, decodeTokenPayload=0/0, isTokenExpired=0/0, getTokenExpiresIn=0/0, validateRequest=0/0, maskSensitiveData=0/0, clearSensitiveFields=0/0, rateLimiters=0/0, checkRateLimit=0/0, debounce=0/0, throttle=0/0. The file self-documents the intent at line 7: 'Token management utilities (for future auth integration)' and line 249 'JWT-like token structure (for future auth integration)', with OWASP refs. This is intentionally-dead scaffolding, not accidental dead code. Real cost: 642 lines of unused CSRF/JWT/secureFetch in an app the task states has NO backend auth can mislead a future security reviewer into believing auth/CSRF protection exists.
  - _FIX:_ Owner decision: either (a) keep as a deliberately-documented library and add a top-of-file banner '// UNUSED: scaffolding for future server-side auth — not wired into any runtime path', or (b) delete src/utils/apiSecurity.ts and its barrel re-export block (utils/index.ts:123-147) to shed 642 dead lines. Do NOT delete without the product/security call since it advertises a security posture.
- **[MEDIUM]** `src/utils/audioManager.ts:289` · L1 Correctness/Bugs · local · conf 0.85 · FLAG
  - **Muting does not stop an in-flight TTS PA announcement** — The `set muted(value)` setter (lines 289-296) updates master/music/machine WebAudio gains but never touches the SpeechSynthesis TTS pipeline: `set muted(value: boolean) { this._muted = value; this.updateMasterVolume(); this.updateMusicVolume(); this.updateMachineVolumes(); this.saveSettings(); this.notifyListeners(); }`. TTS announcements are spoken via `window.speechSynthesis.speak(utterance)` (line 5620), which is NOT routed through the WebAudio gain graph, so zeroing gains does not silence it. By contrast `set ttsEnabled(false)` calls `this.stopTTS()` (line 5329). The queue-processing guard at line 5486 (`if (!this._ttsEnabled || this._muted)`) and the scheduler guards already block NEW announcements when muted, so the bug is bounded to exactly ONE already-speaking utterance continuing to talk after the user mutes. Clearly-wrong UX: muting should silence the voice immediately.
  - _FIX:_ Call `this.stopTTS()` inside the muted setter when value===true (stopTTS already cancels speechSynthesis, clears the queue, and clears the chime timeout). This changes mute semantics, so it needs owner sign-off.
- **[MEDIUM]** `src/utils/audioManager.ts:5438` · L11 DeadCode · local · conf 0.9 · FLAG
  - **29 ungated console.log in TTS in prod** — 29 raw console.log ungated, echo text on every announcement. logger.audio exists.
  - _FIX:_ Use logger.audio.debug.
- **[MEDIUM]** `src/utils/dracoLoader.ts:33,47` · L9 Performance · local · conf 0.88 · FLAG
  - **DRACO decoder loaded from gstatic CDN at runtime — cross-origin dep + offline-cache miss + possible CSP block** — Line 33: `const DRACO_DECODER_PATH = 'https://www.gstatic.com/draco/versioned/decoders/1.5.7/';`, set at line 47, preload()ed at line 49. forklift.glb (1.6M) is the only DRACO model. This adds a cross-origin fetch to gstatic on first model load, is NOT in SW MODEL_CACHE (cross-origin, breaks offline), and gstatic.com is NOT in index.html CSP connect-src (only cdn.jsdelivr.net + fonts.gstatic.com listed) so the decoder fetch may be CSP-blocked in production. Local fallback path commented out at line 36.
  - _FIX:_ Vendor the DRACO decoder into public/draco/ and set DRACO_DECODER_PATH = `${import.meta.env.BASE_URL}draco/` so it is same-origin, SW-cacheable, CSP-allowed, offline-capable.
- **[MEDIUM]** `tailwind.config.js:1` · L8 Visual · local · conf 0.9 · FLAG
  - **Stale v3-style tailwind.config.js is silently ignored under Tailwind v4 (no @config directive)** — Project runs Tailwind v4.1.18 (src/index.css line 1 `@import 'tailwindcss';`, postcss.config.js uses `@tailwindcss/postcss`, theme defined via `@theme` block). In v4 a JS config file is ONLY loaded if referenced with `@config` in CSS. src/index.css has NO `@config` directive (verified). Therefore tailwind.config.js is dead: its `animation: { 'pulse-slow', 'spin-slow' }`, the `addUtilities` plugin (`.sr-only-focusable`), and `fontFamily`/`colors` blocks do nothing. The mill colors and fonts happen to be duplicated in `@theme` so they still work, but the animations and sr-only-focusable plugin are genuinely unavailable. Confirmed zero usage of `animate-pulse-slow`/`animate-spin-slow`/`sr-only-focusable` across src, so no live breakage TODAY, but the file is misleading config that will mislead future contributors who add `animate-pulse-slow` expecting it to work.
  - _FIX:_ Either (a) delete tailwind.config.js since v4 reads @theme/@source from CSS and the file's unique tokens are unused, or (b) if the team wants to keep the JS config, add `@config '../tailwind.config.js';` to src/index.css AND move the pulse-slow/spin-slow keyframes into @theme. Recommend (a) delete — the CSS @theme is already the source of truth. Verify no future code relies on the config-only tokens first.

## LOW

- **[LOW]** `.github/workflows/deploy.yml:79 + CNAME` · L12 Config · local · conf 0.45 · FLAG
  - **deploy.yml does not include CNAME in the Pages artifact (hardening note, NOT a break)** — Repo root CNAME contains `www.millos.net`, but deploy.yml uploads only `staging/` (the built versioned tree) and never copies CNAME into it. This is NOT a launch blocker: the site is live at millos.net and NONE of the workflows copy a CNAME, which confirms the custom domain is configured in repo Settings -> Pages and the GitHub Actions Pages method (configure-pages/upload-pages-artifact/deploy-pages) persists the domain across deploys without a CNAME file. Flagging only as defense-in-depth: if someone ever switches off the Actions deploy method or the Settings domain is cleared, the absent CNAME-in-artifact means no fallback.
  - _FIX:_ Optional hardening: add a step to `cp CNAME staging/CNAME` before upload-pages-artifact so the custom domain survives a Settings change. Verify against the live working deploy before changing anything — do not 'fix' a working deploy.
- **[LOW]** `README.md:1110` · L7 Copy · local · conf 0.6 · FLAG
  - **Acknowledgments cite 'Opus 4.5' which may be stale relative to current build models** — Line 1110: `Built with [Claude Code](https://claude.ai/code) (Opus 4.5), with assistance from GPT-5.1-codex-max xhigh, Gemini 2.5, and Gemini 3.0 Pro...`. This is a factual attribution of the model that built MillOS; flagging for owner verification only — model attributions date quickly and there is no in-repo source of truth to validate '4.5' against. Not auto-fixable since the correct value is owner knowledge.
  - _FIX:_ Owner to confirm/update the Claude model version in the acknowledgments if it has changed since authoring.
- **[LOW]** `README.md:1121` · L7 Copy · local · conf 0.8 · AUTO
  - **README version footer 'MillOS v0.3' contradicts package.json 0.20.0** — Footer line 1121: `**MillOS v0.3**`. package.json `"version": "0.20.0"`. The footer reads as v0.3 (three-tenths), nearly 17 minor versions behind the actual 0.20.0. Likely a typo where 'v0.20' lost its trailing zero, but as written it understates the project's maturity to readers.
  - _FIX:_ Change `**MillOS v0.3**` to `**MillOS v0.20**` to match package.json.
- **[LOW]** `README.md:27` · L7 Copy · local · conf 0.85 · FLAG
  - **README screenshot links into excluded src/0.10 Archive path** — Line 27: `<td><img src="src/0.10%20Archive/assets/Screenshot.png" alt="MillOS Interior" width="400"/></td>`. The interior screenshot is sourced from the archive directory (which is excluded from active code lanes and is a non-canonical location). The file currently exists, but the README's primary hero image depends on an archive folder that is a deletion/cleanup candidate; the exterior shot correctly uses docs/assets/. Fragile coupling of public-facing README to archived assets.
  - _FIX:_ Copy Screenshot.png to docs/assets/ (alongside MillOSoutside.png) and update the src to `docs/assets/Screenshot.png` so the README no longer depends on the archive dir.
- **[LOW]** `README.md:705` · L7 Copy · local · conf 0.7 · FLAG
  - **Quick Start clone URL is an unfilled placeholder** — Line 705: `git clone https://github.com/your-username/millos.git`. The placeholder 'your-username' is never substituted; the actual repo owner is nellwatson (footer line 1127 links github.com/nellwatson). A reader copy-pasting the clone command hits a non-existent repo.
  - _FIX:_ Replace with the real clone URL, e.g. `git clone https://github.com/nellwatson/millos.git` (confirm exact repo slug with owner).
- **[LOW]** `a:1` · L1 · local · conf 0.5 · AUTO
  - **t1** — d1
  - _FIX:_ f1
- **[LOW]** `b:2` · L1 · local · conf 0.5 · AUTO
  - **t2** — d2
  - _FIX:_ f2
- **[LOW]** `c:3` · L1 · local · conf 0.5 · AUTO
  - **t3** — d3
  - _FIX:_ f3
- **[LOW]** `coverage/` · L11 DeadCode · local · conf 0.92 · AUTO
  - **Generated coverage report (48 files, 2.7MB) is git-tracked and not gitignored** — gitignore has no coverage entry. git ls-files coverage lists 48 tracked files. This is regenerable output of vitest run coverage, not source.
  - _FIX:_ Add coverage to gitignore then git rm -r cached coverage.
- **[LOW]** `d:4` · L1 · local · conf 0.5 · AUTO
  - **t4** — d4
  - _FIX:_ f4
- **[LOW]** `e:5` · L1 · local · conf 0.5 · AUTO
  - **t5** — d5
  - _FIX:_ f5
- **[LOW]** `f:6` · L1 · local · conf 0.5 · AUTO
  - **t6** — d6
  - _FIX:_ f6
- **[LOW]** `g:7` · L1 · local · conf 0.5 · AUTO
  - **t7** — d7
  - _FIX:_ f7
- **[LOW]** `index.html:11` · L12 Config · local · conf 0.7 · AUTO
  - **CSP connect-src permits cleartext ws: alongside wss:** — Line 11 connect-src includes both `ws:` and `wss:`. On an HTTPS GH-Pages origin, a cleartext ws: connection would be a mixed-content violation anyway, so `ws:` grants nothing useful and only widens the policy. Tightening to wss: only removes a needless cleartext allowance with no runtime UX change (PeerJS signaling uses wss).
  - _FIX:_ Remove the bare `ws:` token from connect-src, leaving `wss:` (plus the explicit https hosts).
- **[LOW]** `index.html:11` · L3 · local · conf 0.85 · AUTO
  - **CSP whitelists cdn.jsdelivr.net but src never fetches it** — connect-src has cdn.jsdelivr.net; no runtime fetch in src; deps bundled. Over-permissive.
  - _FIX:_ Remove cdn.jsdelivr.net from connect-src in index.html:11.
- **[LOW]** `index.html:16` · L12 Config · local · conf 0.85 · FLAG
  - **Only an inline SVG-emoji favicon; no PNG/ICO favicon, theme-color, or apple-touch-icon** — Line 16 is the sole icon: `<link rel="icon" href="data:image/svg+xml,<svg ...><text ...>🏭</text></svg>" />`. There is no <meta name="theme-color">, no <link rel="apple-touch-icon">, and no raster favicon/ICO in public/ (find found none). Result: blank/generic icon in Safari/iOS home-screen and many bookmark contexts, no browser-chrome theming color, and no Apple touch icon for the digital-twin app you intend people to keep open.
  - _FIX:_ Add <meta name="theme-color" content="#0a0f1a">, a public/apple-touch-icon.png (180x180) with <link rel="apple-touch-icon">, and a public/favicon-32x32.png/favicon.ico fallback.
- **[LOW]** `index.html:18` · L12 Config · local · conf 0.55 · FLAG
  - **No JSON-LD structured data despite a well-defined product (missed SEO opportunity)** — index.html has good OG/Twitter/canonical (lines 22-41) and a meta description, but no <script type="application/ld+json"> structured data. The app is a clearly-scoped SoftwareApplication/WebApplication ('AI-Powered Grain Mill Digital Twin Simulator', browser-based, by Nell Watson) that would qualify for a WebApplication/SoftwareApplication schema, improving rich-result eligibility for a public launch.
  - _FIX:_ Add a JSON-LD block describing a WebApplication (name MillOS, applicationCategory, operatingSystem 'Any (Web)', author, url https://millos.net/, image og-image.png). Pure additive metadata, no runtime effect.
- **[LOW]** `index.html:5` · L12 Config · local · conf 0.6 · FLAG
  - **CSP lacks upgrade-insecure-requests / object-src / base-uri / form-action hardening** — The meta CSP (lines 5-13) has default-src/script-src/style-src/etc. but omits `object-src 'none'`, `base-uri 'self'`, `form-action 'self'`, and `upgrade-insecure-requests`. GH-Pages cannot send the security response headers (HSTS, X-Frame-Options) so the meta CSP is the only control surface; without object-src/base-uri the policy leaves plugin and <base>-injection vectors open, and without upgrade-insecure-requests any accidental http:// subresource is not auto-upgraded.
  - _FIX:_ Add `object-src 'none'; base-uri 'self'; form-action 'self'; upgrade-insecure-requests;` to the CSP. Also consider `frame-ancestors 'none'` since X-Frame-Options can't be set on GH-Pages.
- **[LOW]** `index.html:5-15` · L2 Security · local · conf 0.6 · FLAG
  - **CSP delivered only via meta tag (no enforcement of frame-ancestors/Report-Only/upgrade-insecure)** — The entire security policy ships as <meta http-equiv> tags (CSP line 5, X-Content-Type-Options line 14, referrer line 15). Meta-delivered CSP is parsed only after the parser reaches it and silently ignores frame-ancestors, sandbox, and report-uri/report-to directives. On GitHub Pages there is no way to add HTTP response headers, so X-Frame-Options, Strict-Transport-Security, and frame-ancestors are entirely absent. Combined with 'connect-src ws: wss:' wildcard, the policy is weaker than it appears.
  - _FIX:_ Document that GitHub Pages cannot serve security headers and decide whether to front millos.net with a header-injecting CDN (Cloudflare/Netlify) to add HSTS + frame-ancestors + a header-delivered CSP. If staying on GH Pages, keep the meta CSP but acknowledge frame-ancestors/HSTS gaps in the launch risk register. Product/infra decision.
- **[LOW]** `index.html:67` · L8 Visual · local · conf 0.6 · AUTO
  - **Loading screen relies on 'Inter' webfont before it loads; brief fallback flash on first paint** — index.html inline loading screen sets `font-family: 'Inter', sans-serif` (line 67) and renders immediately in <body> (lines 75-79) BEFORE the Google Fonts stylesheet (line 46, display=swap) has resolved. On first/cold load the loading text paints in the system sans fallback then swaps to Inter (FOUT). This is the documented tradeoff of display=swap (correct choice — avoids invisible text), so it is expected behavior, not a bug. The CSS font stack lacks an explicit metric-compatible fallback (e.g. no `system-ui` before sans-serif on the loading-text rule), making the swap slightly more noticeable than the app's @layer base stack which does include system-ui/-apple-system.
  - _FIX:_ Optionally add `system-ui, -apple-system` to the loading-text font stack (line 67) to match the base stack and minimize the swap jump. Cosmetic micro-optimization; safe to ship as-is.
- **[LOW]** `index.html:7` · L2 Security · local · conf 0.7 · FLAG
  - **script-src 'unsafe-inline' is broader than needed and is silently negated by the absence of nonces/hashes** — script-src is ''self' 'unsafe-inline' 'unsafe-eval' blob:'. 'unsafe-eval' is genuinely required (Rapier/Basis-transcoder WASM compilation via blob workers, troika). However 'unsafe-inline' permits arbitrary inline <script>, which is the classic XSS amplifier: any injected markup that reaches the DOM can execute. The only inline script-like content in index.html is the module loader <script type=module src> (external, not inline) and an inline <style> (covered by style-src, not script-src), so 'unsafe-inline' for scripts appears unnecessary for first-party code. Note: adding a nonce/hash would itself cause browsers to ignore 'unsafe-inline', so the current value provides no protection while widening the attack surface.
  - _FIX:_ Attempt removing 'unsafe-inline' from script-src and verify the build still runs (R3F/Vite emit external modules, not inline scripts). If a bundler-injected inline bootstrap exists, switch to a hashed/nonce'd inline script instead. Keep 'unsafe-eval' and 'blob:' (WASM requirement). Verify in a CSP-enforced browser before shipping → flag for owner since it touches runtime behavior.
- **[LOW]** `output/playwright/` · L11 DeadCode · local · conf 0.85 · AUTO
  - **Committed Playwright screenshot artifacts (3 PNGs, 984KB) at output** — git ls-files output lists three PNGs. Real Playwright output goes to test-results and playwright-report (gitignored).
  - _FIX:_ git rm -r output and add output to gitignore.
- **[LOW]** `package.json:38` · L2 Security · local · conf 0.6 · FLAG
  - **PeerJS 1.5.5 + default public signaling server: untrusted-peer trust boundary unhardened** — peerjs ^1.5.5 (package.json:38, resolved 1.5.5) connects via the default public PeerJS cloud server (src/multiplayer/SignalingService.ts:26-30) with predictable host peer IDs derived from the room code ('millos-${roomCode}', SignalingService.ts:53-55). Predictable IDs let any third party on the public broker impersonate or pre-claim a host ID / squat room codes, and the broad 'wss:' CSP (see separate finding) does nothing to constrain the broker. This is an inherent property of using the shared public broker with deterministic IDs, not a code bug, but it is a real multiplayer trust-boundary weakness for a feature that explicitly accepts untrusted peers.
  - _FIX:_ Add entropy to host peer IDs (append a random nonce shared out-of-band with the room code) so IDs are unguessable, and/or self-host peerjs-server (referenced at SignalingService.ts:28) with the connect-src locked to that host. Validate/authorize incoming DataConnections at the application layer before trusting peer-sent state. Product decision (changes multiplayer UX/join flow).
- **[LOW]** `package.json:4` · L12 Config · local · conf 0.85 · FLAG
  - **Version inconsistency: package.json 0.20.0 vs deployed v0.30 'current'** — package.json declares `"version": "0.20.0"` (line 4), but deploy.yml builds and serves v0.30 as the current/default version (`VERSION=v0.30 npm run build`, root redirects to /v0.30/). metadata.json carries no version. The shipped 'current' is v0.30 while the package manifest says 0.20.0 — version of record disagrees with what users see. Git tags are also inconsistent (v0.10.0, v0.2, v0.3 — mixed formats).
  - _FIX:_ Bump package.json version to 0.30.0 to match the deployed current build, and standardize tag format. Low-risk metadata change.
- **[LOW]** `perf-test-gpu-output.txt` · L11 DeadCode · local · conf 0.9 · AUTO
  - **Disposable perf-test output txt artifacts committed at repo root** — Five tracked text dumps clutter the root. Raw stdout captures from benchmark runs referenced nowhere.
  - _FIX:_ git rm the five perf-test txt files.
- **[LOW]** `public/sw.js:100` · L12 Config · local · conf 0.6 · FLAG
  - **SW calls skipWaiting()+clients.claim() unconditionally while update flow expects a waiting worker** — sw.js calls self.skipWaiting() on install (line 100) and self.clients.claim() on activate (line 125), so every new SW activates immediately and takes over open tabs mid-session. Meanwhile serviceWorkerRegistration.ts:61-75 wires an onupdatefound/onUpdate callback intended to notify the user of a pending update before activation. With unconditional skipWaiting the new SW can swap cached JS/CSS under a running session (chunk-load mismatches) and the onUpdate 'new version available' UX never gets a chance to prompt before takeover.
  - _FIX:_ Either remove skipWaiting()/clients.claim() and drive activation from the onUpdate prompt (postMessage SKIP_WAITING on user confirm), or accept auto-update but document that the onUpdate callback is informational only. This changes update UX timing, so it needs an owner decision.
- **[LOW]** `public/sw.js:142` · L12 Config · local · conf 0.6 · FLAG
  - **SW caches cross-origin DRACO decoder but CSP/connect-src do not list gstatic.com** — sw.js fetch handler explicitly allows cross-origin requests to 'gstatic.com/draco' (line 142) to cache the DRACO decoder. However index.html connect-src (line 11) lists fonts.gstatic.com but NOT www.gstatic.com (where the DRACO decoder is hosted). If the app loads the DRACO decoder from www.gstatic.com at runtime (DRACOLoader default), that fetch would be CSP-blocked, and the SW allowance is moot. Verify where DRACOLoader's decoderPath points; if it is the gstatic CDN, connect-src needs https://www.gstatic.com.
  - _FIX:_ If DRACO is loaded from gstatic CDN, add https://www.gstatic.com to connect-src; if DRACO is self-hosted under public/libs, the sw.js gstatic special-case is dead and can be removed. Confirm DRACOLoader.setDecoderPath before changing.
- **[LOW]** `public/sw.js:146,220-221` · L2 Security · local · conf 0.65 · AUTO
  - **SPA navigation fallback hard-codes absolute '/index.html', breaking under non-root BASE_URL deploys** — networkFirst navigation fallback does caches.match('/index.html') (line 220), an absolute root path. The app explicitly supports non-root base URLs (sw.js:27 precache uses './' relative paths, registration scope uses import.meta.env.BASE_URL, and prior deploys live at /v0.10/ and /v0.20/). Under a sub-path deployment the offline navigation fallback would miss the precached './index.html' (cached under the base-relative key) and return the 503 'Offline' response instead of the SPA shell. Also line 146 dev-guard 'url.includes("/@")' is broad and could accidentally bypass legitimate same-origin asset URLs containing '/@' (e.g. scoped paths).
  - _FIX:_ Replace the absolute '/index.html' fallback with a base-relative lookup, e.g. resolve against self.registration.scope or new URL('./index.html', self.location.href). Tighten the '/@' dev guard to '/@vite' / '/@react-refresh' specifics. Low risk; current production base is '/' so impact is latent.
- **[LOW]** `public/sw.js:28-34,90-92` · L9 Performance · local · conf 0.75 · AUTO
  - **Service worker precaches 1.6MB warehouse.hdr in the install step (blocks SW activation)** — PRECACHE_ASSETS (sw.js:28-34) includes './hdri/warehouse.hdr' (1.6MB per du). On install, `cache.addAll(PRECACHE_ASSETS)` (line 92) must fetch all entries before install resolves, so a 1.6MB HDRI delays SW readiness and competes with initial page-load network. The HDRI is only needed on medium+ quality presets, not all sessions.
  - _FIX:_ Drop './hdri/warehouse.hdr' from PRECACHE_ASSETS so it loads lazily via the normal MODEL_CACHE network path (sw.js:60) on first use; keep install precache to ./, index.html, and the font.
- **[LOW]** `public/v0.30/` · L12 Config · local · conf 0.8 · FLAG
  - **public/v0.30/ is stale cruft — contains only assets/, no index.html, and is not used by deploy** — public/v0.30/ tracks only 10 files under assets/ (worker PNGs), no index.html. deploy.yml builds v0.30 FRESH (`VERSION=v0.30 npm run build` -> copies dist) and never reads public/v0.30/. Unlike public/v0.20/ (330 tracked files, a real pre-built snapshot copied to staging) and public/v0.10/ (13 files, legacy snapshot copied to staging), public/v0.30/ is an incomplete leftover that ships into the dev build's public dir but serves no purpose. Confusing during the dev `serveStaticVersions` plugin era (which only matches v0.10|v0.20 anyway — vite.config.ts:13).
  - _FIX:_ Delete public/v0.30/ (it is regenerated by the deploy build). Confirm with owner the v0.20 snapshot is still intentionally shipped as a selectable legacy version before touching that.
- **[LOW]** `src/components/AICommandCenter.tsx:280` · L5 A11y · local · conf 0.8 · -
  - **AICommandCenter tab switcher uses plain buttons, not ARIA tab semantics** — The Decisions/Strategic tab switcher at lines 280-304 uses styled `<button>` elements toggling `activeTab` but lacks `role="tablist"`/`role="tab"`/`aria-selected`/`aria-controls`, and the content area (line 307) is not a `role="tabpanel"`. Compare WorkerDetailPanel.tsx:486-512 and SCADAPanel.tsx:546-635, which both implement full tab semantics. Functional but inconsistent; SR users don't get tab-position/selection feedback.
  - _FIX:_ Wrap the two buttons in `role="tablist"`, give each `role="tab"` + `aria-selected={activeTab===...}` + `aria-controls`, and mark the content area `role="tabpanel"` with a matching id, mirroring the existing WorkerDetailPanel pattern.
- **[LOW]** `src/components/AboutModal.tsx:30` · L8 Visual · local · conf 0.78 · FLAG
  - **Centered-modal backdrop opacity is inconsistent across modals (50/60/70)** — Three centered modals use three different backdrop darkness values for the same affordance: AboutModal.tsx:30 `bg-black/50`, KeyboardShortcutsModal.tsx:81 `bg-black/60` (dark) / `bg-black/40` (light), GeminiSettingsModal.tsx:182 `bg-black/70`. Opening different dialogs dims the scene by visibly different amounts, an inconsistency in the modal scrim. All three correctly share `z-[1000]` and backdrop-blur, so only the opacity diverges.
  - _FIX:_ Pick one canonical scrim (e.g. `bg-black/60 backdrop-blur-sm`) and apply it to all centered modals; ideally extract a shared <ModalBackdrop> or constant. Low-risk visual standardization.
- **[LOW]** `src/components/AlertSystem.tsx:13` · L7 Copy · local · conf 0.95 · AUTO
  - **Sample alert machineId 'mill-1.5' and 'sifter-0' are invalid machine IDs** — getSampleAlerts() uses `machineId: 'mill-1.5'` (line 13) for a 'Roller Mill #3 back online' message and `machineId: 'sifter-0'` (line 19) for a 'Plansifter #2' message. Neither id exists (valid: rm-101..106, sifter-a/b/c). Any UI logic keying off these machineIds will not match a real machine. Note 'Roller Mill #3' should map to rm-103, 'Plansifter #2' to sifter-b.
  - _FIX:_ Set `machineId: 'rm-103'` (line 13) and `machineId: 'sifter-b'` (line 19) to match the message text. Stays inside AlertSystem.tsx.
- **[LOW]** `src/components/AmbientDetails.tsx:4174` · L1 Correctness/Bugs · local · conf 0.55 · FLAG
  - **Roof-leak drip uses setState every animation frame (stale read, re-render churn)** — RoofLeakPuddle's animation callback (lines 4172-4193) calls `setDropY((prev) => ...)` every frame, contradicting this file's own stated design (line 915 comment: condensation is 'ref-based animation (no setState in useFrame)'). Per-frame setState forces a React re-render of the component each frame. Additionally `dropRef.current.position.y = dropY` (line 4183) reads the stale closure `dropY`, not the freshly computed `newY` returned from the updater, so the mesh visually lags the state by one animation tick. Throttled to every 3rd frame by the manager, so impact is modest, but it is the only per-frame setState in the centralized-animation file.
  - _FIX:_ Convert dropY to a useRef (e.g. dropYRef.current) updated/read inside the callback, matching the condensation-drip pattern, eliminating per-frame setState and the stale read. Visual/3D change — verify the drip still animates; prefer owner review.
- **[LOW]** `src/components/AmbientDetails.tsx:4533` · L1 Correctness/Bugs · local · conf 0.6 · -
  - **Door-toggle interval restarts on every door state change (timing churn)** — AmbientDetailsGroup's door interval useEffect (lines 4532-4556) lists `[doorStates]` as its dependency. Each door toggle calls `setDoorStates(...)`, which changes doorStates, which tears down and recreates the interval — re-rolling the random `15000 + Math.random()*30000` delay on every fire. Net effect: the 'periodic' door behavior never settles to its intended cadence; the timer is effectively reset each cycle. (Sound logic itself is correct: pre-flip `doorStates[doorId]===true` -> playDoorClose, ===false -> playDoorOpen, which matches the open/close transition.) Not a correctness defect in output, only timing/perf.
  - _FIX:_ Use a functional setState inside the callback to read current door state and drop `doorStates` from deps (use [] so the interval is created once), or read state from a ref. Local single-file change; low risk but alters door cadence slightly, so light owner awareness.
- **[LOW]** `src/components/App.tsx:1` · L8 Visual · local · conf 0.8 · FLAG
  - **Palette inconsistency: 69 stray gray-* utilities amid 3901 slate-* (slate is the established standard)** — Color-family scan across src (excluding archive): slate-* = 3901 usages, gray-* = 69, zinc/neutral/stone = 0. Slate is unambiguously the project palette. The 69 gray-* classes (in App.tsx, decisionIcons.tsx, SCADAPanel.tsx, WorkerSpeechBubble.tsx, WorkerDetailPanel.tsx, TruckScheduleWidget.tsx, PredictiveMaintenancePanel.tsx, BreakdownEffects.tsx, QCLabRoom.tsx and others) are slightly cooler/different from slate and create subtle visual seams between panels. Tailwind's gray and slate are visually distinct (slate is blue-tinted).
  - _FIX:_ Migrate the 69 gray-{n} classes to the equivalent slate-{n} (gray-400 -> slate-400, etc.) for palette consistency. Low-risk find-and-replace per-occurrence, but it is a visible color shift, so do it deliberately with visual spot-check rather than a blind global sed. Not auto-fixable without UX review.
- **[LOW]** `src/components/Environment.tsx:490` · L9 Performance - useFrame allocations · local · conf 0.85 · AUTO
  - **new THREE.Color allocated inside the consolidated lens/daylight useFrame** — Inside the consolidated useFrame (block '15. Update Daylight'), line 490 does `const THREEColor = new THREE.Color(color);` every time the block runs. It is throttled to every 6th frame (~10fps) so the impact is bounded, but it still allocates a THREE.Color on the render loop where the file already establishes the module-temp pattern (_cameraDir/_lightPos/_toCamera at lines 95-97). The value is then only used to feed `mat.color.set(THREEColor)`.
  - _FIX:_ Add a module-level `const _daylightColor = new THREE.Color();` near the existing _cameraDir temps, then replace line 490 with `_daylightColor.set(color);` and use `_daylightColor` in the forEach. Behavior identical; one fewer allocation per throttled tick.
- **[LOW]** `src/components/Environment.tsx:490` · L9 Performance · local · conf 0.7 · AUTO
  - **THREE.Color allocated inside useFrame daylight update** — `const THREEColor = new THREE.Color(color);` is allocated inside the useFrame daylight block (:490). Throttled via shouldRunThisFrame(6) (≈10fps at 60fps), so ~10 Color allocations/sec — minor GC churn, consistent with the per-frame-alloc class CLAUDE.md already flags for this file but at a low rate. Reported for completeness; not launch-blocking.
  - _FIX:_ Hoist a module-level `const _daylightColor = new THREE.Color();` and use `_daylightColor.set(color)` then `mat.color.copy(_daylightColor)`. Behavior-identical.
- **[LOW]** `src/components/Environment.tsx:670` · L9 Performance - useFrame allocations · local · conf 0.75 · FLAG
  - **getDaylightProperties returns a fresh object literal per call, invoked from 3 per-frame blocks** — getDaylightProperties (line 670) returns a new object literal on every branch (e.g. `return { color: '#1e3a5f', intensity: 0.1, opacity: 0.2 };`, seven such returns). It is called from 3 separate per-frame useFrame contexts in Environment.tsx (the daylight block at ~488, and two other `const { ... } = getDaylightProperties(gameTime)` call sites), each allocating a short-lived object every frame they run (some throttled, some not verified throttled). Minor but a repeated per-frame literal in the hot path.
  - _FIX:_ Either memoize the last result keyed on the rounded hour (the function is a pure step-function of gameTime), or have it write into a module-level reusable object the callers read. Low risk; values unchanged. Prefer flagging since it touches an exported helper used by multiple call sites.
- **[LOW]** `src/components/FactoryExterior.tsx:1321` · L1 Correctness/Bugs · local · conf 0.5 · AUTO
  - **AnimatedFrog heart key uses Date.now(), can collide on rapid pets** — `const id = Date.now();` (line 1321) is used as the React key/id for spawned HeartParticle elements (line 1420 `key={h.id}`). Two pet clicks within the same millisecond produce duplicate keys, triggering React key collision warnings and potentially one heart's onComplete removing the wrong entry. Purely cosmetic Easter-egg path (clicking the pond frog), so impact is trivial.
  - _FIX:_ Use a monotonic counter ref (`heartCounter.current++`) or `crypto.randomUUID()` for the heart id instead of Date.now().
- **[LOW]** `src/components/FactoryExterior.tsx:424-434` · L1 Correctness/Bugs · local · conf 0.55 · FLAG
  - **AnimatedRiverWater ignores later flowSpeed prop changes (useRef init-once)** — `const uniforms = useRef({ ... flowSpeed: { value: flowSpeed } });` (lines 428-434) initializes the shader uniform from the `flowSpeed` prop only once. If a caller later changes flowSpeed, `uniforms.current.flowSpeed.value` is never updated (no effect syncs it; useFrame at line 534 only updates `time`). Same latent staleness applies to StillCanalWater's analogous uniforms ref. Not a live bug today because all call sites pass static flowSpeed (e.g. line 1216 `flowSpeed={1.2}`), but it is a correctness trap if a caller ever animates flowSpeed.
  - _FIX:_ Add a small useEffect that syncs `uniforms.current.flowSpeed.value = flowSpeed` when the prop changes, or document that flowSpeed is mount-only. Low risk; keep behind owner sign-off since it touches shader visuals.
- **[LOW]** `src/components/ForkliftSystem.tsx:839` · L1 Correctness/Bugs · local · conf 0.5 · -
  - **Unguarded pathActions index access can throw if path/pathActions lengths diverge** — Line 839: `const action = data.pathActions[pathIndexRef.current];` followed by `action.type` reads at 842/849. pathIndexRef is bounded by `% data.path.length` (line 858), NOT by pathActions.length. The two hard-coded forklifts currently have matching lengths (8/8 and 6/6) so there is no live crash, but if path and pathActions ever diverge (e.g. a new route edited in one array only), action is undefined and `action.type` throws inside useFrame, killing the render loop. Defensive gap, not a live failure.
  - _FIX:_ Guard: `const action = data.pathActions[pathIndexRef.current]; if (!action) { pathIndexRef.current = (pathIndexRef.current + 1) % data.path.length; currentTarget.current.set(...data.path[pathIndexRef.current]); return; }` — or assert path.length === pathActions.length at construction.
- **[LOW]** `src/components/ForkliftSystem.tsx:874-878` · L1 Correctness/Bugs · local · conf 0.55 · FLAG
  - **Legacy forklift wheel refs go stale after a close→far→close LOD cycle (wheels stop spinning)** — Legacy (non-physics) path only. Lines 874-876: `if (wheelRefsRef.current.length === 0 && ref.current.children.length >= 12) { wheelRefsRef.current = ref.current.children.slice(8, 12) as THREE.Mesh[]; }`. The capture guard only fires while the array is empty. On a close→far→close LOD round-trip, ForkliftModel unmounts (replaced by ForkliftBillboard at distanceTier 'far') and remounts with NEW child meshes, but wheelRefsRef still holds the old (now-detached) meshes and the `length === 0` guard never re-fires, so the loop at 877-879 mutates detached meshes and the visible wheels stop animating. Visual-only and legacy-only.
  - _FIX:_ Reset wheelRefsRef.current to [] when distanceTier transitions back to 'close' (e.g. in a useEffect on distanceTier), or re-capture when the cached mesh is no longer parented to ref.current. Visual change — verify in-app.
- **[LOW]** `src/components/GeminiSettingsModal.tsx:148` · L6 UX · local · conf 0.72 · FLAG
  - **Save button validates+saves silently; no persistence-success feedback for the no-key path and no plaintext-storage acknowledgement** — handleSave (lines 148-166) shows a transient 'API key saved and connected!' toast then auto-closes after 1500ms (line 160). That is reasonable, but there's no indication the key was *persisted* (vs session-only), and the success toast disappears on auto-close before a user can register what happened. Combined with the missing plaintext disclosure, users get no durable confirmation of what was stored where.
  - _FIX:_ Keep a persistent 'Connected • <masked key> (saved in this browser)' status (the masked-key line at modal lines 234-239 already exists for the connected state — ensure it remains visible and mentions local persistence) rather than relying on the auto-dismissing toast.
- **[LOW]** `src/components/GeminiSettingsModal.tsx:174` · L8 Visual · local · conf 0.85 · FLAG
  - **GeminiSettingsModal exit animation never plays (early return before AnimatePresence)** — Same antipattern as KeyboardShortcutsModal: line 174 `if (!isOpen) return null;` sits *above* the `<AnimatePresence>` at line 177. The backdrop and dialog declare `exit={{ opacity: 0 }}` (181) and `exit={{ scale: 0.9, opacity: 0 }}` (187), but because the whole component returns null when isOpen is false, AnimatePresence has no child to animate out. Opens with fade/scale; closes instantly with no exit transition. The modal is also self-mounted (rendered unconditionally by AICommandCenter.tsx:371), so the exit is genuinely lost.
  - _FIX:_ Move the early return out: keep `<AnimatePresence>` always rendered and gate its child with `{isOpen && (<motion.div ...>...)}` so the exit animation can run, matching AboutModal.tsx.
- **[LOW]** `src/components/GeminiSettingsModal.tsx:177` · L8 Visual · local · conf 0.5 · FLAG
  - **Centered modals lack focus trap / Escape-to-close that the reference AboutModal has** — AboutModal.tsx:4,19 uses `useFocusTrap(modalRef, isOpen, onClose)` giving it a focus trap AND Escape-to-close. GeminiSettingsModal (closes only on backdrop click, line 183) and KeyboardShortcutsModal (closes only on backdrop click, line 78) have neither: keyboard focus can leave the dialog and Escape does not dismiss them. This is primarily a11y/interaction (may overlap another lane) but produces an inconsistent modal experience versus the established AboutModal pattern.
  - _FIX:_ Apply the existing `useFocusTrap` hook to GeminiSettingsModal and KeyboardShortcutsModal (passing modalRef + isOpen + onClose), matching AboutModal. Adds Escape-close and focus containment with no visual change.
- **[LOW]** `src/components/LoadingScreen.tsx:63` · L8 Visual · local · conf 0.6 · FLAG
  - **React LoadingScreen uses 🏭 emoji; CLAUDE.md sanctions the loading-screen emoji only in index.html** — Line 63 `<div style={{ fontSize: '60px', marginBottom: '16px' }}>🏭</div>`. CLAUDE.md allows the mill emoji in the 'Loading screen icon (index.html)'. This is a separate React component (src/components/LoadingScreen.tsx), not the index.html inline loading screen. Borderline: it IS a loading screen so it matches the intent of the exception, but the literal rule names index.html. Reporting at low severity / moderate confidence as a consistency note, not a hard violation.
  - _FIX:_ Acceptable to leave as-is (matches the loading-screen exception intent). If strict compliance is desired, replace with a Lucide Factory icon to match the rest of the UI. Owner decision; no functional impact.
- **[LOW]** `src/components/ProductionMetrics.tsx:278-294` · L8 Visual · local · conf 0.55 · FLAG
  - **Throughput AreaChart has no empty/initial-data state (renders blank area on first paint)** — The AreaChart at lines 279-293 binds `data={data}` where `data` is `useState<MetricsData[]>([])` (line 118) and is only populated by a mount effect (lines 200-214). On first render the chart area is empty with no placeholder, so the 'Production (30m)' card briefly shows a blank chart body under the 'Live' label. Unlike SCADAPanel which has an explicit empty state (SCADAPanel.tsx:984-989), this mini-chart shows nothing. Brief but visible flicker on panel open.
  - _FIX:_ Render a subtle placeholder (e.g. a faint baseline or 'Collecting data…') when `data.length === 0`, or seed the first data point synchronously. Low risk; verify it doesn't disturb the sr-only data table.
- **[LOW]** `src/components/SCADAPanel.tsx:1006-1013` · L8 Visual · local · conf 0.4 · FLAG
  - **SCADA trend Tooltip sets dark contentStyle but no label text color (low-contrast label row)** — The recharts <Tooltip contentStyle={{ backgroundColor: '#1e293b', border:'1px solid #334155', borderRadius:'8px', fontSize:'12px' }}> sets a dark surface but no text/label color. Recharts colors the per-series *item* rows with the line stroke (cyan/green/etc, visible), but the *label* row (the formatted timestamp) uses recharts' default dark label color, which is low-contrast on #1e293b. Cannot confirm exact rendered contrast without running the app; flagging as a readability risk on the tooltip label only, not the whole tooltip.
  - _FIX:_ Add `labelStyle={{ color: '#e2e8f0' }}` (and optionally `itemStyle={{ color: '#cbd5e1' }}`) to the Tooltip so the timestamp label is legible on the dark background. Verify visually after change.
- **[LOW]** `src/components/SCADAPanel.tsx:156` · L8 Visual · local · conf 0.45 · FLAG
  - **Trend line palette pairs deuteranopia-confusable green/red series** — `const TREND_COLORS = ['#06b6d4', '#10b981', '#f59e0b', '#ef4444', '#8b5cf6', '#ec4899'];` includes green (#10b981, idx 1) and red (#ef4444, idx 3), a classic red-green confusion pair for deuteranopia/protanopia. Mitigating factors: they are separated by amber (idx 2), and each line carries a text legend entry (Legend formatter at SCADAPanel.tsx:1021), so identification does not rely on color alone. Minor.
  - _FIX:_ Optional: swap to a color-blind-safe categorical palette (e.g. Okabe-Ito) for the 6 trend series. Low priority given legend labels already disambiguate.
- **[LOW]** `src/components/TruckBay.tsx:2683-2796` · L1 Correctness/Bugs · local · conf 0.92 · -
  - **Shipping dock-status update nested inside `if (receivingTruckRef.current)` block** — The shipping truck's HolographicDisplay status update (lines 2775-2795, `const shippingCycle = adjustedTime % CYCLE_LENGTH; ... updateDockStatus('shipping', ...)`) is mis-nested: brace-depth analysis confirms it sits inside the `if (receivingTruckRef.current) {` block opened at line 2683 and closed at line 2796. So shipping dock status only updates when the receiving truck ref is non-null. In practice this is a no-op because both `<group ref={shippingTruckRef}>` and `<group ref={receivingTruckRef}>` are rendered as a pair and attach together, so the receiving ref is always present when shipping status needs updating. Still a genuine logic-locality bug: the shipping-status block is unrelated to the receiving truck and belongs at the useFrame top level.
  - _FIX:_ Move the shipping-status computation/updateDockStatus block (lines 2775-2795) out of the `if (receivingTruckRef.current)` block to sit at the useFrame body level alongside the receiving-status block, mirroring the symmetric structure.
- **[LOW]** `src/components/TruckBay.tsx:3116,3536,1016` · L1 Correctness/Bugs · local · conf 0.6 · AUTO
  - **Inline workAreaBounds object literal churns WarehouseWorker registration effect** — At line 3116 `workAreaBounds={{ minX: -8, maxX: 8, minZ: -5, maxZ: 8 }}` (and line 3536) passes a fresh object literal each render. WarehouseWorkerWithPalletJack registers the worker in an effect with `}, [workerId, isActive, workAreaBounds]);` (line 1016), so the new object identity re-runs the effect (unregisterWorker + registerWorker) on every parent re-render. Same low-but-real churn class as the getTruckState finding. Additionally `isActive={shippingDoorsOpenRef.current}` (line 3115) reads a useFrame-mutated ref in render, so the worker's active state does not reactively update (related to the dock-equipment staleness finding).
  - _FIX:_ Hoist the workAreaBounds literal to a module-level const or wrap in useMemo so identity is stable across renders; this removes the registration churn without behavior change.
- **[LOW]** `src/components/TruckBay.tsx:3206,3580,3658` · L1 Correctness/Bugs · local · conf 0.6 · FLAG
  - **Inline getTruckState arrow defeats RealisticTruck registration effect, causing register/unregister churn** — At line 3206 `getTruckState={() => getShippingState(performance.now() / 1000)}` (and line 3580 for receiving) a new arrow function is created on every TruckBay render. RealisticTruck's registration effect lists it as a dependency: line 3658 `}, [truckId, getTruckState]);`. Since RealisticTruck is a plain React.FC (not memoized) and getTruckState identity changes each render, the effect tears down (unregisterTruckComponents + clearTimeout) and re-registers (new setTimeout(checkAndRegister, 0)) on every parent re-render. Impact is bounded because TruckBay re-renders are infrequent (user-driven productionSpeed slider / isTabVisible / graphicsQuality), but it is wasteful churn of the truck animation registry and can briefly drop the truck from the animation manager during the 0ms timeout window.
  - _FIX:_ Wrap getShippingState/getReceivingState (or the inline arrows passed at 3206/3580) in useCallback so identity is stable, OR remove getTruckState from the effect dependency array at line 3658 since it is only used to register a stable accessor. Verify visually that trucks still animate.
- **[LOW]** `src/components/UIOverlay.tsx.original` · L11 DeadCode · local · conf 0.95 · AUTO
  - **Tracked editor backup file UIOverlay.tsx.original (99KB) is dead** — git ls-files lists src/components/UIOverlay.tsx.original (101349 bytes). The original extension excludes it from lint and prettier so it sits inert beside the live UIOverlay.tsx. Nothing imports it.
  - _FIX:_ git rm src/components/UIOverlay.tsx.original.
- **[LOW]** `src/components/UIOverlay.tsx.original:1` · L8 Visual · local · conf 0.9 · AUTO/FLAG
  - **Stale duplicate UIOverlay.tsx.original (2194 lines) shipped in src/ (dead-code, out of L8 scope)** — `src/components/UIOverlay.tsx.original` is a 2194-line near-duplicate of the live UIOverlay.tsx (contains the old CollapsibleLegend at line 1453+ etc). It is not imported anywhere and bloats the source tree. Flagging here because it surfaced during the visual grep; the actual fix belongs to the dead-code lane.
  - _FIX:_ Delete src/components/UIOverlay.tsx.original (dead-code lane). Confirm no tooling references the .original extension before removal.
- **[LOW]** `src/components/UIOverlay.tsx:1500` · L5 A11y · local · conf 0.7 · AUTO
  - **Version-switcher <select> has no programmatic label (name via title only)** — The version dropdown inside the <h1> (lines 1500-1511) has only a `title="Switch version"` and no `<label>` or `aria-label`. `title` is a valid name source but is unreliable on touch devices and some screen readers; it also navigates the whole page on change (`window.location.href`) with no warning. Nested interactive control inside an h1 is also unusual for heading semantics.
  - _FIX:_ Add `aria-label="Switch app version"` to the <select>. (Optional, owner call: move it out of the <h1> so the heading text isn't an interactive container.)
- **[LOW]** `src/components/UIOverlay.tsx:1993` · L6 UX · local · conf 0.6 · FLAG
  - **FiveAxesPanel (z-40, top-20 right-4, w-80) can overlap the forklift info panel (z-50, top-20 right-4)** — UIOverlay.tsx:1993 places FiveAxesPanel at `fixed top-20 right-4 z-40 ... w-80`. App.tsx:610 renders the selected-forklift panel at `fixed right-4 top-20 ... z-50 min-w-64`. Both anchor to the exact same top-right corner; when a forklift is selected while the axes panel is open, they stack on the same pixels (forklift wins on z, but the axes panel is partially occluded with no offset). Not a crash, but a visible overlap on smaller viewports.
  - _FIX:_ Offset one panel vertically (e.g. give the forklift panel a different top, or collapse/hide FiveAxesPanel when a forklift is selected). Confirm visually since 3D-adjacent overlays aren't test-covered.
- **[LOW]** `src/components/UIOverlay.tsx:2043` · L5 A11y · local · conf 0.5 · FLAG
  - **MachineInfoPanel popup lacks dialog role / labelled region** — The machine info popup (motion.div at 2043) is a focused contextual panel with a heading (`<h2>` at 2062) and a close button, but it carries no `role="dialog"`/`aria-labelledby` and is not announced as a distinct region when opened. Unlike WorkerDetailPanel (role="dialog" + focus trap at 426/314), this panel is not clearly modal — adding role="dialog"/aria-modal would also imply a focus-trap obligation. Flagging for an owner decision rather than asserting a hard violation; the close-name and status-color findings above stand independently.
  - _FIX:_ Owner decision: either (a) treat as non-modal and add `role="region" aria-labelledby` pointing at the h2 (give the h2 an id), or (b) make it a proper dialog with role="dialog"/aria-modal + focus trap mirroring WorkerDetailPanel. Do not add dialog role without the focus-trap.
- **[LOW]** `src/components/VillageArea.tsx:1714` · L9 Performance · local · conf 0.45 · FLAG
  - **Module-level villageCobbleMaterial uses onBeforeCompile without a customProgramCacheKey** — `villageCobbleMaterial.onBeforeCompile = (shader) => { ... }` injects custom vertex/fragment chunks but no `villageCobbleMaterial.customProgramCacheKey` is set. Three.js's documented severe variant of this bug (Date.now() in the key) is already fixed in TerrainMaterial; here the risk is the inverse — a default cache key means this shader-modified material could share a compiled program with another MeshStandardMaterial of identical parameters and render without the feathering injection. No observed collision in this scope (it is a single module-level singleton used once at :1750), so this is precautionary, not a reproduced defect.
  - _FIX:_ Add a stable explicit key: `villageCobbleMaterial.customProgramCacheKey = () => 'villageCobble_feather_v1';` to guarantee the injected variant gets its own compiled program. No runtime UX change.
- **[LOW]** `src/components/WorkerDetailPanel.tsx:426-443` · L8 Visual · local · conf 0.6 · FLAG
  - **Non-embedded WorkerDetailPanel claims aria-modal but is a bare floating panel with a literal × close glyph (dead path)** — In non-embedded mode the panel sets `role="dialog" aria-modal={!embedded}` (true) yet renders as `absolute bottom-6 left-6 w-96` with NO backdrop overlay and no focus trap, so aria-modal=true misrepresents a non-blocking floating panel. The close button uses a literal `×` character (line 443) instead of a Lucide `X` icon (CLAUDE.md mandates Lucide). NOTE: every shipping call site passes `embedded={true}` (ContextSidebar.tsx via the minified `n` alias), so this non-embedded branch is effectively dead — severity kept low.
  - _FIX:_ Since the path is unused, either delete the non-embedded branch (dead-code) or, if kept, drop aria-modal for the floating variant and replace `×` with `<X className="w-4 h-4" />`. Confirm with owner whether non-embedded mode is intended to ship.
- **[LOW]** `src/components/index.ts:1-90` · L11 DeadCode · ripple · conf 0.9 · AUTO
  - **components/index.ts barrel has zero importers — and is the only thing (besides a test) keeping orphaned UIOverlay 'reachable'** — No file imports the components barrel; App.tsx imports every component by direct path (App.tsx:7 './components/MillScene', :10 './components/ui-new/GameInterface', :11 './components/SpatialAudioTracker', etc.). The barrel re-exports `UIOverlay` at line 38 (`export { UIOverlay } from './UIOverlay'`), which is no longer rendered anywhere in the live app. Deleting the barrel is safe; classified ripple only because UIOverlay's dead-status (next finding) is the real coupled item.
  - _FIX:_ Delete src/components/index.ts (zero runtime impact). Confirmed no test imports it.
- **[LOW]** `src/components/machines/UIComponents.tsx:43` · L1 Correctness/Bugs · local · conf 0.45 · AUTO
  - **Machine panel re-registers under a fresh random id on every status change** — Lines 41-55: the registration effect depends on [enabled, status] and computes `const id = `panel-${Math.random()}`` inside the effect body. On each status transition (running→warning→critical, which is routine), the effect tears down (unregisterPanel(prevId)) and re-registers under a brand-new random id, churning panelRegistry entries. Registration is also conditional (`if (screenMaterial.current)`) while cleanup is unconditional, so an unregister can run for an id that was never registered (harmless no-op). No leak because each effect run closes over its own id, but the random-id-per-status pattern is fragile and makes the registry key non-deterministic.
  - _FIX:_ Generate a stable id once with useId() or a useRef, e.g. `const idRef = useRef(`panel-${useId()}`)`, and use idRef.current in both register and unregister so the same panel keeps one identity across status changes.
- **[LOW]** `src/components/mobile/MobileFirstPersonController.tsx:215` · L9 Performance · local · conf 0.7 · AUTO
  - **Two THREE.Vector3 allocated per frame inside useFrame (mobile movement)** — Inside the useFrame body: `const forward = new THREE.Vector3(0, 0, -1).applyQuaternion(camera.quaternion);` and `const right = new THREE.Vector3(1, 0, 0).applyQuaternion(camera.quaternion);` (lines 215-216). These run every frame the player is moving on mobile, allocating 2 Vector3 per frame → GC pressure on the lowest-powered target devices. Gated behind movement (early return at :206 when direction length is 0), so impact is bounded to active-movement frames — minor, not launch-blocking.
  - _FIX:_ Hoist to module-level reusable vectors (the pattern already used elsewhere in this codebase per CLAUDE.md, e.g. SmartForklift/Environment fixes): `const _forward = new THREE.Vector3(); const _right = new THREE.Vector3();` then `_forward.set(0,0,-1).applyQuaternion(camera.quaternion); _right.set(1,0,0).applyQuaternion(camera.quaternion);` Behavior-identical.
- **[LOW]** `src/components/mobile/MobilePanel.tsx:893` · L8 Visual · local · conf 0.6 · AUTO
  - **Mobile panel border color differs from desktop sidebar (border-slate-700/50 vs border-white/10)** — MobilePanel uses 'bg-slate-900/95 backdrop-blur-xl border border-slate-700/50 rounded-2xl' (line 893) while the desktop ContextSidebar uses the same bg/blur/radius but 'border border-white/10' (ContextSidebar.tsx:251). Same panel family, two different border tokens, so the mobile and desktop panel chrome read slightly differently. Same inconsistency family as the floating-panel drift above.
  - _FIX:_ Use border-white/10 on MobilePanel to match the canonical panel border, or define one shared panel-chrome class consumed by both.
- **[LOW]** `src/components/mobile/MobilePanel.tsx:910` · L8 Visual · local · conf 0.45 · -
  - **Mobile panel content maxHeight uses a hardcoded 56px header subtraction that can mismatch actual header height** — The panel is capped at maxHeight: '33vh' (line 888) and the scrollable content area is maxHeight: 'calc(33vh - 56px)' (line 910). The header is 'px-4 py-3' (24px vertical padding) plus icon/text line-height plus a 1px bottom border, so 56px is an estimate. If the header text wraps on a narrow device or the icon/font scales, the subtraction is too small and the content's bottom is clipped or the panel exceeds 33vh. Magic-number coupling between two siblings rather than a measured/flex layout.
  - _FIX:_ Use a flex column (header flex-none, content flex-1 min-h-0 overflow-y-auto) inside the maxHeight:33vh container instead of subtracting a hardcoded 56px, so the content height tracks the real header height.
- **[LOW]** `src/components/mobile/RotateDeviceOverlay.tsx:17` · L8 Visual · local · conf 0.3 · AUTO
  - **RotateDeviceOverlay (z-[100]) sits below modals (z-[1000]); a modal open during rotation would obscure the rotate prompt** — RotateDeviceOverlay uses 'fixed inset-0 z-[100]' (line 17), but KeyboardShortcutsModal (z-[1000], KeyboardShortcutsModal.tsx:77) and DecisionReplay (z-[1000], DecisionReplay.tsx:75) sit above it. If a full-screen modal is open and the device rotates to an unsupported orientation, the 'please rotate' overlay renders behind the modal and the user sees a stale modal instead of the rotate prompt. Low co-occurrence (these modals are desktop-oriented), hence low confidence.
  - _FIX:_ Raise RotateDeviceOverlay to a z-index above all app modals (e.g. z-[2000]) since it is a hard blocking state that should supersede everything.
- **[LOW]** `src/components/mobile/RotateDeviceOverlay.tsx:48` · L8 Visual · local · conf 0.9 · FLAG
  - **Mill emoji used outside the three CLAUDE.md-sanctioned branding spots** — Line 48 `<span className="text-lg">🏭</span>` renders the mill emoji as a branding mark in the mobile rotate-device overlay footer ('MillOS Digital Twin'). CLAUDE.md permits 🏭 ONLY in: favicon (index.html), loading screen icon (index.html), and the top-left header logo (UIOverlay.tsx). This overlay is none of those. It is branding (spirit-aligned) but violates the literal rule and is inconsistent with the no-emoji-in-UI policy that mandates Lucide icons elsewhere (the same file already imports RotateCcw from lucide-react).
  - _FIX:_ Replace the 🏭 span with a Lucide icon (e.g. `import { Factory } from 'lucide-react';` then `<Factory className="w-4 h-4" />`), matching the no-emoji-use-icons rule. Pure local change, no behavior impact, but it is a visual swap so flag for owner aesthetic approval.
- **[LOW]** `src/components/multiplayer/MultiplayerLobby.tsx:166-178` · L3 Privacy/GDPR — Multiplayer data sharing · local · conf 0.85 · FLAG
  - **Player-name entry has no data-sharing disclosure before broadcasting the name to peers and broker** — The name field collects free-text (up to 20 chars) and on Create/Join it is immediately stored and broadcast: `<input ... value={playerName} onChange={(e) => setPlayerName(e.target.value)} placeholder="Enter your name" maxLength={20} ... />` with no adjacent notice. handleCreateRoom/handleJoinRoom call setLocalPlayerName(playerName.trim()) and connect. Users may enter their real name without being told it is shared with untrusted peers (and sent as broker metadata). This is a transparency/data-minimization gap; the field also accepts arbitrary PII a user might volunteer.
  - _FIX:_ Owner decision (UX copy): add a short helper line under the name input noting the name is visible to other players in the room. Optionally suggest a nickname. No runtime-default behavior change required for the copy itself.
- **[LOW]** `src/components/multiplayer/MultiplayerLobby.tsx:63` · L2 Security · local · conf 0.85 · FLAG
  - **Local player name & room code entered without sanitizePlayerName/sanitizeRoomCode** — Name entry uses only `.trim()`: `setLocalPlayerName(playerName.trim())` and `manager.hostRoom(playerName.trim())` (lines 63-65), `manager.joinRoom(joinCode.trim().toUpperCase(), playerName.trim())` (line 89). Same pattern in MultiplayerPanel.tsx:94-96,118-120. sanitizePlayerName() (max 20, charset-restricted) and sanitizeRoomCode() (sanitize.ts:203, exactly 6 uppercase alnum) exist and are exported but unused at these entry points. Names of arbitrary length/charset enter the store and get broadcast to peers.
  - _FIX:_ Replace `.trim()` with `sanitizePlayerName(playerName)` at the setLocalPlayerName/host/join call sites in both MultiplayerLobby.tsx and MultiplayerPanel.tsx, and gate join with `const code = sanitizeRoomCode(joinCode); if (!code) return;`. Import from '@/utils'. Two-file change but each edit is intra-file.
- **[LOW]** `src/components/truckbay/test-import.ts:1-3` · L11 DeadCode · local · conf 0.95 · AUTO
  - **truckbay/test-import.ts is a 2-line scratch re-export with zero consumers** — Full contents: `import * as BufferGeometryUtils from 'three/examples/jsm/utils/BufferGeometryUtils.js'; export { BufferGeometryUtils };`. Grep for 'test-import' / imports of '.../truckbay/test-import' returns nothing — no file consumes it. The filename ('test-import') and content signal a one-off import-resolution check left behind. Git-tracked.
  - _FIX:_ Delete src/components/truckbay/test-import.ts. If BufferGeometryUtils is needed, callers should import it directly from three/examples.
- **[LOW]** `src/components/truckbay/useTruckPhysics.ts:1` · L9 Performance - useFrame allocations · local · conf 0.8 · FLAG
  - **Truck-state functions allocate large object literals every frame (called 4x/frame)** — calculateShippingTruckState / calculateReceivingTruckState each build ~6 inline waypoint object literals (e.g. `const TUNNEL = { x: 20, z: 270, rot: Math.PI };`, APPROACH, SETUP, DOCK, PULLOUT_END, EXIT_ROAD) plus a large ~14-field return object literal (`return { phase: 'entering', x: ..., z: ..., rotation: ..., speed: ..., steeringAngle: 0, brakeLights: ..., reverseLights: false, ... }`) on EVERY invocation. They are pure functions called once per frame from each of: TruckBay.tsx main shipping useFrame (~2608), TruckBay.tsx receiving useFrame, and FactoryExterior.tsx checkpoint useFrame (~4193-4194 calls BOTH). With 2 checkpoints that is up to 4-6 calls/frame, each allocating ~7 objects = sustained per-frame GC pressure (the documented anti-pattern this lane targets). The waypoint constants are frame-invariant and could hoist to module scope; the return shape could reuse a module-level mutable state object the callers read synchronously.
  - _FIX:_ Hoist the frame-invariant waypoint literals (TUNNEL/APPROACH/SETUP/DOCK/PULLOUT_END/EXIT_ROAD and the per-style dimension tables) to module-level const. Optionally have each function mutate-and-return a single module-level TruckAnimState object instead of a fresh literal, since callers consume it synchronously within the same frame and do not retain it. Verify visually that truck animation is unchanged.
- **[LOW]** `src/components/ui-new/dock/Dock.tsx:108` · L8 Visual · local · conf 0.4 · AUTO
  - **Dock (z-50) and MobilePanel (z-50) share the same z-index, relying on DOM order to layer** — The Dock is 'z-50' (line 108) and the mobile slide-up panel is 'z-50' (MobilePanel.tsx:885) with its backdrop at 'z-40' (line 875). Two interactive fixed elements at the same z-index resolve stacking by DOM source order rather than explicit intent; the MobilePanel bottom is positioned above the dock via 'bottom: max(100px, ...+90px)' (line 887), so they currently do not overlap, but the equal z-index is fragile if either offset changes.
  - _FIX:_ Give the dock and the mobile panel distinct, intentional z-index values (e.g. dock z-50, mobile panel z-[60]) so layering is explicit rather than order-dependent.
- **[LOW]** `src/components/ui-new/dock/Dock.tsx:202` · L8 Visual · local · conf 0.5 · AUTO
  - **Dock lucide icons have no explicit size class - DockItem icons vs trailing-button icons may render at different sizes** — DockItems pass bare icons (e.g. <Factory />, <Brain /> at lines 125-186) and the trailing buttons also pass bare icons (<Database /> line 202, <Eye /> line 220, <Maximize/Minimize/> line 241), none with an explicit w-/h- class - they all rely on lucide's default 24px. Elsewhere in the codebase icons are explicitly sized (e.g. MobilePanel getPanelIcon uses 'w-5 h-5'). Relying on the default means any future lucide default change, or wrapping that constrains the SVG, silently changes dock icon scale; it also makes the dock icon size implicit and inconsistent with the explicit-sizing convention used in sibling components.
  - _FIX:_ Add an explicit size to the dock icons (e.g. size={20} or className 'w-5 h-5') for consistency with the rest of the UI and to lock the dock's visual scale.
- **[LOW]** `src/components/ui-new/panels/OverviewPanel.tsx:58` · L5 A11y · local · conf 0.8 · AUTO
  - **Game-speed button group lacks selected-state semantics; Pause button is icon-only with only title** — The speed controls (Pause / 1x / 10x / 60x, lines 59-105) form a mutually-exclusive selection (`gameSpeed === 0/180/1800/10800` toggles active styling) but no button exposes `aria-pressed` or radio semantics, so screen readers can't tell which speed is active. Additionally the Pause button (lines 59-69) is icon-only (`<Pause className="w-3.5 h-3.5" />`) relying solely on `title="Pause"` for its name; `title` is an unreliable accessible name. (The 1x/10x/60x buttons have visible text, so only Pause lacks a robust name.)
  - _FIX:_ Add `aria-label="Pause"` to the Pause button (line 59) and `aria-pressed={gameSpeed === N}` to all four speed buttons (or wrap as `role="radiogroup"` with `role="radio" aria-checked` like SettingsPanel.tsx:255). aria-pressed/label is SR-only; the radiogroup conversion is semantic.
- **[LOW]** `src/components/ui-new/panels/SafetyPanel.tsx:55` · L8 Visual · local · conf 0.45 · -
  - **Minor section-heading color drift across widgets (text-slate-400 vs text-slate-500, one tracking-wide outlier)** — The established section-heading convention is 'text-xs font-bold ... uppercase tracking-wider text-slate-400' (SafetyPanel.tsx:55,126,173). Across ui-new widgets/panels the survey shows the heading muted color split between text-slate-400 (12 uses) and text-slate-500 (10 uses), and one heading uses tracking-wide instead of tracking-wider. Small but visible inconsistency in the muted-heading shade and letter-spacing between adjacent panels.
  - _FIX:_ Pick one muted heading color (text-slate-400) and one tracking value (tracking-wider) and apply consistently, ideally via a shared SectionHeading component or class.
- **[LOW]** `src/components/ui-new/widgets/ManagementStylePanel.tsx` · L11 DeadCode · local · conf 0.85 · FLAG
  - **ui-new/widgets/ManagementStylePanel.tsx is a half-finished-migration orphan — zero references anywhere** — Repo-wide grep for 'ManagementStylePanel' and 'ManagementStyle' (excluding its own file and Archive) returns ZERO hits. It is not imported by ContextSidebar.tsx, not registered in sidebar/panelPreloader.ts (which uses string-keyed lazy `import()`s), and not in any test. Built as part of the ui-new migration but never wired into GameInterface.
  - _FIX:_ Delete src/components/ui-new/widgets/ManagementStylePanel.tsx, or wire it into ContextSidebar/panelPreloader if it was meant to ship. Owner decides intended-but-unwired vs abandoned.
- **[LOW]** `src/components/ui-new/widgets/PreferenceRequestWidget.tsx` · L11 DeadCode · local · conf 0.82 · FLAG
  - **ui-new/widgets/PreferenceRequestWidget.tsx component is orphaned — its backing data/store IS used, the widget is not** — Grep for 'PreferenceRequest' shows the type/store machinery is alive (types.ts:332/376, workerMoodStore.ts createPreferenceRequest/grant/deny/triggerRandomPreferenceRequest, aiEngine.ts:4018/4035/4052, WorkerMoodOverlay.tsx:473-515), but the WIDGET COMPONENT (PreferenceRequestWidget.tsx) has zero importers: no hit references the component file outside itself. The preference-request feature renders via WorkerMoodOverlay, not this widget. Not in panelPreloader's string-keyed lazy imports.
  - _FIX:_ Delete src/components/ui-new/widgets/PreferenceRequestWidget.tsx if the feature is surfaced via WorkerMoodOverlay, or wire it into the sidebar if it was meant to be the canonical UI. Do NOT touch the PreferenceRequest type/store — those are actively used.
- **[LOW]** `src/components/ui-new/widgets/VotingPanel.tsx:537` · L5 A11y · local · conf 0.85 · -
  - **Active/History tab toggle uses plain buttons without tab semantics** — The Active/History switcher is a `<div className="flex rounded bg-slate-700/30 p-0.5">` (line 537) containing two `<button>`s (lines 538-557) styled by `activeTab === 'active'/'history'` but with no `role="tablist"` on the container, no `role="tab"` / `aria-selected` on the buttons, and the content region below (`activeTab === 'active' ? ...`, line 562) has no `role="tabpanel"`. Screen readers cannot perceive selected state or the tab relationship. Lower severity as this is a secondary in-widget toggle, not primary navigation.
  - _FIX:_ Add `role="tablist"` to the wrapper (line 537), `role="tab" aria-selected={activeTab==='active'}` (and 'history') to each button, and `role="tabpanel"` to the list container at line 561. No visual change but it alters semantics, so verify against tests.
- **[LOW]** `src/components/ui/AlertAcknowledgmentFlow.tsx` · L11 DeadCode · local · conf 0.85 · FLAG
  - **ui/AlertAcknowledgmentFlow.tsx is orphaned — referenced only by the dead ui/index.ts barrel** — Repo-wide grep for 'AlertAcknowledgmentFlow' (excluding its own file and Archive) returns exactly one hit: src/components/ui/index.ts. No component, store, test, or dynamic import references it. Since the ui/index.ts barrel itself has zero importers, this panel is fully dead.
  - _FIX:_ Remove the AlertAcknowledgmentFlow export from ui/index.ts and delete src/components/ui/AlertAcknowledgmentFlow.tsx. Confirm via grep no string-keyed/lazy loader references it (none found).
- **[LOW]** `src/components/ui/CollapsibleLegend.tsx:87` · L5 A11y · local · conf 0.8 · FLAG
  - **Legend drag handle is a non-keyboard div; toggle lacks aria-expanded** — The reposition handle `<div onMouseDown={handleMouseDown} className="... cursor-grab ..." title="Drag to move">` (lines 87-97) is a div with only a mouse handler — no role/tabIndex/keyboard alternative, so keyboard users cannot move the legend (WCAG 2.1.1). The expand/collapse button (lines 98-121) also has no aria-expanded. Lower severity: panel is desktop-only (`hidden md:block`, line 79) and repositioning is non-essential.
  - _FIX:_ Add aria-expanded={expanded} to the toggle button. For the drag handle, either provide a keyboard reposition affordance or mark it aria-hidden with an accessible note that dragging is mouse-only; at minimum it should not be a focus dead-end.
- **[LOW]** `src/components/ui/CostEstimationOverlay.tsx:61` · L8 Visual · local · conf 0.5 · FLAG
  - **Floating-panel chrome drift: rounded-xl + colored borders vs design-system rounded-2xl + border-white/10** — The design-system surfaces (Dock.tsx:108, ContextSidebar.tsx:251, ShiftHandoverSummary.tsx:193) use 'border border-white/10 rounded-2xl'. The HUD floating panels diverge on corner radius and border treatment: CostEstimationOverlay.tsx:61 'rounded-xl border border-cyan-500/30', EnergyDashboard.tsx:127 'rounded-xl border border-emerald-500/30', MultiObjectiveDashboard.tsx:130 'rounded-xl border border-purple-500/30'. The colored accent borders may be intentional semantic color-coding, but the corner-radius mismatch (rounded-xl vs rounded-2xl) and white-vs-colored split are visible inconsistencies when these float alongside the sidebar/dock.
  - _FIX:_ Standardize corner radius to rounded-2xl across all floating glass panels to match Dock/Sidebar. If the accent border colors are deliberate, keep them but align radius and border-opacity; otherwise unify on border-white/10.
- **[LOW]** `src/components/ui/EmergencyControlPanel.tsx` · L11 DeadCode · local · conf 0.85 · FLAG
  - **ui/EmergencyControlPanel.tsx is orphaned — referenced only by the dead ui/index.ts barrel** — Grep for 'EmergencyControlPanel' (excluding own file and Archive) yields only src/components/ui/index.ts. Distinct from EmergencyStopButton (which UIOverlay imports). No live consumer; the lone barrel reference is itself unimported.
  - _FIX:_ Remove the EmergencyControlPanel export from ui/index.ts and delete src/components/ui/EmergencyControlPanel.tsx after confirming no dynamic/string reference.
- **[LOW]** `src/components/ui/GraphicsSettingsPanel.tsx` · L11 DeadCode · local · conf 0.82 · FLAG
  - **ui/GraphicsSettingsPanel.tsx is orphaned — referenced only by the dead ui/index.ts barrel (note: ui-new has SettingsPanel)** — Grep for 'GraphicsSettingsPanel' (excluding own file and Archive) returns only src/components/ui/index.ts. Graphics settings in the live app are handled by the ui-new SettingsPanel; this old-gen panel is unreferenced. Flagged for owner because graphics/quality UI is product-facing and the owner should confirm ui-new fully covers it before deletion.
  - _FIX:_ Remove the GraphicsSettingsPanel export from ui/index.ts and delete the file, after the owner confirms ui-new/panels/SettingsPanel supersedes it.
- **[LOW]** `src/components/ui/GraphicsSettingsPanel.tsx:611` · L6 UX · local · conf 0.75 · FLAG
  - **'Reset to 10am' and 'Reset Simulation' sit adjacent with near-identical styling — easy mis-click between mild and destructive action** — Lines 610-636: a benign amber 'Reset to 10am' button sits directly left of the destructive red 'Reset Simulation' (full localStorage wipe + reload), both same size in a `flex gap-2` row. The only differentiator is amber-vs-red tint at [10px] font. Combined with no confirmation (see related finding), the layout invites destructive mis-clicks. Same adjacency exists in SettingsPanel.tsx:452-473 (stacked).
  - _FIX:_ Visually separate the destructive action (own row, more padding, an explicit 'Danger zone' subheading) and/or require confirmation. Pair this with the confirmation-dialog fix.
- **[LOW]** `src/components/ui/MultiObjectiveDashboard.tsx:130` · L8 Visual · local · conf 0.4 · FLAG
  - **Top-left anchor collision: MultiObjectiveDashboard, ShiftHandoverSummary, and StatusHUD all stack at left-4/top** — Three independently-toggled overlays anchor to the top-left corner: MultiObjectiveDashboard 'fixed left-4 top-20 ... z-40' (line 130), ShiftHandoverSummary 'fixed top-16 left-4 z-50' (ShiftHandoverSummary.tsx:193), and StatusHUD default position {x:16,y:16} (StatusHUD.tsx:28). When the MultiObjective dashboard is open and a shift change fires, ShiftHandover (top-16=64px) renders directly over the dashboard's header (top-20=80px). ShiftHandover is transient (5s auto-dismiss) and on a higher z-index, so the overlap is brief, but the layout has no coordinated stacking.
  - _FIX:_ Offset ShiftHandoverSummary's top anchor when MultiObjectiveDashboard is visible, or move one panel to a different corner so the two cannot overlap.
- **[LOW]** `src/components/ui/VCLDebugPanel.tsx:277` · L8 Visual · local · conf 0.5 · FLAG
  - **Emoji legend rendered in VCLDebugPanel UI (debug surface, mirrors vclEncoder protocol)** — Lines 277-304 render emoji directly as UI legend text, e.g. line 286 `<div>🏛️ Silo | ⚙️ Mill | 🔀 Sifter | 📦 Packer</div>` and line 303 `<div>👑 Supervisor | 🔧 Engineer | 👷 Operator | 🛠️ Tech</div>`. These document the VCP/VCL emoji-encoding protocol in src/utils/vclEncoder.ts, where emoji are an intentional compact data format for LLM context (not decorative UI). This is a developer/debug panel, so it is borderline w.r.t. the no-emoji rule. Reporting low-confidence: the emoji here are a legend FOR an emoji protocol, which is a defensible exception. The vclEncoder.ts emoji themselves are data, not UI, and are NOT a violation.
  - _FIX:_ Acceptable to leave — this is a debug panel documenting an emoji-based protocol. If strict no-emoji compliance is wanted even in debug surfaces, gate the panel behind a dev flag (it may already be) and/or note the exception. No launch-blocking action.
- **[LOW]** `src/components/ui/WeatherControlPanel.tsx` · L11 DeadCode · local · conf 0.82 · FLAG
  - **ui/WeatherControlPanel.tsx is orphaned — referenced only by the dead ui/index.ts barrel** — Grep for 'WeatherControlPanel' (excluding own file and Archive) returns only src/components/ui/index.ts. No live consumer (WeatherEffectsOverlay is separate and also barrel-only). The single barrel reference is itself unimported, so this is dead.
  - _FIX:_ Remove the WeatherControlPanel export from ui/index.ts and delete the file after confirming no string-keyed reference.
- **[LOW]** `src/components/ui/index.ts:1-35` · L11 DeadCode · local · conf 0.88 · AUTO
  - **ui/index.ts barrel has zero importers — ui-new and UIOverlay reach ui/ panels by direct path, not the barrel** — No file imports the components/ui barrel (`from '.../components/ui'`). Consumers use direct paths, e.g. MissionControl.tsx:5 `import { MillClockDisplay } from '../../ui/MillClockDisplay'` and UIOverlay.tsx:66 `import { KeyboardShortcutsModal } from './ui/KeyboardShortcutsModal'`. The 35-line barrel (re-exporting all 28 ui/ panels) is dead and is the sole 'reference' to 4 otherwise-orphaned panels (next finding).
  - _FIX:_ Delete src/components/ui/index.ts (no runtime impact; never bundled). After deletion the 4 barrel-only panels below become provably unreferenced.
- **[LOW]** `src/hooks/useKeyboardShortcuts.ts:284` · L6 UX · local · conf 0.65 · FLAG
  - **Cost-overlay shortcut '$' (Shift+4) collides with the F4 'ultra' quality intent and is undiscoverable** — Line 284 binds the cost overlay to `key === '$'` (Shift+4). This shortcut appears in NO help surface (GraphicsSettingsPanel cheat-sheet, GeminiModal toggles list which wrongly says 'C', or App.tsx sr-only notice). A Shift+symbol shortcut is the least discoverable kind and is documented nowhere correctly, so the cost tracker is practically un-toggleable by keyboard.
  - _FIX:_ Either document '$' consistently across the GeminiModal toggle chip (currently 'C', line 59) and the cheat-sheet, or rebind to an unused letter and update all three help surfaces together.
- **[LOW]** `src/index.css:33` · L8 Visual · local · conf 0.95 · -
  - **POSITIVE: documented Tailwind v4 universal-reset bug is NOT present (clean)** — Per the CLAUDE.md known-bug section, verified there is NO universal `* { margin: 0; padding: 0; }` reset in src/index.css or the index.html inline <style>. index.html line 51 correctly uses scoped `body, html { ... margin: 0; ... }` and `#root { ... }` only. src/index.css base layer (lines 33-46) scopes to html/body/#root. All global base styling is inside `@layer base` / `@layer components` / `@layer utilities`, so cascade-layer precedence is respected and Tailwind utilities are not overridden. The 5 `!important` uses (lines 102-111) are all inside the prefers-reduced-motion media query, which is the standard legitimate a11y use. No action needed — recording as a verified non-issue so it is not re-flagged.
  - _FIX:_ No fix required. This confirms the recurring Tailwind v4 cascade bug is currently absent. Keep universal resets out of index.css and index.html per CLAUDE.md.
- **[LOW]** `src/index.css:53` · L8 Visual · local · conf 0.55 · FLAG
  - **Focus-visible outline and scrollbar use hardcoded hex instead of theme tokens** — src/index.css uses hardcoded colors that bypass the @theme tokens: line 73/83 `outline: 2px solid #22d3ee;` and line 85 `box-shadow: 0 0 0 4px rgba(34, 211, 238, 0.2);` (cyan-400, but --color-mill-cyan is #06b6d4 / cyan-500 — a DIFFERENT cyan), scrollbar thumb line 64 `rgba(100, 116, 139, ...)` (slate-500). The focus ring cyan (#22d3ee) does not match the brand mill-cyan token (#06b6d4), so the focus accent is a slightly different hue than brand accents elsewhere. Minor token/brand drift, not a defect.
  - _FIX:_ Optionally align the focus-visible outline to a defined token (either add a --color-focus-ring token or reuse cyan-400 consistently) so the focus accent matches the brand accent system. Cosmetic; safe to leave for launch.
- **[LOW]** `src/main.tsx:1` · L4 API/Errors · local · conf 0.85 · AUTO
  - **No global unhandledrejection / window.onerror handler — async rejections outside React are invisible** — Grep across src for 'unhandledrejection' / 'window.onerror' / 'onunhandledrejection' returns zero matches. React's ErrorBoundary (main.tsx:53, App.tsx:651) only catches render-phase errors; it does NOT catch unhandled promise rejections (e.g. the Rapier prewarm at main.tsx:10, dynamic imports, audio resume, fetch in modelLoader) or async errors thrown outside the React tree. At launch these fail completely silently with no logging or telemetry, making field diagnosis impossible. main.tsx already monkeypatches console.warn (:17), so it is the natural place to add a listener.
  - _FIX:_ In main.tsx add window.addEventListener('unhandledrejection', e => logger.error('[unhandledrejection]', e.reason)) and a window.addEventListener('error', ...) so background async failures are at least logged. No UX change.
- **[LOW]** `src/multiplayer/MultiplayerManager.ts:247-275` · L1 Correctness/Bugs - Integration (SCADA+multiplayer+protocols) · local · conf 0.65 · AUTO
  - **handlePeerDisconnected can run twice for one peer (onClose + stale sweep) → double PLAYER_LEAVE broadcast** — A peer can be torn down via two independent paths: the PeerConnection onClose callback wired in handlePeerConnected (line 146 `onClose: () => this.handlePeerDisconnected(peerId)`) and the stale-connection sweep in the ping interval (lines 486-487). There is no guard that the peer is still present before acting, so for a peer that goes stale and then fires 'close', handlePeerDisconnected runs twice: it broadcasts PLAYER_LEAVE twice (lines 262-265) and calls store.removeRemotePlayer/removePeer twice. Store removals are idempotent, but the duplicate PLAYER_LEAVE broadcast is wasted/again-untrusted traffic.
  - _FIX:_ Early-return if the peer is already gone: `if (!this.peerConnections.has(peerId)) return;` at the top of handlePeerDisconnected.
- **[LOW]** `src/multiplayer/MultiplayerManager.ts:313-321` · L2 Security · local · conf 0.68 · FLAG
  - **Guest accepts FULL_STATE_SYNC/STATE_SYNC from any connected peer, not verified-as-host** — case 'FULL_STATE_SYNC' / 'STATE_SYNC' only check `!store.isHost` before applying the payload to the production store (applyFullState/applyStateDiff dispatch window CustomEvents that drive machine state). They do not verify the message actually came from the host connection. In the current topology a guest connects only to the host, but PeerJS allows any peer to open a DataConnection to the guest's peer-id (`millos-${roomCode}-${playerId}`, predictable). Such a peer could push a forged FULL_STATE_SYNC to overwrite the guest's machine state, weather, and machine locks.
  - _FIX:_ Record the host's peerId on join and, in these branches, verify the originating peerId matches the known host before applying. Drop state-sync messages from any non-host peer.
- **[LOW]** `src/multiplayer/MultiplayerManager.ts:354-355` · L2 Security · local · conf 0.7 · FLAG
  - **CHAT message stored from untrusted payload with attacker-controlled 'from'/'fromName' and no length bound** — case 'CHAT': store.addChatMessage(message.payload). The entire ChatMessage (id, from, fromName, message, timestamp) is taken from the untrusted peer with no validation. from/fromName can be spoofed to impersonate another player or the host in the chat UI, and message has no length cap (DoS / store bloat). Rendering is via React JSX (MultiplayerChat.tsx auto-escapes, so no DOM XSS), but identity spoofing in chat and unbounded growth remain.
  - _FIX:_ Validate CHAT payload shape and cap message length (e.g. 500 chars). On the host, overwrite from/fromName with the sender's authenticated peer identity before re-broadcasting, so guests cannot forge sender identity.
- **[LOW]** `src/multiplayer/PeerConnection.ts:176-178` · L2 Security · local · conf 0.6 · FLAG
  - **Connection metadata trusted for playerId without validation (disconnect/eviction targeting)** — getMetadata() returns the raw conn.metadata cast to Record<string,unknown>. MultiplayerManager.handlePeerDisconnected (line 251-255) reads metadata.playerId and uses it (when host) as the player id to removeRemotePlayer/removePeer and broadcast PLAYER_LEAVE. metadata is fully controlled by the connecting peer (set client-side in SignalingService.connectToHost metadata, lines 122-125). A guest can set metadata.playerId to another player's id so that, on its own disconnect, the host evicts the victim instead.
  - _FIX:_ On the host, map peerId->playerId at verified join time and use that map for disconnect cleanup; never trust metadata.playerId for destructive store operations. Treat metadata as a display hint only.
- **[LOW]** `src/multiplayer/SignalingService.ts:29` · L2 Security · local · conf 0.55 · AUTO
  - **PeerJS debug uses process.env.NODE_ENV in a Vite/browser context (likely undefined → never debug, minor)** — PEERJS_CONFIG.debug = process.env.NODE_ENV === 'development' ? 2 : 0 (line 29). Vite exposes import.meta.env, not process.env, in browser code; process.env.NODE_ENV is statically replaced by Vite's define only if configured, otherwise it is undefined at runtime. Practically this means debug is always 0 (the safe branch), so there is no information-leak risk — but it is a latent correctness smell indicating the env check does not do what it reads as, and if a future define exposes process.env in prod it could flip verbose PeerJS logging on. Not a security defect today; flagged for the env-API mismatch.
  - _FIX:_ Use import.meta.env.DEV (consistent with serviceWorkerRegistration.ts:45) instead of process.env.NODE_ENV for the debug gate. Behavior-neutral today, removes the latent footgun.
- **[LOW]** `src/multiplayer/SignalingService.ts:53-57` · L2 Security · local · conf 0.6 · FLAG
  - **Host peer-id squatting: predictable id enables room hijack/denial before host registers** — Host peer-id is the deterministic `millos-${roomCode}` (line 54). An attacker who learns/guesses a code can register that exact peer-id on the shared public PeerJS server first, causing the legitimate host's Peer() to fail with 'unavailable-id' (handled at line 82) OR letting the attacker impersonate the host to guests that connect to `millos-${roomCode}`. There is no proof-of-host (signature/secret) binding the room to the legitimate creator.
  - _FIX:_ Bind room ownership to a secret: host generates a random host token, derives a non-guessable peer-id (e.g. include a high-entropy suffix not shown to guests until they prove the join code), and guests verify a host-signed value in metadata. Minimum: self-host the signaling server so id allocation can be authenticated.
- **[LOW]** `src/multiplayer/hooks/useMultiplayerSync.ts:110-151` · L1 Correctness/Bugs - Integration (SCADA+multiplayer+protocols) · local · conf 0.6 · FLAG
  - **applyStateDiff treats the host's full STATE_SYNC as a diff; sequence number is ignored and emergencyActive is never applied on guests** — The host's STATE_SYNC payload (MultiplayerManager.ts:448-458) is a full snapshot (entire machines array + sequence + timestamp), not a diff, but is consumed by applyStateDiff as GameStateDiff. The `sequence` field is dropped (no ordering/staleness check — benign because the channel is reliable:true). More concretely, the emergencyActive branch is a no-op: `if (diff.emergencyActive && !gameStore.emergencyActive) { /* In a full implementation, this would trigger the full emergency sequence */ }` (lines 145-150) — the comment confirms guests never react to host emergency state, so a fire-drill/emergency started by the host does not propagate to guests.
  - _FIX:_ Implement the emergencyActive propagation on guests (call the gameSimulation emergency trigger) and rename/treat STATE_SYNC honestly as a snapshot. Behavior change — owner decision on the emergency-sync semantics.
- **[LOW]** `src/multiplayer/types.ts:20` · L4 API/Errors · local · conf 0.85 · FLAG
  - **ConnectionState 'reconnecting' is defined but never set — dead UI state / misleading reconnect contract** — types.ts:20 declares `type ConnectionState = 'disconnected' | 'connecting' | 'connected' | 'reconnecting'`. SignalingService.ts:93-97 logs 'attempting reconnect' and relies on PeerJS auto-reconnect, but nothing in MultiplayerManager or the multiplayer store ever calls setConnectionState('reconnecting') (grep: no setter usage of the literal). On PeerJS 'disconnected', the guest path goes straight to handleHostDisconnect (MultiplayerManager.ts:271) / setConnectionState('disconnected') (:103) with no reconnecting phase. Any UI keyed on 'reconnecting' is dead code, and the auto-reconnect is invisible to the user.
  - _FIX:_ Either wire setConnectionState('reconnecting') into the SignalingService 'disconnected' handler (and back to 'connected' on reopen) so the UI reflects the auto-reconnect, or remove the unused literal from the union. First option changes UX — flag for owner if chosen.
- **[LOW]** `src/prototypes/AssetPrototypePage.tsx:1` · L11 DeadCode · local · conf 0.7 · FLAG
  - **Internal deck public at /prototypes** — 4200-line dev deck routable in prod. Posture question.
  - _FIX:_ Owner: keep/gate/remove.
- **[LOW]** `src/scada/SCADABridge.ts:219` · L1 Correctness/Bugs - Integration (SCADA+multiplayer+protocols) · local · conf 0.5 · -
  - **calculateMachineVisuals omits machineName, so name-only machines resolve no SCADA prefix (visuals blank while store metrics sync)** — scadaToStoreMetrics passes machineName to getMachineTagPrefix (line 390, called as `scadaToStoreMetrics(machine.id, ..., machine.name)` in store.ts:179), enabling the display-name fallbacks for 'RM-101'/'SIFTER A'/'PACK LINE N' (getMachineTagPrefix lines 168-197). But calculateMachineVisuals calls `getMachineTagPrefix(machineId)` with no name (line 219, invoked from useSCADAVisuals.ts:30/47). For any machine whose id does NOT match the id-prefix patterns (silo-/rm-/sifter-/packer-) but whose name would, the prefix is null in calculateMachineVisuals → no temperature/vibration/rpm visuals, while scadaToStoreMetrics still syncs metrics. Inconsistent behavior between the two paths. Only bites if such name-only machines exist (roster ids not in types.ts/store.ts inline, unverified).
  - _FIX:_ Thread machineName through calculateMachineVisuals and pass it to getMachineTagPrefix, matching scadaToStoreMetrics. Confirm a name-only machine actually exists before treating as more than a consistency cleanup.
- **[LOW]** `src/scada/SCADABridge.ts:498` · L4 API/Errors · local · conf 0.6 · AUTO
  - **applyCorrelations divides by engHigh-engLow with no guard — NaN/Infinity if a correlated tag has zero range** — `const normalizedCorrelated = ((correlatedValue.value as number) - correlatedDef.engLow) / (correlatedDef.engHigh - correlatedDef.engLow);` (lines 497-499). If a correlated tag definition has engHigh === engLow (degenerate/misconfigured range), this is division by zero → Infinity/NaN, which then propagates into the simulated value via correlationEffect (line 502-505) and ultimately into TagValue.value. Although tick() later clamps to engLow/engHigh of the *target* tag (SimulationAdapter line 411), a NaN survives Math.max/Math.min as NaN and can reach the 3D bridge.
  - _FIX:_ Guard the denominator: `const range = correlatedDef.engHigh - correlatedDef.engLow; if (range <= 0) return;` (skip that correlation) before computing normalizedCorrelated.
- **[LOW]** `src/scada/adapters/PIAdapter.ts:76-79` · L2 Security · local · conf 0.62 · FLAG
  - **PI Basic-auth credentials base64-encoded with no TLS enforcement on baseUrl** — case 'basic': const credentials = btoa(`${config.username}:${config.password}`); this.authHeader = `Basic ${credentials}`. The baseUrl is used verbatim in fetchWithAuth with no scheme check (PIAdapter.ts:338-360, no `new URL()` validation at all, unlike RESTAdapter). If a user configures an http:// PI Web API baseUrl, the Basic credentials are transmitted in cleartext. The comment at line 354 ('SSL verification is browser-controlled') acknowledges TLS is not enforced by this code.
  - _FIX:_ Before sending Authorization, assert this.baseUrl uses https:// (or host is localhost); otherwise throw a clear configuration error. Add `new URL()` validation to PIAdapter.fetchWithAuth to match RESTAdapter's guard.
- **[LOW]** `src/scada/adapters/RESTAdapter.ts:270` · L4 API/Errors · local · conf 0.55 · FLAG
  - **REST getStatistics readsPerSecond/uptime computed from connectTime even across reconnect churn; no avgReadLatency reset** — getStatistics (lines 270-280) computes `uptime = (Date.now() - this.connectTime)/1000` and divides cumulative readCount by it. On reconnect, connect() resets connectTime (line 91) but stats (readCount/totalLatency) are never reset, so readsPerSecond is computed against a fresh uptime denominator with a stale cumulative numerator, yielding inflated rates right after reconnect. Diagnostic-only, but misleading for operators reading the stats panel.
  - _FIX:_ Either reset the stats object in connect()/disconnect(), or track a separate `statsWindowStart` independent of connectTime so rate denominators match the counter accumulation window.
- **[LOW]** `src/scada/adapters/RESTAdapter.ts:286` · L4 API/Errors · local · conf 0.72 · FLAG
  - **poll() swallows all errors as 'connection lost' — distinguishes neither HTTP status nor parse failures** — `private async poll() { ... try { ... } catch { this.handleConnectionError(); } }` (lines 286-295). Any throw — auth 401/403, malformed JSON, a single bad batch — is treated identically as a transient connection error driving exponential backoff. A 401 (bad API key) will never recover via reconnect and just burns 5 attempts then tears down subscribers. No status-code-specific handling.
  - _FIX:_ In readTags/readTag surface the HTTP status on the thrown error, and in poll's catch distinguish auth/4xx (terminal, set lastError and stop) from 5xx/network (retry).
- **[LOW]** `src/scada/adapters/RESTAdapter.ts:354` · L2 Security · local · conf 0.7 · FLAG
  - **RESTAdapter sets Bearer token via raw fetch, bypassing secureFetch timeout/validation wrapper** — Line 354 `headers.set('Authorization', 'Bearer ${this.config.apiKey}')` is sent via raw fetch at line 332 (`return await fetch(url, { ...options, signal: controller.signal })`). The codebase ships a secureFetch wrapper (apiSecurity.ts:512) with timeout + same-origin credentials + audit hooks specifically for this, but RESTAdapter does not use it. The Bearer key (SCADA endpoint credential) rides on whatever URL config.url points to with no allowed-host validation. Confirms the secureFetch dead-code finding from the consumer side.
  - _FIX:_ Owner decision: either standardize RESTAdapter on secureFetch (gains timeout/audit, but changes request semantics), or document that raw fetch is intentional and remove the unused secureFetch. Adapter behavior change needs validation against the 1199-test baseline.
- **[LOW]** `src/scada/adapters/WonderwareAdapter.ts:59` · L4 API/Errors · local · conf 0.45 · FLAG
  - **OPC quality mask comment says 16-bit but math assumes 8-bit; values >0xFF mis-classified** — mapOpcQuality does `const major = (opcQuality >> 6) & 0x3;` while the doc comment (lines 56-58) states 'OPC quality is 16-bit: high 8 bits = major'. For a standard 8-bit OPC DA quality byte, `(q>>6)&0x3` correctly extracts the 2-bit major field, so current behavior is right for 8-bit codes. But if Wonderware's REST API returns the full 16-bit OPC quality word (which some AVEVA endpoints do), the major bits are at >>14, and `(q>>6)&0x3` reads substatus bits instead — every value would be mis-graded. Contract ambiguity between comment and code.
  - _FIX:_ Confirm the WW REST API's Quality field width. If 8-bit, fix the comment to say 8-bit. If 16-bit is possible, normalize first: `const q8 = opcQuality > 0xff ? (opcQuality >> 8) : opcQuality; const major = (q8 >> 6) & 0x3;`
- **[LOW]** `src/stores/aiConfigStore.ts:330` · L6 UX · local · conf 0.7 · FLAG
  - **Debug overlays default ON for end users (VCL Context + Shift Handover)** — Lines 330 and 334: `showVCLDebug: true` and `showShiftHandover: true` default to enabled, while every other AI visualization toggle defaults false (lines 327-333 comment explicitly says 'all default OFF'). showVCLDebug in particular reads as a developer debug overlay; shipping it on by default clutters a first-time user's screen and contradicts the stated 'all default OFF' intent.
  - _FIX:_ Set showVCLDebug to false (and reconsider showShiftHandover) so the launch experience is clean; let users opt in via the toggles. Confirm with owner whether Shift Handover is intended-on.
- **[LOW]** `src/stores/multiplayerStore.ts:331-337` · L3 Privacy/GDPR — Multiplayer data sharing · local · conf 0.75 · FLAG
  - **Other users' chat and names held in volatile memory only — good minimization, but no notice; chat is plaintext over the channel** — Positive note for data-minimization: the multiplayer store keeps remote players, peers, and chat in in-memory Maps/arrays with no persistence — grep confirmed no localStorage/sessionStorage/IndexedDB usage anywhere under src/multiplayer or multiplayerStore.ts. addChatMessage caps history at 100 (`chatMessages: [...state.chatMessages.slice(-99), message]`) and leaveRoom()/reset() drop it. The residual privacy point: chat messages carry `fromName` (display name) and are relayed to all peers via host re-broadcast (MultiplayerManager.handleMessage CHAT case) with no content disclosure or retention notice to participants. No GDPR storage issue, but the absence of any disclosure that chat is shared in cleartext with all room peers (and re-broadcast by the host) is worth flagging.
  - _FIX:_ Owner decision: include chat in the same pre-join disclosure ('chat is visible to everyone in the room'). No code change needed for the minimization aspect, which is already sound.
- **[LOW]** `src/systems/bas/educationalContent.ts:2` · L7 Copy · ripple · conf 0.75 · FLAG
  - **Educational content names the system 'BAMS' while README/UI call it 'BAS'** — educationalContent.ts uses 'BAMS' / 'Bilateral Alignment Management System' (header line 2, plus lines 88, 138, 161, 221, 235, 387, 582) while the README and the rest of the app exclusively use 'BAS' / 'Bilateral Autonomy System' (README §'Bilateral Autonomy System (BAS)'). A user reading a tooltip sees 'BAMS continuously monitors...' / 'BAMS Spec Section 6.4' with no definition of BAMS anywhere user-facing. Note 'BAMS' also appears across ~15 src files, so this is a project-wide naming drift, not a single-file slip.
  - _FIX:_ Standardize on one acronym. If BAS is canonical (README uses it), replace BAMS->BAS in educationalContent.ts user-visible strings. Because the term spans ~15 files and a source citation ('BAMS Spec Section 6.4'), owner should decide the canonical name before a global rename.
- **[LOW]** `src/utils/aiEngine.ts:3390` · L1 Correctness/Bugs · local · conf 0.8 · AUTO
  - **AI engine teardown leaks pending safety-report resolution timeouts** — `initializeAIEngine()`'s returned cleanup (lines 3390-3398) clears `loopInterval` but never clears the module-level `activeResolutionTimeouts` map (declared line 3978, populated line 4109 with 5000ms setTimeouts via `activeResolutionTimeouts.set(report.id, timeoutId)`). After teardown these timeouts still fire and call `useSafetyReportStore.getState().resolveSafetyReport(...)` against a store that may belong to a torn-down session. The debounceTimeout in initializeDecisionOutcomeTracking IS cleaned up (line 3028) — this map is the inconsistent gap. Low impact (one-shot 5s timers, store calls are idempotent) but a genuine lifecycle leak.
  - _FIX:_ In the cleanup closure, add: `for (const id of activeResolutionTimeouts.keys()) clearTimeout(activeResolutionTimeouts.get(id)!); activeResolutionTimeouts.clear();` before/after clearing loopInterval. Stays inside the file; no behavior change in normal operation.
- **[LOW]** `src/utils/aiEngine.ts:3891` · L4 API/Errors · local · conf 0.7 · -
  - **No AbortController on in-flight Gemini strategic call — late response can resolve after config/key change** — generateStrategicDecision (aiEngine.ts:3891) awaits geminiClient.generateContent(prompt) with no AbortController/cancellation. If the user clears their key or disconnects (clearGeminiConfig at aiConfigStore.ts:527 calls geminiClient.disconnect()) while a request is in flight, the awaited promise still resolves, recordApiUsage runs (:3900) charging the cost overlay, and setStrategicPriorities/recordDecision fire for a now-disconnected client. The legacy SDK's generateContent does accept an AbortSignal via requestOptions, so this is wireable. Low because it is a transient correctness/cost-accounting issue, not a crash.
  - _FIX:_ Thread an AbortController through geminiClient.generateContent (pass { signal } as the SDK requestOptions second arg) and abort it from disconnect(). At minimum, re-check geminiClient.isConnected() after the await before recording cost/priorities.
- **[LOW]** `src/utils/aiEngine.ts:3921` · L4 API/Errors · local · conf 0.8 · FLAG
  - **Emoji in user-facing AI decision text violates project No-Emoji rule** — CLAUDE.md mandates 'Never use emoji characters in the codebase' (only the 🏭 branding exception). aiEngine.ts:3921 sets `action: \`🧠 Strategic: ${strategic.priorities[0]}\``, and :3913-3914 build reasoning text with 💡 and ⚖️. These strings flow into AIDecision.action/reasoning and are rendered verbatim in the AI decision panels (AICommandCenter). Line 3756 even has to strip the '🧠 Strategic: ' prefix back out, confirming it is real rendered content. Lane-marginal and cosmetic, but a genuine guideline violation in shipped UI text.
  - _FIX:_ Replace the emoji prefixes with Lucide icons in the rendering component (per CLAUDE.md), or drop the inline emoji and keep the label plain ('Strategic: ...', 'Insight: ...', 'Trade-off: ...'). Changes displayed text — flag for owner.
- **[LOW]** `src/utils/aiEngine.ts:3951` · L1 Correctness/Bugs · local · conf 0.75 · AUTO
  - **Strategic Gemini failures never trigger API exponential backoff** — `generateStrategicDecision()` swallows its own errors: `catch (error) { logger.error('[Strategic] Decision generation failed:', error); setStrategicThinking(false); return null; }` (lines 3951-3955) and returns null instead of rethrowing. The AI-loop caller wraps it in try/catch (lines 3292-3308) expecting to call `recordApiFailure()` on throw — but because the error is swallowed and null returned, the loop sees a benign null and `recordApiFailure()` (line 3304) never runs for strategic API errors. So repeated Gemini failures on the strategic path do not increment apiBackoff.consecutiveFailures, defeating the backoff for that layer. NOTE: do NOT route the loop-level null to recordApiFailure — null is also the benign no-op return (not active / not connected / no priorities).
  - _FIX:_ Add `recordApiFailure();` inside the catch block at line 3952 (the genuine-error site), alongside the existing logger.error. Local, single-file.
- **[LOW]** `src/utils/apiSecurity.ts:106` · L2 Security · local · conf 0.9 · FLAG
  - **rateLimiters (gemini/chat/connect) exported but never wired — no rate limiting active** — rateLimiters.gemini/.chat/.connect are pre-built wrappers intended to throttle Gemini calls, chat spam, and reconnection storms, but grep for `rateLimiters.` across src/ (minus def+barrel) returns nothing. So untrusted-peer chat (sendChat at MultiplayerManager.ts:596) has no spam limit and Gemini calls have no client-side quota guard despite the wrappers existing. checkRateLimit itself is also unused outside this file.
  - _FIX:_ Wire rateLimiters.chat around sendChat() in MultiplayerManager.ts and rateLimiters.gemini around geminiClient.generateContent calls, OR remove the unused exports. Wiring changes runtime behavior (messages/requests can now be dropped) so it needs an owner decision; the removal path is the safe launch option.
- **[LOW]** `src/utils/apiSecurity.ts:268` · L2 Security · local · conf 0.95 · FLAG
  - **JWT helpers (decodeTokenPayload/isTokenExpired/getTokenExpiresIn) exported but never used** — decodeTokenPayload (268), isTokenExpired (306), getTokenExpiresIn (323) and TokenPayload type are 'for future auth integration' (per file header) but have ZERO consumers — grep across src/ minus def+barrel returns nothing. The app has no auth/JWT flow (frontend-only, no backend). Re-exported via index.ts:129-131. Dead scaffolding shipping to production.
  - _FIX:_ Remove these three exports + TokenPayload from utils/index.ts and delete the unused functions, or move to a clearly-marked future/ stub. Dead-code cleanup, no runtime impact.
- **[LOW]** `src/utils/apiSecurity.ts:344` · L2 Security · local · conf 0.95 · FLAG
  - **CSRF helpers (generate/getOrCreate/clearCsrfToken) exported but never used — dead scaffolding** — generateCsrfToken (344), getOrCreateCsrfToken (361), clearCsrfToken (375) are only consumed internally by secureFetch (line 553), which itself has no callers. Grep for these three across src/ (minus def+barrel) returns nothing. With no backend/server auth (frontend-only GitHub Pages app), client-side CSRF tokens validate against nothing. They are dead exports re-exported via index.ts:132-134.
  - _FIX:_ Remove the three CSRF exports from utils/index.ts and delete the unused functions (and CSRF_TOKEN_KEY) once secureFetch is also removed. Pure dead-code deletion; no runtime behavior depends on them.
- **[LOW]** `src/utils/apiSecurity.ts:512` · L2 Security · local · conf 0.95 · FLAG
  - **secureFetch() exported but never used; RESTAdapter uses raw fetch instead** — secureFetch() (rate limiting, CSRF header injection, timeout, same-origin credentials) is defined here and re-exported via utils/index.ts:136, but has ZERO consumers project-wide (grep across src/ minus the def+barrel returns nothing). The actual SCADA caller src/scada/adapters/RESTAdapter.ts:332 uses raw `fetch(url, { ...options, signal })` and manually sets `Authorization: Bearer ${this.config.apiKey}` at line 354 rather than secureFetch. So the 'secure' wrapper is dead scaffolding.
  - _FIX:_ Either delete secureFetch + its SecureFetchOptions type and remove from the barrel (dead-code cleanup), or have RESTAdapter adopt it. For launch, removing the unused export is the low-risk path; adopting it in RESTAdapter changes runtime request behavior and needs an owner call.
- **[LOW]** `src/utils/apiSecurity.ts:612` · L2 Security · local · conf 0.85 · FLAG
  - **maskSensitiveData / clearSensitiveFields exported but never used; key masking done ad hoc** — maskSensitiveData (612) and clearSensitiveFields (633) are exported via index.ts:137-138 but have ZERO consumers. GeminiSettingsModal.tsx:237 displays the key via geminiClient.getMaskedApiKey() (geminiClient.ts:165), which re-implements masking inline (`slice(0,4)...slice(-4)`) instead of using the shared maskSensitiveData. Duplicated logic + dead export.
  - _FIX:_ Either have getMaskedApiKey() delegate to maskSensitiveData (de-dupe), or remove maskSensitiveData/clearSensitiveFields from the barrel as dead code. Consolidation is low-risk but touches geminiClient output format slightly; removal is cleanest for launch.
- **[LOW]** `src/utils/audioManager.ts:5487` · L1 Correctness/Bugs · local · conf 0.7 · AUTO
  - **Production TTS pipeline emits ~14 unconditional console.log debug lines** — The announcement/TTS path logs verbosely to the raw console regardless of environment: e.g. line 5480 `console.log('[TTS] Queue empty, stopping')`, 5487 `console.log('[TTS] Blocked in queue processing:', {...})`, 5510, 5559 `console.log('[TTS] Speech STARTED')`, 5565, 5593, 5602, 5614-5619, 5624. These use `console.log` directly rather than the project's gated `audioLog`/`logger` abstraction used elsewhere in the same file (e.g. audioLog.warn at 5580/5631). On every PA announcement this spams the production console at millos.net. Not a crash, but console noise and minor info leak of internal state.
  - _FIX:_ Route these through the existing gated logger (audioLog.debug) or guard with import.meta.env.DEV. Mechanical, no runtime UX change.
- **[LOW]** `src/utils/dracoLoader.ts:33,48` · L2 Security · local · conf 0.7 · FLAG
  - **DRACO JS decoder executed as third-party code from www.gstatic.com (remote-code dependency)** — DRACO_DECODER_PATH = 'https://www.gstatic.com/draco/versioned/decoders/1.5.7/' (line 33) with setDecoderConfig({ type: 'js' }) (line 48). Three's DRACOLoader fetches draco_decoder.js / draco_wasm_wrapper.js and runs them inside a blob Worker. Although the Worker runs from a blob (allowed by worker-src 'self' blob:), the executed code is fetched at runtime from a Google-controlled CDN and is not subresource-integrity-pinned. A compromise or version change at gstatic would execute attacker-controlled JS in the worker context. This is a deliberate trade-off (offline-fallback path to '/draco/' is commented at line 36), but for launch it pins integrity to Google's CDN with no SRI.
  - _FIX:_ Vendor the DRACO 1.5.7 decoder into public/draco/ and switch DRACO_DECODER_PATH to '/draco/' (the commented local path), removing the runtime third-party code fetch entirely and fixing the CSP gap in the same stroke. This also makes DRACO models work offline via the existing SW MODEL_CACHE. Behavior-neutral if the local decoder matches the bundled three version.
- **[LOW]** `src/utils/geminiClient.ts:130` · L2 Security · local · conf 0.6 · FLAG
  - **API key retained on GeminiClient instance and never cleared from response cache scope** — initialize() stores `this.apiKey = apiKey` (line 130). disconnect() nulls it (line 177), which is correct. The key is never logged (getMaskedApiKey masks correctly), so no leak via logger. The residual concern is only that the in-memory plaintext key plus the persisted localStorage copy (aiConfigStore.ts:558) means two live plaintext copies; clearSensitiveFields() exists for exactly this but is unused. Lower confidence because JS gives no real memory-zeroing guarantee anyway and disconnect already nulls the reference.
  - _FIX:_ No code change strictly required; if hardening, route key clearing through clearSensitiveFields in disconnect() for consistency. Primarily reinforces the plaintext-localStorage finding above — fix that one first.
- **[LOW]** `src/utils/geminiClient.ts:146` · L2 Security · local · conf 0.85 · AUTO
  - **Misleading log: 'Initialized with Gemini 2.0 Flash' while model is 'gemini-3-flash-preview'** — Line 135 configures `model: 'gemini-3-flash-preview'` but the success log at line 146 says `logger.info('[GeminiClient] Initialized with Gemini 2.0 Flash')`. Mislabeled diagnostic; not a security hole but degrades incident-response accuracy when reading logs about which model handled a request. Pricing constants in aiConfigStore.ts:26-27 are labeled 'Gemini Flash ... as of Dec 2024' which also may not match gemini-3 pricing — flagging the log-string mismatch as the concrete, citable issue.
  - _FIX:_ Change the log string at line 146 to match the actual model id, e.g. `logger.info('[GeminiClient] Initialized with gemini-3-flash-preview')`.
- **[LOW]** `src/utils/geminiClient.ts:48` · L4 API/Errors · local · conf 0.72 · -
  - **Context-overflow detection relies on brittle English substring matching of error messages** — isContextOverflowError (geminiClient.ts:256-261) matches error.message.toLowerCase() against a hardcoded English phrase list (CONTEXT_OVERFLOW_PATTERNS at :30-37: 'context length','token limit','too long', etc.). The legacy SDK's actual overflow errors carry structured status codes (e.g. INVALID_ARGUMENT) and provider-specific wording that may not contain any of these phrases. When it misses, the overflow is counted as a normal failure (recordFailure at :317), needlessly advancing the circuit breaker toward open instead of triggering the intended heuristic fallback (:310-314). Low because truncation at :293 already caps prompt size, making real overflows rare.
  - _FIX:_ Prefer matching on the SDK error's status/code (e.g. err.status === 'INVALID_ARGUMENT' or HTTP 400 with a token-limit reason) and keep the substring list only as a secondary heuristic.
- **[LOW]** `src/utils/index.ts:1-147` · L11 DeadCode · local · conf 0.92 · AUTO
  - **utils/index.ts barrel has zero importers — every consumer imports source modules directly** — No file in src imports the utils barrel by bare path (no 'from ".../utils"' / 'from "@/utils"'). Spot-checks confirm all consumers go direct: App.tsx:30 `import { audioManager } from './utils/audioManager'`, Machines.tsx:9 `import { getStatusColor } from '../utils/statusColors'`, Machines.tsx:12 `import { ... } from '../utils/sharedMaterials'`. The 147-line barrel re-exports ~80 symbols that nothing consumes through it. This is what makes the dead apiSecurity module appear 'reachable' to a casual reader, though apiSecurity is dead independently.
  - _FIX:_ Delete src/utils/index.ts (zero runtime impact — never in the bundle since nothing imports it). Tests confirmed not to import it.
- **[LOW]** `src/utils/pathfinding.ts:7` · L11 DeadCode · local · conf 0.85 · FLAG
  - **Dead A-star module, zero importers** — Header TODO DEAD CODE; refs elsewhere are comments. Exports unused.
  - _FIX:_ Delete or convert TODO to issue.
- **[LOW]** `src/utils/sanitize.ts:224` · L2 Security · local · conf 0.9 · FLAG
  - **sanitizeWorkerName / sanitizeUrl / isUrlSafe / decodeHtmlEntities exported but never used** — sanitizeWorkerName (224), sanitizeUrl (254), isUrlSafe (287), decodeHtmlEntities (57), validateObject/createStringValidator/createNumberValidator are exported via index.ts:99-115 but grep across src/ (minus def+barrel) returns ZERO consumers. These are unused sanitization scaffolding shipped to the bundle. Not a vulnerability by itself but inflates bundle and signals unfinished security wiring.
  - _FIX:_ After wiring the chat/name sanitizers (the genuinely needed ones), prune the remaining unused sanitize exports from utils/index.ts or wire sanitizeWorkerName where custom worker names are accepted. Dead-code cleanup, no runtime impact.
- **[LOW]** `src/utils/serviceWorkerRegistration.ts:231-247` · L2 Security · local · conf 0.6 · AUTO
  - **SW message handler trusts any message origin/shape (no source/type validation)** — In public/sw.js the message handler (lines 231-247) destructures event.data.type without verifying event.origin or that event.data is an object, and replies on event.ports[0] for CLEAR_CACHE and GET_CACHE_SIZE. Any script in any client controlled by this SW (same-origin only, so impact is bounded) can clear all millos- caches or enumerate cached URL pathnames (getCacheStats returns full URL pathnames, lines 259-262). For a same-origin-only app this is low severity, but a successful same-origin XSS gains a cache-wipe / cache-enumeration primitive for free.
  - _FIX:_ Guard the handler: 'if (!event.data || typeof event.data !== "object") return;' and validate type against an allowlist before acting. Optionally restrict GET_CACHE_SIZE detail (drop full pathnames, return counts only). Low-risk hardening, no UX change.
- **[LOW]** `src/utils/texturePreloader.ts:21-58, src/App.tsx:410` · L9 Performance · local · conf 0.7 · -
  - **16 procedural canvas textures (up to 512x512) generated synchronously on the main thread during boot useEffect** — preloadGenerativeTexturesOnce() runs in an App.tsx:410 mount useEffect. Though wrapped in `new Promise(resolve => {...})`, the body (texturePreloader.ts:28-53) runs all 16 generate* calls SYNCHRONOUSLY before resolve() — incl. generateConcrete(512,...) twice and generatePanelNormal(512,...). Main-thread CPU work during the first frames after mount (code measures/logs elapsed ms), adding TTI jank on low-end devices.
  - _FIX:_ Defer to requestIdleCallback or chunk the 16 generations across animation frames so they don't block the first interactive frames. Low risk; verify magnitude before prioritizing.
- **[LOW]** `src/workers/textureWorker.ts:107,150 + src/hooks/useTextureWorker.ts:92` · L9 Performance · local · conf 0.7 · -
  - **If kept: texture worker uses structured-clone (no Transferables) and TextureWorkerManager lacks onerror/fallback** — If kept rather than deleted: worker results return ImageData via `self.postMessage({ ..., result })` (textureWorker.ts:107) with no transfer list, and the hook posts via `workerRef.current.postMessage({type,id,payload})` (useTextureWorker.ts:92) — full-copy structured clone of potentially 2048x2048 RGBA buffers (~16MB) per decode instead of transferring the ArrayBuffer. Also TextureWorkerManager (useTextureWorker.ts:151) sets onmessage but NO onerror handler and has no main-thread decode fallback if the module worker fails to construct.
  - _FIX:_ Prefer deleting (prior finding). If kept: transfer ImageData.data.buffer in postMessage transfer lists, add onerror handler + main-thread createImageBitmap fallback in TextureWorkerManager.
- **[LOW]** `vite.config.ts:54` · L12 Config · local · conf 0.9 · AUTO
  - **loadEnv return value discarded (dead call)** — `loadEnv(mode, '.', '');` is called but its return value is unused. The commented-out `define` block below it (lines 75-78) once consumed it via `env.GEMINI_API_KEY`. Now nothing reads loaded env in the config, and base path is derived from `process.env.VERSION` directly (line 56). The call is a harmless no-op but is dead code left from the removed key-embedding logic.
  - _FIX:_ Remove the `loadEnv(mode, '.', '');` line, or assign and actually use it. No behavior change. The adjacent SECURITY comment block (lines 73-78) correctly documents why the API-key define is disabled — keep that.
- **[LOW]** `vite.config.ts:96-116 + src/components/SPCCharts.tsx:28 + src/components/ComplianceDashboard.tsx:43` · L9 Performance · local · conf 0.6 · -
  - **recharts (343kB) chunk risk — verify SPCCharts/ComplianceDashboard sit behind a lazy boundary** — manualChunks puts recharts in a 'charts' chunk (vite.config.ts:109). ProductionMetrics (lazy via UIOverlay.tsx:74) and SCADAPanel (lazy via panelPreloader) are deferred, but SPCCharts.tsx:28 and ComplianceDashboard.tsx:43 import recharts directly. Whether the 343kB charts chunk loads on boot depends on whether these two are reachable only through a lazy boundary. If an eager parent renders them, the charts chunk loads for users who never open a chart panel. Lower confidence — needs a real build chunk-graph check.
  - _FIX:_ Audit the import parents of SPCCharts.tsx and ComplianceDashboard.tsx; ensure every recharts consumer is reachable only through a lazy() boundary so the 343kB charts chunk is demand-loaded.